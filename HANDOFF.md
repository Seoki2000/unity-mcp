# 작업 인수 (다음 세션용)

최종 갱신: 2026-08-28 (인수 스위트 부트스트랩 결함 + **메서드 줄 범위 디코딩 버그**(전수 27.4%)
+ `lineKind` + 개수 필드 이름 정정 + 독립 검증 후속 2라운드(4건). `dev-0.0.7`)
브랜치: `optimized` — 정본. **2026-08-28 세션 커밋은 로컬에만 있다(push 는 사용자 확인 후).**
정확한 수는 `git rev-list --count origin/optimized..HEAD` 로 볼 것 — 여기 숫자를 적으면 낡는다.
지난 작업 브랜치들을 fast-forward 머지해 합치고 지웠다.
게임 프로젝트의 핀은 `9d420e6`(이 브랜치의 조상)을 가리킨다.
**`main` 이 아니다. §1 주의사항 참조**

---

## 0. 30초 요약

Unity MCP 포크를 "에디터 원격조작"에서 "프로젝트 이해"로 옮기는 작업.
Unreal Fest 의 NarshaADK 세션(강현우, 넥스트스테이지)에서 출발했다.
핵심 명제: **"문제는 AI 의 능력이 아니라 컨텍스트. 로직의 절반은 바이너리 안에 있다."**

Unity 로 번역하면 — `.cs` 는 AI 가 이미 읽는다. 못 읽는 절반은
`.prefab`/`.unity`/`.asset` 의 직렬화 데이터다. 그리고 **차별점은 둘 중 하나가 아니라
둘의 조인**이다(`m_Script.guid`).

완료: 보안 차단(0-A) → 컨텍스트 위생(1) → 아키텍처 결정(1.5) → 인덱스 3레이어 + 조인(2a/2b)
→ 정확도 감사(2026-08-23~24) → **값 해석(2c, 2026-08-24)**.

**2026-08-24 세션 결론.** §3 최우선 항목이던 YAML 컴포넌트 값 해석기를 만들었다
(`unity_get_asset_components`). 만드는 과정에서 기존 레이어의 결함 4개가 더 드러났고 전부 고쳤다 —
그중 하나는 **실제로 참조되는 에셋 3건에 "참조 0" 을 답하던 것**이다(VFX Graph 가 참조를 YAML
문자열 안의 JSON 으로 쓴다). 어제와 같은 종류의 오답이 형태만 바꿔 남아 있었다.
새 도구를 만들 때마다 기존 레이어의 거짓말이 하나씩 드러난다는 것이 이 단계의 패턴이다.

그리고 **그 뒤 독립 감사(Codex CLI)를 한 번 더 돌렸다.** 결함 9개가 더 나왔고(경로 봉쇄 누락,
확장자 화이트리스트, `.meta` 참조, 자기 참조, 조용한 절단 등) 전부 재현 후 고쳤다.
"참조 0" 오답은 이번 세션에서 총 **40건** 사라졌다.

**2026-08-25 세션 결론 — Unity 를 켜고 대조군을 세웠다.** 레이어 D 의 새 주장 5개를
`AssetDatabase.GetDependencies` 와 대조했고 **예상 5개가 전부 적중했다**(보고서 §11).
라이브 `tools/list` 도 캐시 경로와 바이트 단위로 같다(82개 / 39,669 B).
그런데 대조를 준비하며 우리 도구에 같은 질문을 던지자 **`unity_find_references` 가
`BombAction.cs` 에 "참조 0" 을 답했다** — 인덱스는 그 엣지를 갖고 있었고 다른 두 도구에만
실려 있었다. 레이어 D 를 만들 때 이 도구를 안 고친 것이다. 스윕 결과 **38건**이 같은 형태였고
고쳤다(`1102330`). §4-(21) 참조.

**2026-08-26 세션 결론 — Phase 3 P1 을 만들었고, 로드맵 전제를 버렸다.**
P1 원안은 "그래프에 중심성 랭킹을 얹는다" 였다. 게임 레포 커밋 188건을 정답지로 채점하니
**중심성이 가장 약한 신호**였다(hit@50: 중심성 28.2% / 부착 33.0% / churn 33.5% / 기준선 19.1%).
그래서 `unity_project_map` 을 **"코드를 읽어서는 알 수 없는 배치·진입점 지도"** 로 재정의했다.
비용은 실측 `tools/list` +1,236 B(+334 토큰/세션), 질의 웜 48 ms.
인수 시험을 스크립트로 넣었다 — `Tools/probe-project-map.js`, 기본 예산 6,000 에서 6/7.
설계 문서: `C:/dev/unity-mcp-p1-design.md` — ⚠️ **이 머신에만 있다. 레포에 없다.** 교훈은 §4-(22)(23).

**같은 날 Phase 3 P2 도 끝냈다 — `unity_impact_analysis`.** "이걸 바꾸면 무엇이 깨지나" 를
축별로 답한다(호출자 / 파생 / 부착 에셋 / 참조 에셋 / 인스펙터 배선 / 타입이름 / 경로 로드 /
ProjectSettings·빌드 씬 / 속성). **합계를 만들지 않고, `unknown` 을 응답의 일부로 싣는다.**
P1 교훈대로 **프로브를 먼저 썼고**(`Tools/probe-impact-analysis.js`, 10개) 첫 실행 10/10 이었다.
그런데 프로브가 못 잡은 것을 경계 조건 손검사가 셋 잡았다 — §4-(24).
부수로 결함 3건을 고쳤다: ProjectSettings 가 스캔 밖(프로젝트 에셋 19건 누락, 그중 씬 13),
캐시가 중복 타입 123개를 잃던 것, 없는 대상에 "영향 0" 을 답하던 것.
비용: `tools/list` 40,905 → 42,142 B (84 도구), 질의 웜 0~6 ms. **현재는 85개 / 43,368 B** 다.

**2026-08-27 세션 결론 — P3 를 설계대로 만들지 않았고, 그 판단이 옳았다.**
P3(검증 루프)의 원안은 "컴파일 오류를 인덱스와 조인한다" 였고, 나는 **기존
`unity_get_compilation_status` 의 응답을 브릿지가 가공하는 안(A)** 을 권했다.
사용자가 착수 전에 Codex CLI 와 의논하라고 지시했고, 그 감사가 A 를 뒤집었다.
결정적 근거는 **조인 키가 아예 없다**는 것이다 — 인덱스는 PDB 의
`MethodDebugInformation` 에서 Document 컬럼만 읽으므로 메서드→파일은 있어도
**줄번호→메서드가 없다**. 게다가 컴파일이 실패하면 Unity 가 `ScriptAssemblies` 를
갱신하지 않으므로, 인덱스는 **정확히 필요한 순간에** 직전 성공 빌드를 설명한다.
전부 직접 재현했다. 교훈은 §4-(25)(26).

그래서 P3 를 둘로 쪼갰다. **P3-a 는 새 도구 0개** — `tools/list` 42,142 B 불변이었다.
① 인덱스 신선도 재검증(아래), ② 컴파일 진단 완결성, ③ 콘솔 절단 회계, ④ 오류 출처 분류.
복합 도구(P3-b)는 그때 보류했다(만들더라도 A 가 아니라 별도 도구로 격리). 아래 참조 — 결국 만들었다.

**그 과정에서 P3 와 무관한 실제 버그를 찾았다 — 이번 세션 최대 수확.**
`ensureIndex` 가 `if (_index && !force) return _index;` 로 시작해서, **한 번 올라간
인덱스를 세션 내내 다시 검증하지 않았다.** 지문 대조는 `loadCache()` 안에 있는데
그건 `_index` 가 null 일 때만 돈다. 즉 브릿지가 붙은 채로 재컴파일하면 인덱스 도구
11개 전부가 컴파일 이전 그래프로 답했다(`8f80fce`). §4-(21) 과 같은 형태다.

**그 뒤 사용자 지시로 §0.5 의 백로그를 전부 소진했다(2026-08-27~28).**
P3-b 는 결국 만들었는데 **형태를 바꿨다** — 감사가 최대 위험으로 꼽은 전송 계층 리팩터를
피하려고, Unity 로 왕복하지 않고 **진단을 입력으로 받는 순수 로컬 조인**으로 했다.
그 밖에: PDB SequencePoints 디코딩(줄번호→메서드) · `search_project` 의 `folder` 버그
(그게 곧 타임아웃의 원인이었다) · 중첩 타입 신원(키는 안 바꾸고 `Outer/Inner` 별칭) ·
필드 타입 서명 디코딩 · 오버로드 분리(시그니처 그래프를 **병렬**로) · `effectByOperation`
실검증(세 주장 모두 참, 내 예상 하나는 틀렸다) · Missing Script 전수 분류.

**그리고 두 번째 독립 감사가 내가 하루 전에 넣은 버그 둘을 찾았다.**
`find_callees` 가 시그니처 키에 **거짓 0** 을 답했고, `find_callers` 는 시그니처 키에서
`(params)` 를 이름에 붙인 채로 속성을 조회해 **안전 축이 조용히 사라졌다.** 둘 다
§4-(21) 의 같은 형태다 — 공유 리졸버를 고치고 소비자를 하나만 고쳤다. 그래서 키 파싱을
`Bridge/index/methodkey.js` 하나로 모았다. 교훈은 §4-(27)~(32).

**2026-08-28 세션 결론 — 인수 자료가 먼저 틀렸고, 그 다음 출하 층이 틀렸다.**
넘겨받아 §0.5 의 "30초 안에 돌려볼 것" 을 그대로 돌리니 **프로브 6개가 FAIL** 했다.
회귀가 아니라 **인수 스위트가 캐시 없이는 서지 못하는 것**이었다(§4-(33)).
그 다음 남은 백로그의 `gap` 줄이기를 하다가 더 큰 것을 찾았다 — **메서드 줄 범위가
전수 27.4% 틀렸고, 음수 줄번호가 126개 있었다.** 표본 40/40 이 그것을 가리고 있었다.
원인은 Portable PDB 시퀀스 포인트 디코더가 상태 변수 하나로 두 가지를 표현한 것이고
(숨은 포인트 뒤의 **절대** 줄을 부호있는 델타로 읽었다), 고친 뒤 **전수 0% / 겹침 0 /
음수 0** 이다. 부분 클래스에서 메서드가 어느 파일인지 잃고 있던 것도 같이 고쳤다.
그 층 위에 이미 도구를 두 개 얹은 뒤였다는 것이 이 세션의 요점이다 — 교훈은 §4-(33)~(37).
그 밖에: `lineKind` 로 gap 의 86.0% 를 설명(파일 수준 줄은 이제 메서드에 붙지 않는다) ·
개수 필드 이름 정정(`assetGuidPairCount`) · 패키지 어셈블리 심볼 항목 실측(파견).
**그리고 Unity 를 켜서 `effectByOperation` 의 남은 세 연산을 전부 실검증했다**(E4~E6):
셋 다 주장대로였고, 그 과정에서 (a) 이동·삭제 뒤에는 도구조차 깨진 상수 경로를 말하지
못한다는 것을 보고 `danglingLoads` 를 넣었고(이 프로젝트에 실제 2건 있다),
(b) PDB 문서가 없는 타입의 에셋 축이 **거짓 0** 이던 것을 고쳤다(§4-(39)).

**측정 보고서: `C:/dev/unity-mcp-measurement-report-dev-0.0.1.md`** (토큰·정확도 실측, 근거 커밋 목록). ⚠️ **이 머신에만 있다. 레포에 없다** — 인용된 수치는 §0.5 "확인된 것" 표에 재현 명령과 함께 옮겨져 있다.
dev-0.0.2 절(§8), 독립 감사 절(§9), 레이어 D 절(§10), 라이브 교차검증 절(§11),
**P1 프로젝트 맵 절(§12)**, **P2 영향 분석 절(§13)** 이 뒤에 붙어 있다.
결론 한 줄은 그대로다: 이 작업은 토큰을 줄인 것이 아니라
**정확도를 산 것**이다(세션 고정비 약 +2,500 토큰, 손익분기는 인덱스 질의 1회/세션).

---

## 0.5 ⏭️ 다음 세션은 여기서 시작한다

**2026-08-29 세션(셋째 날) 요약 — 미뤄 둔 판단 하나를 닫고, 권고 하나를 구현했다. Unity 는 계속 꺼져 있었다.**
백로그의 두 항목이 **둘 다 전제가 틀려 있었다.**
⑴ Missing Script A군(오클루전)은 두 세션 동안 "의도된 삭제였나" 를 사람이 정할 일로 남아
있었는데, `git log --all -S` 한 줄이 **삭제된 적이 없다**는 것을 보여줬다 — 세 클래스는
`origin/feature/trensparent` 에만 있고 우리 브랜치의 조상이 아니다. 게임 경로 영향을 네 축
(씬 직접·전이 BFS·Addressables·경로 로드) 전부 0 으로 확인하고 **현상 유지로 종결**했다(§5, §4-(44)).
⑵ ECID 승격은 문서가 "530건에 0 비용" 이라고 적어 뒀는데, 갈래를 쪼개니 모집단이 절반이었다 —
**실효 1,090건**이고 응답 바이트는 0 이 아니었다(§4-(45)). 구현하고 프로브 19개로 고정했다.
독립 검증(Codex)이 여섯 건을 냈고 **여섯 다 지금은 발현되지 않는 잠복 결함**이었지만,
그중 하나가 "발현되는 날 = 진단이 가장 필요한 날" 이라 전부 고쳤다.
그리고 **두 레포를 push 했다**(사용자 확인 후).

한 줄로: **이 세션은 "적혀 있던 전제를 다시 잰" 세션이다.** 수치는 다 맞았고 모집단이 틀렸다.

**2026-08-28 세션(둘째 날) 요약 — 인수 자료의 결함, 출하 층의 버그, 그리고 정리 1건.**
넘겨받아 §0.5 의 명령을 그대로 돌리자 **프로브 6개가 FAIL** 했다(회귀가 아니라 부트스트랩
결함 — §4-(33)). 그 다음 `gap` 줄이기를 하다가 **메서드 줄 범위가 전수 27.4% 틀렸다**는 것을
찾았다 — 음수 줄번호까지 있었고, 표본 40/40 이 그것을 가리고 있었다(§4-(34)).
독립 검증(Codex)을 세 라운드 돌려 그 수정에서 다시 6건을 잡았고(§4-(38)(41)), 파견 감사가
문서 모순 30건을 잡았다(§4-(40)). Unity 를 켜서 `effectByOperation` 의 남은 세 연산을
실검증했고(E4~E6), 그 결과로 `danglingLoads` 를 넣었다. 마지막으로 **정리 1건을 실행했다** —
`DefaultVolumeProfile.asset` 의 고아 컴포넌트 9개(§5, §4-(43)).

한 줄로: **이 세션은 새 기능이 아니라 "이미 답하고 있던 것이 틀렸다" 를 찾아 고친 세션이다.**
그래서 프로브가 8개 스위트 **93항목**(9·11·8·10·8·28·12·7)에 전수 스윕 하나로 늘었다.

**그 전(2026-08-27~28)에 §0.5 의 백로그를 전부 소진했다.** 커밋 46개(`8f80fce`..):
P3-a(진단 정직성 + 인덱스 신선도) · PDB SequencePoints · `search_project` folder 버그 ·
중첩 타입 신원 · P3-b(진단×인덱스 조인) · 필드 타입 서명 디코딩 · 오버로드 분리 ·
P3-b 줄 귀속 정밀화 · `effectByOperation` 실검증 · Missing Script 전수 분류 ·
독립 감사 후속(버그 2건 수정) · 정리 패스.

### 🔌 넘겨받는 시점의 상태 (2026-08-28 밤)

| 항목 | 상태 |
|---|---|
| 리포 | 워킹트리 깨끗, 브랜치 `optimized`. **2026-08-29 에 사용자 확인을 받아 원격(`Seoki2000/unity-mcp`)에 올렸다 — ahead 0 / behind 0.** 정확한 HEAD 는 `git log --oneline` 으로 볼 것 — 여기 해시를 적으면 그 커밋 자신 때문에 항상 낡는다 |
| **Unity** | **켜져 있다** (2026-08-30). §4-(46) 의 창 수정을 라이브로 검증하려고 켰고 그대로 뒀다. 컴파일 깨끗(에러 0 · 콘솔 에러 0). ⚠️ **직렬화 에셋을 고치거나 지우는 작업은 끄고 할 것**(§0.5 함정 표). 끄려면 `File/Exit`. 켜는 방법과 준비 확인은 아래 "Unity 를 켤 때" |
| **게임 레포** | `C:/Unity/MainProject` 브랜치 `feature/Boss23` — **2026-08-29 에 push 완료(ahead 0)**. 올라간 것은 `3429479`(고아 컴포넌트 9개 제거, §5) · `b7c41cf`(오클루전 A군 종결 기록) · `5b54f18`(이 패키지 핀을 `a191eee` -> `2d55d15` 로, 102 커밋) · 셋업 문서·검사 스크립트·아트 리비전 핀(`Docs/tech/environment-setup.md` · `check-environment.py` · `art-svn.json`). **origin 은 팀 레포(`Fujino-Tatsuya/MainProject`)이고 브랜치는 본인 것**이다(최근 커밋 전부 `Seoki2000`). 남아 있는 `M ProjectSettings/EditorBuildSettings.asset` 는 이 작업과 무관하고 **일부러 안 건드렸다** |
| `tools/list` | **85개 / 43,368 B / 도구당 510.2** — 배열 기준(2026-08-28 실측, Unity 켜짐). 응답 줄 43,412 B, 디스크 캐시 43,415 B. ⚠️ **2026-08-29 에 `unity_get_asset_components` 설명이 +288 B 늘었다**(ECID 승격을 설명하는 한 문장, 약 72 토큰/세션) — 그러니 지금은 43,656 B 근처일 것이다. Unity 없이 잰 **로컬 12개 도구** 배열은 10,868 → **11,156 B**. 85개 기준은 Unity 를 켤 때 다시 잴 것 |
| 인덱스 캐시 | **버전 17**(12 에서 다섯 번 올림 — 구 캐시는 틀린 줄 범위·짧은 `endLine`·빠진 두 번째 문서 범위·`danglingLoads` 부재·빠진 경로 로드 엣지를 담는다), **4,473,393 B(약 4.47 MB, `guidCoverage: assets`)** — 전체 커버리지로 빌드하면 약 **8.16 MB** 다. 어느 커버리지인지 안 적으면 두 배 차이가 회귀로 보인다 (`fileIndex` 588 + `extraSpans` 5 + `danglingLoads` 2). 참조 엣지 **6,306** · 호출 엣지 8,673 · 시그니처 엣지 8,848 · `guidToPath` 3,142 |
| 콜드 빌드 | **5.69~6.90 s** (Unity **꺼짐**, 5회) / **6.79~7.49 s** (Unity 켜짐 + 검증 스크립트 동시 실행, 3회). ⚠️ 전 세션 기록 4.67~4.71 s 는 Unity **유휴**에 다른 부하 없이 잰 값이다 — 셋은 조건이 다른 셋이다. 웜 질의 13~22 ms |
| 검사 | 프로브 **9개** 스위트(9 · 11 · 8 · 10 · 8 · 28 · 12 · 7 · **23**) + 전수 스윕 1개, 전부 통과. 명령은 아래 "돌려볼 것" |

### ▶️ 넘겨받고 30초 안에 돌려볼 것

```bash
cd C:/dev/unity-mcp
for p in probe-verify-loop probe-method-lines probe-field-types probe-overloads          probe-nested-types probe-error-impact probe-impact-analysis probe-project-map          probe-ecid-promotion; do
  node Tools/$p.js >/dev/null 2>&1 && echo "$p ok" || echo "$p FAIL"
done
node Tools/sweep-field-checks.js | tail -5     # §6 전수 집계 (약 32 s, Unity 불필요)
```

전부 Unity 없이 돈다. 하나라도 FAIL 이면 **거기서 멈추고** 원인을 먼저 볼 것.

디스크 캐시(`~/.unity-mcp/index-*.json`)가 없으면 **첫 프로브가 한 번 콜드 빌드하고
`note  인덱스 캐시가 없어 콜드 빌드했다 (약 5.7 s)` 를 찍는다** — 정상이다. 나머지는
그 캐시를 쓴다. 2026-08-28 인수 때는 이 부트스트랩이 없어서 6개가 FAIL 했다(§4-(33)).
`probe-project-map` 은 예산별로 표를 찍으므로 **낮은 예산의 FAIL 은 정상**이다
(기본 6,000 에서 6/7, 8,000 이상에서 7/7). 종료 코드만 보면 된다.

### 확인된 것 (다시 재현하지 말 것)

무엇을 어떻게 쟀는지까지 적는다. 조건이 없으면 다음 사람이 다른 것을 재고 회귀라고 부른다.

**비용**

| 항목 | 값 |
|---|---|
| 전체 에셋 지문 | **678~702 ms** (5,825 파일) — 매 호출에 못 붙인다 |
| 어셈블리 서명 | **11~18 ms** (378 파일) — 붙일 수 있다. 그래서 신선도가 2단 검사다 |
| 콜드 빌드 | **5.69~6.90 s** (Unity **꺼짐**, 2026-08-28 실측 5회 — 마지막 회차가 6.90 s). Unity **유휴** 상태에서는 4.67~4.71 s 였다(2026-08-27). ⚠️ 더 예전 기록 7.15~7.45 s 는 Unity 재컴파일 중에 잰 값이라 **무효**. 세 값은 **조건이 다른 세 측정**이다 — 조건 없이 비교하면 회귀로 보인다 |
| 필드 서명 디코딩 | 순수 68 ms. 캐시 +247,009 B |
| 시그니처 그래프 | 캐시 +769,032 B (+20.8%) |
| `tools/list` 순증 | P3-b 하나뿐: **+1,226 B** (정규화 후 실측. 정의 원본 1,250 B ≈ 331 ±99 토큰/세션) |

**도구가 무엇을 아는가**

| 항목 | 값 |
|---|---|
| 줄번호→메서드 | **있다**(`d0714ad`), 그리고 **2026-08-28 에 디코딩을 고쳤다**(§4-(34)). 단 `line` 은 **본문 시작**이지 시그니처 줄이 아니다 — 포함 검사로 쓰면 시그니처 오류가 범위 밖이다. 그래서 P3-b 가 `signature`/`gap` 으로 승급한다 |
| 줄 범위의 정확도 | **전수 대조 5,313/5,313 (0% 불일치)** · 겹치는 범위 **0 쌍** · 음수·역전 **0** · 두 파일 뷰에 동시 등장 **0**(5,958개 중). 고치기 전에는 27.4% / 3,036 쌍 / 126개 / 37건이었다. 재는 명령: `node Tools/probe-method-lines.js`(프로브 8·9·10, 61 ms) + `probe-error-impact.js`(프로브 20) |
| `endLine` 의 뜻 | **마지막 시퀀스 포인트의 끝 줄**이다(2026-08-28 정정). 그전에는 마지막 포인트의 **시작** 줄이라 여러 줄 문장의 꼬리가 범위 밖이었다 — 메서드 133개, 차이 중앙 2줄 / 최대 22줄. 독립 검증(.NET 참조 구현)이 짚었다: **디코딩이 참조와 같다는 것이 필드의 뜻이 맞다는 뜻은 아니다** |
| 분류의 모집단 | `lineKind` 는 **exact 가 아닌 줄 + 생성자 범위**를 분류한다. 일반 메서드 본문은 `in-method-body` 다. 전부 분류하면 본문의 `damage += 1;` 이 `field-declaration` 으로 나가고(정밀도 18.8%), 반대로 전부 빼면 **필드 초기화자 2,417줄이 가려진다** — 시퀀스 포인트가 `.ctor`/`.cctor` 에도 붙기 때문이다. 둘 다 실측으로 확인했다 |
| 필드 판정의 근거 | 이름 일치만으로는 안 된다. 선언 자리 요구 **다섯**: 앞에 `(` 없음 · 앞이 `.` 아님 · **앞에 `=` 없음**(대입 우변 배제) · 앞에 식별자 하나 이상(타입 토큰) · 뒤가 `;`/`=`/`,`/줄끝. 그리고 **속성이 같은 줄에 있으면 떼고 같은 규칙을 적용**하고 `attributesOnSameLine: true` 를 붙인다 |
| 필드 분류의 정확도 | 독립 오라클(Roslyn 구문 트리, 85,122줄) 기준 **정밀도 99.18% / 재현율 97.01%**(2026-08-28 최종). 진짜 필드 선언 4,013줄 중 거짓 양성 **32** · 가려진 것 **120**(`unknown` 117 + 타입 선언 3) · member 이름 오류 **26**. 재는 방법: `Tools/verify/measure.js` + `Tools/verify/roslyn-oracle~/`(레포 안. 2026-08-31 에 옮겼다 — 그전에는 어느 머신의 절대경로였다). ⚠️ **정밀도만 보면 안 된다** — 한때 정밀도 96.2% 인데 재현율은 70.5% 였다(§4-(41)) |
| 여러 문서에 걸친 메서드 | 실측 **5개**(부분 클래스의 `.ctor`/`.cctor`). 두 번째 범위를 `extraSpans` 로 싣는다 — 없으면 그 파일의 그 줄이 **다음 메서드로** 귀속된다 |
| 오버로드와 오류 응답 | 줄 범위는 오버로드를 구분하지만 `unity_explain_compile_errors` 의 `callerCount`·`callers` 는 **이름 키로 합쳐진다**(실측 136 이름 그룹 / 344 정의 / 한 이름 최대 8개). 독립 검증이 확인한 알려진 한계다 — 시그니처 그래프는 §0.5 "남은 것" 4번 |
| 메서드 줄 정보의 모집단 | 줄 있음 **5,960** / 없음 **1,305**(추상·인터페이스·이벤트 add/remove·Netcode RPC 핸들러 등 **컴파일러·ILPP 생성**). Assembly-CSharp 만 보면 3,005 / 689. 없는 것은 null 이고 0 으로 채우지 않는다 |
| 부분 클래스 | 메서드마다 `fileIndex` 를 싣는다(**파일이 둘 이상인 타입에만**, **588개** — 2026-08-28 캐시 실측. 583 은 생성자 문서 수정 전 값이다). 없으면 `AudioSourceProxy.cs` 와 `.Methods.cs` 가 서로의 범위를 갖는다 |
| 본문시작−선언줄 거리 | 중앙 **2** / 최대 5 (표본 276) → 위로 8줄 스캔이면 덮인다. ⚠️ 단 **로컬 함수를 품은 메서드**는 자기 첫 문장이 로컬 함수 뒤에 온다(선언 98 / 본문 108 실례). 여러 줄 시그니처·긴 주석도 8줄을 넘는다 — 전수 대조는 40줄까지 훑는다(§4-(36)) |
| 오류 줄이 무엇인가 | `lineKind` 로 답한다. 매핑된 559 파일 **45,816 코드 줄**: exact 34,124(74.5%) / signature 3,746(8.2%) / **gap 7,706(16.8%)** / 귀속 없음 240. gap 7,706 중 kind 가 붙는 것 **6,630 = 86.0%**: file-level 2,596 · field-declaration 1,827 · member-attribute 1,090 · type-declaration 758 · type-attribute 359 · **unknown 1,074(13.9%)**. `file-level` 2,596 은 메서드 귀속을 아예 하지 않고, `field-declaration` 1,827 은 실제 필드로 조인된다. 재는 스크립트는 프로브 18·21 이 같은 계산을 한다 |
| 남은 unknown 1,074 의 정체 | (분류 도입 직후 2,164 였고 `endLine` 정정으로 줄었다) 표본 분류: 접근수정자로 시작 · 여러줄 시그니처 · 세미콜론 종료 · 제어·접근자 키워드 순. 대부분 **여러 줄 필드 초기화자의 연속 줄**과 **여러 줄 시그니처**다 — 중괄호 깊이를 추적해야 덮인다 |
| PDB Document 경로 | **절대와 상대가 섞여 있다.** PDB 189개 중 첫 문서가 절대 **15** / 상대 **174**(`.\Library\PackageCache\...`). `Library/PackageCache/...` 를 가리키는 document **10,849개**. `toProjectRelative` 는 절대를 전제하므로 상대는 전부 버려진다 — 패키지 심볼의 조인 키가 없어 보이던 이유다(§4-(37)) |
| 필드 서명 | **전** 어셈블리(189개) 105,972 필드 실패 **0건**. **사용자** 어셈블리는 5,920(중복 포함)/5,554(제거) — **모집단이 다르다.** 디스크 대조는 표본 150/150 이고, 소스가 매핑 안 된 타입의 필드 1,052개는 **대조 자체가 불가능** |
| 오버로드 실제 규모 | 선언 2개 이상 키 **146**, 그런 키를 향하는 엣지 **366/8,673 = 4.2%**, 영향 키 101(상위는 벤더). **키를 안 바꾼 근거다** |
| 시그니처 그래프 | 엣지 8,848 / 타깃 4,443 / **미해석 0**. 이름 엣지(8,673)보다 많은 것이 정상 |
| 중복 타입 123 | **레코드 수**다. 고유 이름은 **29개**, 그중 19개가 컴파일러 생성. 자기 코드 충돌은 `PassData`(3)·`Segment`(2) 둘 |
| Missing Script | 14 GUID / 164 참조 = 자기 코드 **3개/132** + 서드파티·테스트 **11개/32**. §5 전수 분류 |

**Unity 가 어떻게 행동하는가 (라이브 실측)**

| 항목 | 값 |
|---|---|
| 컴파일 실패 시 어셈블리 | **갱신되지 않는다.** 실제 실패를 만들어 확인 — 서명이 바이트 단위 동일(hash 2042387871). 그래서 그동안 인덱스는 **직전 성공 빌드**를 설명한다 |
| 무변경 재컴파일 | Editor.log `2182 evaluated, **1 updated**` — 사용자 어셈블리 0개 컴파일. `observedAssemblyCount: 0` 이 그것이다 |
| `diagnosticsState` 4상태 | `unobserved` / `collecting` / `complete`+observed 0 / `complete`+observed N **전부 라이브 관측** |
| `SessionState` | 도메인 리로드를 넘는다 (generation 1→2→3 유지) |
| 이 프로젝트의 CS 경고 | ~~0건~~ → **3건이다**(2026-08-28 정정). 예전 기록은 **컴파일이 일어나지 않은 상태**의 Editor.log 를 본 것이다. 실제로 재컴파일하면 `warningCount: 3` 이고 `BossTeleportManager.cs(287,16) CS0618` · `ZoneBridgeGateManager.cs(261,16) CS0618` · `BossBomb.cs(152,10) CS0114` 다. **콜드 빌드 수치와 같은 종류의 착오** — 조건을 안 적으면 다른 것을 잰다 |
| 에셋을 옮기면 | GUID 는 `.meta` 와 함께 유지되고 **GUID 기반 축은 전부 살아남는다.** 컴파일러·콘솔 **침묵**. 상수 경로만 깨지는데, 흔적은 `pathLoadUnresolved` +1 뿐이었다 — 그래서 `danglingLoads`(파일+경로)를 남기게 했다. E4 |
| 에셋(스크립트)을 지우면 | 컴파일러는 **아직 그 타입을 부르는 코드가 있을 때만** 잡는다(CS0246). 코드가 통과하면 `get_prefab_info` 가 그 컴포넌트를 **조용히 목록에서 뺀다**(Missing Script 표시도, 콘솔도 없다). 남는 신호는 직렬화 데이터의 고아 GUID 이고 `find_missing_scripts` 가 잡는다. E5 |
| 파일에 MonoBehaviour 가 여럿이면 | 파일명과 같은 클래스가 없을 때 Unity 는 **파일에서 처음 선언된 MonoBehaviour** 에 묶는다. **순서만 바꿔도** 컴포넌트가 다른 클래스로 조용히 재바인딩되고 직렬화된 필드 값은 따라가지 않는다. E6·E6b |
| 경로 기반 로드 | 엣지 **24** · 해석 **26** · 깨진 것 **2**(파일과 경로를 안다) · 동적 **43**. ⚠️ `#if` 로 비활성인 블록의 호출 1건이 동적에 섞여 있다(전처리기를 평가하지 않는다) — 활성 기준은 42다. 그리고 상수가 상수로 만들어지는 경로도 이제 접는다 |
| 깨진 로드 경로 | 실제로 **2건 있다** — `Assets/1.Scripts/Effects/Editor/EffectSystemSetup.cs` 가 `Assets/5.VFX/Common/FX_Hit_Spark.prefab` 과 `…/FX_Hit_Blunt Variant.prefab` 을 부르는데 그 폴더가 없다. `unity_index_status` 의 `danglingLoads` 로 나온다 |
| 컴파일러가 내는 경로 | **역슬래시 + 프로젝트 상대**(`Assets\_X\Y.cs`). 절대 경로도 온다 |
| `effectByOperation` | **실검증됨.** 세 주장 모두 참 — CS1061 이 호출자를 짚는다 / 배선은 컴파일러·콘솔·Editor.log 전부 침묵 / **클래스만 바꿔도 부착은 살아남는다**(내 예상이 틀렸다). 하네스: `Tools/verify-effect-by-operation.md` |
| `GetAllAssetPaths()` 의 24,233 | Assets **3,143** + Packages **21,075**. 패키지 에셋은 `Packages/<id>/...` 로 나오지 `Library/PackageCache/...` 가 아니다 |
| 두 참조 검색 경로 | 같은 대상: `search_project(reference)` 3,143 스캔 **1,344 ms** vs `find_references` **14 ms**(96배). 답은 둘 다 2건. **경계에서 갈리므로 둘 다 남긴다** |

**⚠️ 함정 (여기서 시간을 잃었다)**

| 함정 | 내용 |
|---|---|
| **정션** | `Assets/50.Art` 는 SVN 으로 가는 **Junction** 이고 `Dirent.isDirectory()` 가 **false** 를 낸다. 안 따라가면 `.meta` 1,105개(전체 3,142 중 35%)가 통째로 빠진다. **`find -type f` 도 안 따라간다** — 대조군으로 쓸 때 주의 |
| **키 안의 괄호** | 명시적 인터페이스 구현의 **메서드 이름에 괄호가 들어간다**. 키는 반드시 **마지막 `(`** 로 쪼갤 것. 파싱은 `Bridge/index/methodkey.js` 하나만 쓸 것 |
| **"전체 커버리지"** | GUID **해석** 범위를 뜻한다(`includePackageCache: true`, GUID 27,321). 문서 **스캔** 범위는 Assets 다 — PackageCache 에셋까지 훑으면 컴포넌트가 28,319 로 뛴다 |
| **지표 정의** | "낡은 키 가진 컴포넌트 19" 는 **확신** 케이스다. 확신/불확실을 뭉개면 145 가 나오고 회귀처럼 보인다(126 은 베이스가 패키지로 나가 도구가 이미 inconclusive 라고 말한다) |
| **바이트 기준** | `tools/list` 43,368 은 **배열**이다. 응답 줄 43,412, 디스크 캐시 43,415. 기준을 안 적으면 다른 것을 재고 회귀라고 부른다 |
| **표본 통과** | 표본이 통과했다는 것은 **전수의 증거가 아니다.** 줄 범위는 "표본 40/40" 으로 넘어갔는데 전수로는 27.4% 가 틀렸다. 잴 수 있으면 전수로 잰다(그 대조가 61 ms 다) — §4-(34) |
| **인수 명령의 전제** | 프로브는 디스크 캐시가 있어야 돌던 것이 6개였다(지금은 스스로 빌드한다). 문서에 명령을 적을 때 **그 명령이 전제하는 상태**를 같이 적을 것 — §4-(33) |
| **백로그 문구** | 그대로 구현하면 안 되는 것이 두 건 나왔다: `affectedAssetCount` 는 **이미 다른 뜻으로 존재**했고(§4-(35)), "어셈블리→타입이름 맵" 은 조인 키가 아니라 **null 을 하나도 못 메운다**(§4-(37)) |
| **에셋을 고칠 때 Unity** | 파괴적 편집(직렬화 에셋 수정·삭제)은 **에디터를 끄고** 한다. Unity 가 그 서브에셋을 메모리에 들고 있어서, 디스크를 고친 뒤 Unity 가 먼저 쓰면 편집이 사라진다. 2026-08-28 정리에서 그 순서를 지켰고, 재기동 후 파일 해시가 불변임을 확인했다 |
| **도구가 못 보는 실패 형태** | `find_missing_scripts` 는 **GUID 조인**이라 `m_Script: {fileID: 0}`(GUID 자체가 없는 것)을 원리적으로 못 본다. 그 형태는 `scriptlessComponentCount` 로 따로 센다. **정리 같은 파괴적 작업 전에는 원본을 직접 훑어 도구의 답과 대조할 것** — 실제로 4개라고 답한 파일에 9개 있었다(§4-(43)) |
| **기준선 감소가 개선일 때** | 하루에 세 번 났다(참조 엣지 −4, `missingScript` −4, `scriptComponents` −3 — 전부 정리 때문). 프로브·스윕이 빨간불로 잡는 것이 정상 동작이다. 갱신할 때 **왜 줄었는지와 "여기서 더 줄면 도구 문제"** 를 그 자리에 적을 것 |
| **URP 기본 프로필 Reset** | Graphics Settings → URP Default Volume Profile → **Reset 을 누르면** URP 17.3.0 템플릿의 고아 컴포넌트 9개가 **다시 들어온다**(템플릿 자체가 오염돼 있고 `AssetDatabase.CopyAsset` 으로 파일째 복사된다). §5 참조 |
| **한글 `\uXXXX` 이스케이프** | 손으로 이스케이프하면 조용히 뜻이 바뀐다 — "지웠다"→"지우메", "참조"→"추적" 이 실제로 문서에 들어갔다. 텍스트는 파일로 넘기고(`cat > f <<'EOF'`) python 이 `encoding='utf-8'` 로 읽어 치환할 것. 콘솔이 깨져 보이는 것과 파일이 깨진 것은 다르니 확인은 **파일을 다시 읽어서** 한다 |
| **참조 수의 단위** | 도구가 말하는 "참조 N"(`totalReferenceCount`)은 **(에셋, GUID) 쌍**이다. 컴포넌트 인스턴스 수가 아니다. 오클루전 A군은 쌍 132 인데 실제 컴포넌트는 **560** 이었다 — 정리 규모를 쌍으로 잡으면 4배 이상 어긋난다. 원본 YAML 직접 집계는 `Tools/scan/occlusion-reach.py` (§4-(44)) |
| **Missing Script 의 두 가지 뜻** | "코드가 사라진 흔적" 일 수도 있고 **"코드가 아직 안 온 흔적"**(미머지 브랜치) 일 수도 있다. 처방이 정반대다. 판단 전에 `git log --all -S "<클래스명>"` 과 `git merge-base --is-ancestor <커밋> HEAD` 를 먼저 돌릴 것 — 오클루전 3개는 두 세션 동안 "의도된 삭제" 로 오해돼 있었다(§4-(44)) |
| **`Packages/` 는 에디터 안에서만 뚫린다** | 에디터 프로세스 안에서는 `System.IO` 가 `Packages/<패키지>/...` 를 실제 위치로 리다이렉트한다 — `File.Exists` 가 **true** 를 내고 `Path.GetFullPath` 가 `C:/dev/unity-mcp/...` 를 준다. **파일시스템 링크가 아니라 인프로세스 가상화라 셸에서는 원리적으로 안 보인다**(Unity 가 켜져 있어도 `ls Packages/` 는 manifest·lock 둘뿐이다). 에디터 코드의 경로 판정을 셸에서 재고 결론 내지 말 것 — 2026-08-30 에 그렇게 틀렸다(§4-(46)) |
| 절대경로 입력 | 인덱스 도구는 조용한 0 이 아니라 **명시적 에러**를 낸다(`Could not resolve ... to a GUID`) |
| §4-(21) 회귀 | 없음. `BombAction` → `totalCount 0` + `referencedByTypeName [Wells.asset]` + "NOT unused" note |

### 🔬 계측기 — 새 기능을 만들 때 **먼저 여기에 질문을 추가**한다

| 파일 | 무엇을 검사하나 |
|---|---|
| `Tools/probe-verify-loop.js` | 인덱스 신선도·콘솔 절단 (8 — 8번이 깨진 로드 경로를 목록으로 내는지) |
| `Tools/probe-method-lines.js` | 메서드 위치·오버로드 가시성 (11 — 8~11 이 **전수 불변식**: 음수 없음 / 겹침 없음 / 전수 디스크 대조 / 디코딩 버그 재현) |
| `Tools/probe-field-types.js` | 필드 타입 서명 (8) |
| `Tools/probe-nested-types.js` | 중첩 타입 신원 (8) |
| `Tools/probe-overloads.js` | 오버로드 분리 (10) |
| `Tools/probe-impact-analysis.js` | 영향 분석 (11 — 11번이 PDB 문서 없는 타입의 파일명 폴백) |
| `Tools/probe-error-impact.js` | P3-b 진단×인덱스 조인 (24 — 15~19 가 `lineKind`, **20~21 이 전수 불변식**(파일 뷰 누출 금지 / 본문 줄에 분류 금지), 22~24 가 독립 검증이 짚은 재현: 생성자 범위의 필드 초기화자 / 시그니처 줄 오판 / 두 번째 문서 범위) |
| `Tools/probe-project-map.js` | 지도 (7, **예산에 따라 답이 달라진다** — 2k 1/7 · 4k 5/7 · 6k 6/7 · 8k~10k 7/7) |
| `Tools/probe-ecid-promotion.js` | ECID 승격 (23 — 1~6 이 승격해야 하는 자리, **7~11 이 승격하면 안 되는 자리**, 12 가 필터, **13~14 가 전수 불변식**(승격 수 = 원본 YAML 직접 집계 / 해석된 행에는 0건), 5b 가 설명문을 행마다 싣지 않는지) |
| `Tools/verify/` | **검증 오라클.** Roslyn 정답지 · 출하 인덱스와의 대조 · 오프라인 컴파일 rsp 생성기. 2026-08-31 에 레포 밖 절대경로에서 옮겼다 — README 참조 |
| `Tools/scan/occlusion-reach.py` | A군 오클루전의 규모와 게임 경로 영향 4축 (Unity 불필요, §5) |
| `Tools/sweep-field-checks.js` | §6 전수 집계 재측정(측정 시임 `queries._checkFields`) |
| `Tools/verify-effect-by-operation.md` | `effectByOperation` 실검증 절차와 관측 기록 |
| `Tools/prototype/*.js` | **독립 오라클.** 출하 인덱스와 다른 경로로 같은 질문에 답한다 |
| `Editor/Debug/DependencyCrossCheck.cs` | 메뉴 `Window/MCP Dependency Cross-Check` |

**프로브를 쓸 때의 규칙**(§4-(22)(24)(27)(31)에서 나왔다):
1. 구현보다 **먼저** 쓴다
2. 단언이 **아무것도 없을 때도 참이 되는지** 물어본다(이번 세션에 그 형태를 다섯 번 만들었다)
3. 최소 하나는 **다른 경로로 구한 답과 맞춰본다** — 실제로 버그를 잡은 것은 전부 그 형태였다
4. 이름이 단언보다 많이 주장하지 않게 한다

### 남은 것 — 전부 새 판단이 필요하다

**한눈에 (2026-08-31 기준).** 아래 표가 지금 열려 있는 전부다. 각 항목의 근거와 수치는
그 아래 번호 항목에 있다. **"막는 것" 은 하나도 없다** — 전부 "할지 말지" 의 판단이다.

| # | 무엇 | 상태 | 판단이 필요한 지점 |
|---|---|---|---|
| 2-B | ~~Missing Script B군~~ | **2026-08-31 종결** | 문서 28개 제거(§5). missing 10 -> 4. git 쪽은 커밋했고 **SVN 쪽 16파일은 작업사본에만 있다 — `svn commit` 은 아직** |
| 2-B' | `Character Fire.vfx` 의 고아 1건 | 미결 | VFX 그래프 **노드 목록이 그 fileID 를 참조한다.** 문서만 지우면 dangling 이라 건너뛰었다. 고치려면 그래프 모델을 이해하고 노드 참조까지 같이 빼야 한다 |
| 3 | ~~`effectByOperation` 남은 두 연산~~ | **2026-08-31 종결** | Unity 라이브로 E7(Resources 키 이동)·E8(씬 배선 delete/move) 둘 다 돌렸다. **이 축은 이제 전부 실검증됐다.** 관측은 `Tools/verify-effect-by-operation.md` |
| 4 | ~~오버로드 키 교체~~ | **2026-08-31 닫음: 하지 않는다** | 근거가 세 번 재도 반대쪽이다 — 영향 엣지 **4.2%**(366/8,673), 상위가 벤더 코드, UnityEvent·속성은 이름만 갖는다. 병렬 시그니처 그래프가 이미 그 질문에 답한다(`find_callers` 가 특정 오버로드를 받는다). **되살릴 조건**: 이름 키가 실제로 오답을 낸 사례가 나올 때 |
| 5 | ~~얕은 `GUID → 타입 이름` 맵~~ | **2026-08-31 닫음: 하지 않는다** | ECID 승격이 1,090 을 답한 뒤 남는 순증이 **292 + `.asset` 117** 뿐인데 대가는 캐시 **+832 KB(+18.6%)** · 콜드빌드 **+3.9 s**. 그 292 는 애초에 ECID 조차 없는 것들이라 맵으로도 이름만 얻고 검증은 여전히 안 된다. **되살릴 조건**: 캐시·빌드 예산이 늘거나, 292 건이 실제 질문에서 걸릴 때 |
| 5b | 컴포넌트 값 파싱 메모 | 미결 | 질의마다 다시 파싱한다. 일반 프리팹 3~4 ms 라 급하지 않다 |
| 6 | ~~`lineKind` 의 `unknown`~~ | **2026-08-31 처리** | `member-declaration` 추가로 1,187 -> **691**(−41.8%). 백로그의 "중괄호 추적" 전제는 틀렸고(24.3%), 실제 덩어리는 멤버 선언 줄 56% 였다. 남은 691 은 소스 파싱이 필요해 새 판단 |
| 7 | 분류·해석의 남은 한계 셋 | 기록됨 | 가려진 필드 120줄 · `#if` 비활성 블록 · 파일명 폴백의 네임스페이스 미검증. 전부 범위가 실측돼 있다 |
| — | git URL 환경에서 `resolvedPath` | 미검증 | 이 머신은 로컬 `file:` 참조라 확인 불가. 핀으로 받는 환경이 생기면 `Window/MCP Server` 를 열어 볼 것(§4-(46)) |
| — | 영향 분석의 도구 경계 | **사용자 보류** | 지시 전까지 도구를 지우거나 합치지 말 것 |
| — | `measure.js` / `counterexamples.js` 재실행 | 미실행 | 레포 안으로 옮기고 경로만 걷어냈다. 필드 분류를 다음에 건드리면 그때 전체를 한 번 돌릴 것(`Tools/verify/README.md`) |

**2026-08-29~31 에 닫힌 것**: Missing Script A군(현상 유지 종결) · ECID 승격 ·
`find_missing_scripts` 가 이름으로 답하게 · 첫 셋업 경로(`Window/MCP Server`) · push ·
패키지 핀 · 새 환경 셋업 문서와 검사 스크립트 · 아트 리비전 핀 · 검증 오라클 레포 반입.

1. **여기서 닫는다.** 도구들은 모르는 것을 "모른다" 고 답한다
2. **Missing Script — A군 종결(2026-08-29), B군만 남았다.**
   2026-08-28 에 `DefaultVolumeProfile.asset` 의 고아 컴포넌트 **9개**를 지웠다
   (죽은 GUID 4 + `m_Script: {fileID: 0}` 5). 절차·검증·되돌리기와 **URP 17.3.0 템플릿
   오염이라는 출처**는 §5. missing 14 -> 10 / 쌍 164 -> 160 / scriptless 5 -> 0.
   - **A. 자기 코드 3개 / 쌍 132 (실제 컴포넌트 560) — 종결: 현상 유지.**
     "의도된 삭제였나" 라는 질문이 틀렸다. 세 클래스는 **지워진 적이 없고**
     `origin/feature/trensparent` 에만 있다(우리 브랜치의 조상이 아니다). 게임 경로 영향은
     네 축 전부 0 으로 확인했다. 근거·수치·재현 스크립트(`Tools/scan/occlusion-reach.py`)는
     §5, 어떻게 틀렸었는지는 §4-(44). **다시 판단하지 말 것** — 바뀔 조건은 하나,
     `feature/trensparent` 를 머지하기로 할 때다(그때는 충돌 12건 · 역행 위험을 먼저 볼 것)
   - **B. 서드파티·테스트 잔여물 7개 / 28 참조 — 미결.** 재임포트로 되돌아올 수 있는
     자리다. 분류는 §5 에 있다. 손대기 전에 §4-(43) 대로 **원본 YAML 을 직접 훑어
     도구의 답과 대조**하고 Unity 를 끄고 할 것
3. ~~**`effectByOperation`**~~ — **완전히 끝났다(2026-08-31).** 2026-08-28 에 E4~E6
   (`moveOrReimport`·`delete`·다중 MonoBehaviour)을, 2026-08-31 에 남은 둘을 돌렸다:
   **E7 Resources 키 축의 이동** — 폴더째 옮기면 **살아남고**(키가 안 바뀐다),
   `Resources/` 안의 구조를 바꾸면 **조용히 깨진다**(`danglingLoads` 가
   `kind: "resources-key"` 로 잡는다). 같은 "이동" 인데 `asset-path` 축과 결과가 정반대다.
   **E8 씬 안의 배선** — 이동은 살아남고(GUID 유지), 삭제는 조용히 깨진다.
   씬은 프리팹과 **형태가 같다.** 단 `inspectorWirings` 는 타입이 사라져도 **1 로 남는다** —
   배선 레코드가 이름으로 적혀 있으니 데이터를 정확히 말하는 것이다(클래스를 되살리면 산다).
   관측 전문은 `Tools/verify-effect-by-operation.md`.
4. **오버로드 키 교체** — 지금은 시그니처 그래프가 **병렬**이다. 이름 키를 실제로 바꾸려면
   근거 수치가 위 표에 있다(엣지 4.2%, 벤더 비중, 그리고 UnityEvent·속성이 이름만 갖는다는 제약)
5. **§3 의 나머지 — ~~ECID 승격~~ 은 2026-08-29 에 했다. 두 건이 남았다**
   (`totalReferenceCount` 이름은 2026-08-28 에 처리했다):
   - ~~**ECID 승격**~~ — **완료.** 타입 미해석일 때 에셋이 적어 둔 이름
     (`m_EditorClassIdentifier`)을 `displayName` 과 `script.typeNameFromAsset`
     (`verified: false` + 사유 코드)로 올린다. `script.type` 은 건드리지 않고
     `typeByFullName`/`typesByShortName` 에도 합치지 않았다. 검사는
     `Tools/probe-ecid-promotion.js`(23항목, 전수 불변식 2개 + 독립 검증 재현 4개 + find_missing_scripts 4개).
     ⚠️ **실효는 문서가 적던 530 이 아니라 1,090 이었다** — 822 라는 모집단이
     "패키지라 미해석" 만 센 것이었고, **스크립트가 아예 없는 560**(= §5 A군 오클루전
     컴포넌트 수와 정확히 일치)이 빠져 있었다. 자세한 것은 §4-(45).
     대가(실측): 도구 정의 **+288 B / 약 72 토큰/세션**, 승격이 있는 응답의 바이트
     **+10.7%**(사유 설명을 행마다 싣던 첫 구현은 15.5% 였다), 캐시·콜드빌드 **0**
   - **컴포넌트 값 파싱 메모** — 아직 질의마다 다시 파싱한다. 일반 프리팹 3~4 ms 라 급하지 않다
   - **패키지 어셈블리 심볼(얕은 `GUID → 타입 이름` 맵)** — **아직 미결. 그런데 남은
     이득이 줄었다.** 필요한 것은 "어셈블리→타입이름" 이 아니라 `GUID → 타입 이름` 맵 +
     상대 PDB 경로 처리다(§4-(37)). 실측: 얕은 맵이 메우는 것 5,365 인데 그 **83%(4,425)가
     VFX 그래프 내부 객체**다. 사람이 묻는 표면에서 **ECID 승격이 이미 1,090 을 답한다.**
     맵이 추가로 답하는 것은 **292건**(붙음·패키지·ECID 없음) + `.asset` 117건뿐이고,
     대가는 캐시 **+832 KB(+18.6%)** 와 콜드 빌드 **+3.9 s**(표적 `.meta` 방식.
     전체 커버리지는 +12.1 s) / 파싱 +3~5 ms 다. **292 건에 그 값을 치를지가 판단할 것.**
     하기로 하면 가장 얕은 형태만 넣고, 어떤 경우에도
     `typeByFullName`/`typesByShortName` 에 **합치지 말 것** — 짧은 이름 유일성 98개가
     깨지고 fullName 충돌이 3,560건이다(§4 의 URP `Volume.cs` 오답이 그 실패 모드)
6. ~~**P3-b 의 `gap` 줄이기**~~ — **2026-08-31 에 했다. unknown 1,187 -> 691 (−41.8%).**
   백로그가 오래 적어 온 "정체는 대부분 여러 줄 필드 초기화자와 여러 줄 시그니처이고,
   **중괄호 깊이를 추적하면 덮인다**" 는 **틀렸다.** 전수로 쪼개니 괄호 추적으로 덮이는 것은
   **24.3%** 뿐이고, 가장 큰 덩어리는 **멤버(메서드·속성·생성자) 선언 줄 670(56%)** 이었다.

   ⚠️ 그리고 **그 56% 의 이유에 대한 내 첫 설명도 틀렸다.** "시퀀스 포인트가 없어서
   `signature` 로 안 잡힌 것" 이라고 적었는데, 확인 없이 쓴 추측이었다. 실제로는:

   | 그 줄이 귀속된 곳 | 수 | 비중 |
   |---|---|---|
   | **일반 메서드 (올바른 멤버)** | 516 | 77.0% |
   | 접근자(`get_`/`set_`) | 102 | 15.2% |
   | 귀속 없음 | 49 | 7.3% |
   | 컴파일러 생성 메서드(`<X>b__0`) | 3 | 0.4% |

   즉 도구는 **"어느 멤버인지" 는 이미 알고 있으면서 "이 줄이 무엇인지" 만 `unknown`** 이라고
   답하고 있었다. 없던 정보를 만들어야 하는 문제가 아니라, 있는 답을 안 싣던 문제였다.

   **한 것**: `lineKind` 에 `member-declaration` 을 추가했다. 필드 판정과 **같은 규율**이다 —
   인덱스에 그 멤버가 실재해야 하고 선언 자리여야 한다(앞에 `(`·`=`·`.`·`new` 없음,
   앞에 식별자 하나 이상, 메서드는 뒤가 `(`, 속성은 뒤가 `{` 또는 `=>`). 생성자는 이름이
   `.ctor` 이라 **타입 이름**으로 찾는다. 못 맞히면 `unknown` 이다 — 지어내지 않는다.

   결과: `member-declaration` **496**줄, `unknown` 1,187 -> **691**(gap 대비 13.9% -> 8.6%).
   캐시 0 바이트 · 콜드빌드 0 ms · 도구 정의 0 바이트(설명이 kind 를 열거하지 않는다).
   검사는 `probe-error-impact.js` 의 4항목(속성 · 생성자 · **호출 줄 903개에 오판 0** ·
   **exact 줄 3,448개에 누출 0**). 재는 명령은 `Tools/verify/measure-gap-unknown.js`.

   **남은 691 은 새 판단이 필요하다.** 상위는 제네릭 타입의 생성자(`WeightedAttack<T>` —
   인덱스의 이름은 `` WeightedAttack`1 ``), `typesBySourceFile` 에 안 들어오는 타입의 필드,
   여러 줄 문자열 리터럴이다. 여기서 더 줄이려면 **소스를 실제로 파싱**해야 하고,
   그 비용과 실패 양상은 아직 안 쟀다.
7. **분류·해석의 남은 한계 셋** (전부 실측으로 범위가 잡혀 있다):
   - `lineKind` 의 가려진 필드 **120줄**(`unknown` 117) — 대부분 여러 줄 필드 초기화자의
     연속 줄이다. 중괄호 깊이를 추적하면 덮이지만 그때는 "소스를 파싱한다" 는 뜻이다
   - **`#if` 로 비활성인 블록**을 평가하지 않는다. 로드 호출 1건이 동적에 섞이고,
     분류는 비활성 선언도 그 줄이 말하는 대로 읽는다(거짓 양성 32 중 3건)
   - **파일명 폴백에 네임스페이스 검증이 없다.** 이 프로젝트에는 동명 충돌이 0건이라
     109/109 정확하지만, 다른 네임스페이스의 동명 타입이 유일한 `<short>.cs` 를 가지면
     오선택한다(독립 검증이 합성 반례로 증명). 고치려면 소스에서 네임스페이스를 확인해야 한다
8. ~~**`push` 가 남아 있다**~~ — **완료(2026-08-29).** 두 레포 다 올렸다.
   MCP 포크 `optimized` 와 게임 레포 `feature/Boss23` 모두 ahead 0 / behind 0.
   다음 세션도 push 전에는 `git rev-list --count origin/<브랜치>..HEAD` 로 세고 사용자 확인을 받을 것

### 미결 결정 — 영향 분석의 도구 경계 (사용자 보류, 2026-08-26)

독립 감사가 "새 도구는 정당하지 않다"(기존 3개에 `impact: true` 플래그면 된다)고 했다.
실측 비용: 현재 독립 도구 1,236 B 약 334 토큰/세션 / 플래그 3개 471~756 B 약 127~204 토큰
(대가: 호출자가 대상 종류를 먼저 알아야 하고 응답 형태가 3종) / 설명 축약 약 900 B 약 244 토큰.
**사용자가 현상 유지로 보류했다.** 지시 전까지 도구를 지우거나 합치지 말 것.

2026-08-27 의 P3-b 판단이 이 결정에 재료를 하나 더 준다 — §4-(25) 참조.
"새 능력은 기존 도구의 빠진 답인가, 새 질문인가" 를 가르는 기준이 그때보다 선명해졌다.

### Unity 를 켤 때 알아 둘 것

- ✅ `Window/MCP Server` 의 경로 탐색은 **2026-08-30 에 라이브로 검증했다**(§4-(46)).
  런처 경로가 채워지고 `Resolved Package` 가 `Local  C:/dev/unity-mcp` 를 답한다.
  **아직 안 본 것: git URL 로 받은 환경**에서 `resolvedPath` 가
  `Library/PackageCache/...@해시` 를 주는지. 그런 환경이 생기면 볼 것
- **준비 확인은 포트로 한다.** `~/.unity-mcp/auth-token-3000.json` 은 에디터를 재시작해도
  **갱신되지 않는다**(토큰 재사용). mtime 을 준비 신호로 쓰면 영원히 기다린다.
  `Get-NetTCPConnection -LocalPort 3000 -State Listen` 이 `::1` 과 `127.0.0.1` 둘 다 뜨면 준비된 것이다.
  실측 2026-08-27: 어셈블리가 최신이면 **20초**만에 뜬다(컴파일 없이)
- 인덱스 도구와 프로브는 Unity 없이도 동작한다. 켜는 이유는 **대조군**과 라이브 진단뿐이다
- **JS 만 고쳤으면 Unity 를 안 켜도 된다.** 단 `.meta` 는 Unity 가 만드므로
  새 파일을 커밋하려면 한 번은 물려야 한다
- **C# 을 고쳤으면 `unity_recompile_scripts` 로 충분하다** (2026-08-27 실측: 새 코드가
  라이브로 올라오기까지 **약 25초**). §1-(3) 의 "Assets/Refresh 후 4분" 은 콜드 임포트 포함
  최악치다. `.meta` 생성도 이 경로로 됐다
- **재컴파일이 실제로 컴파일했는지 확인할 것.** `observedAssemblyCount` 가 0 이면
  아무것도 다시 컴파일되지 않은 것이다 — 고친 코드가 검증된 적이 없다는 뜻이다
- 브릿지 호출 하네스가 없으면 stdio JSON-RPC 로 직접 물으면 된다. 라이브 서버는 §6 참조

### 오프라인 C# 컴파일 rsp (2026-08-27 재작성)

**`bash Tools/verify/make-offline-rsp.sh` 로 만든다**(2026-08-31). 예전에는 `C:/dev/codex-p3/` 에 만들어 둔 파일을 가리켰는데, 남의 Unity 설치 경로가 박혀 있어 다른 머신에서는 못 썼다. 생성기는 `ProjectVersion.txt` 를 읽어 **그 버전의 Unity** 를 쓰고(최신을 고르면 조용히 다른 Unity 로 컴파일한다), 경로를 `cygpath -m` 으로
Windows 형식으로 바꿔야 한다** — Git Bash 의 `/c/...` 를 csc 가 잘못 해석한다.

## 1. ⚠️ 먼저 알아야 할 함정 4개

**(1) `main` 브랜치를 쓰지 마라.**
프로젝트가 쓰던 커밋 `2ea969e` 는 `main` 의 조상이 **아니다**. 실제 작업 브랜치는
`optimized` 다. `main` 에는 `Editor/Core/McpJobStore.cs`(리로드 생존, 253줄)와
`McpToolError.cs` 가 없고 `Bridge/mcp-bridge.js` 가 269줄 적다.
`main` 기준으로 작업하면 큰 회귀가 난다.

**(2) 이 레포는 게임 프로젝트 밖에 있다.**
```
C:\dev\unity-mcp        ← 이 레포 (Seoki2000/unity-mcp)
C:\Unity\MainProject    ← 게임 레포 (Fujino-Tatsuya/MainProject, feature/Boss23)
```
게임 레포의 `Packages/manifest.json` 은 **커밋된 값은 git URL 그대로**이고,
로컬 워킹트리만 `file:../../../dev/unity-mcp` 다. `skip-worktree` 로 고정돼 있어
`git status` 에 뜨지 않는다. 자세한 내용과 pull 복구 절차는 `LOCAL_DEV_SETUP.md`.

**(3) Unity 는 `file:` 패키지 변경을 자동으로 안 문다.**
에디터가 포커스를 잃은 상태면 스크립트를 고쳐도 구 어셈블리를 계속 쓴다.
스키마에 새 파라미터가 안 보인다고 코드를 의심하기 전에:
```
unity_execute_menu { "menuPath": "Assets/Refresh" }
```
그 다음 `unity_get_compilation_status` 로 완료를 기다린다.

**(4) 오프라인 컴파일 검증 후 빌드 서버를 끄라.**
Roslyn 컴파일 서버가 파일 핸들을 잡아 디렉터리 이동이 `Device or resource busy` 로 막힌다.
```bash
dotnet build-server shutdown
```

---

## 2. 완료 상태

커밋 해시가 근거다. **어느 커밋이 무엇을 왜 바꿨는지는 커밋 메시지에 있다** —
이 표는 색인이고, 판단의 근거는 메시지 본문에 적어 뒀다.

| Phase | 커밋 | 내용 |
|---|---|---|
| 0-A | `2bb84de` | 경로 탈출 차단, 세션 토큰, CORS 제거, 메뉴 게이팅, 요청 상한 |
| 1 | `6698ddc` | 툴 annotations, 페이지네이션, 검색 랭킹, isError, SSE 캐리버퍼 |
| 1 | `1424e30` | 리소스 페이지네이션·라인범위, TypeCache, 응답 상한, 에러코드 |
| 1.5 | `9b39f93` | 아키텍처 결정(아웃프로세스) + 프로토타입 근거 |
| 2a | `26f353f` | 레이어 A(직렬화 데이터) + 조인 + 로컬 도구 5개 |
| 2b-1 | `d374773` | 레이어 B(어셈블리 심볼 + PDB 소스 매핑), 조인 완성 |
| 2b-2 | `a8d2b68` | IL 호출 그래프, find_callers/find_callees |
| 2c | (2026-08-24) | YAML 값 파서 + `unity_get_asset_components`, 캐시/목록 수정 |
| 2c-감사 | (2026-08-24) | 경로 봉쇄, 확장자 화이트리스트 폐기, `.meta` 참조, 자기참조 제외 |
| 3-P0 | (2026-08-24) | 레이어 D — 인스펙터 배선 / 타입 이름 문자열 / 속성 진입점 / 경로 로드 |
| 3-P0 교차검증 | `1102330` `53e0bf3` | 라이브 대조군(예상 5/5 적중) + `find_references` 타입이름 축 오답 38건 수정 |
| 3-P1 | `95872ad` | `unity_project_map` — 배치·진입점 지도. 중심성 랭킹 기각(측정), 인수 프로브 6/7 |
| 3-P2 | `25144ca` `5bf9382` `f75cf7c` | `unity_impact_analysis` — 축 9개를 합치지 않고 + `unknown`. 프로브 10/10. 동반 수정: ProjectSettings 스캔 / 캐시 중복 타입 / 없는 대상 / `find_callers` 리졸버 / `effectByOperation` |
| 3-P3a | `8f80fce` `1d294f2` `0a32e82` `cdf0709` | 인덱스 신선도 재검증 + 컴파일 진단 완결성 + 콘솔 절단 회계 + 오류 출처. **새 도구 0개, `tools/list` 불변**. 프로브 7/7 |
| 3-P3a+ | `d0714ad` `ca434fa` `9437042` | PDB SequencePoints 디코딩 -> 메서드 소스 위치. 오버로드 병합을 **구체적으로** 알린다(선언 수와 위치). 새 도구 0개. 프로브 7/7 |
| 3-P3a++ | `337276f` | `search_project` 의 `folder` 가 무시되던 것. 그게 곧 타임아웃의 원인이었다 — Assets 3,143 vs Packages 21,075 |
| 3-부채 | `c330cfb` `c8588ee` `7286212` | 중첩 타입 신원(`declaringType` + `Outer/Inner`). **호출 그래프 키는 안 바꿨다.** 프로브 8/8 |
| 3-P3b | `99a6fd7` `aa770e9` | `unity_explain_compile_errors` — 진단 x 인덱스 조인. Unity 왕복 0. 프로브 10/10 |
| 3-부채2 | `97e77da` `cef6fd2` `25270a9` | 필드 타입 서명 디코딩(ECMA-335 II.23.2.12). 전 어셈블리 필드 105,972개 실패 0건. 값 검증 경로에 연결. 프로브 8/8 |
| 3-P3b 수정 | `c0e6a04` | P3-b 가 붙은 에셋 수에 조용한 0 을 답하던 것(`scriptUsers` -> `scriptRefs`) |
| 3-부채3 | `d4c853d` `7b486e7` `89cc59b` | 오버로드 분리 — 시그니처 키 그래프를 **병렬로**. 이름 키 불변. 프로브 10/10 |
| 3-P3b+ | `f29dd3b` | P3-b 줄 귀속을 확신도 3단계로(exact/signature/gap). 프로브 14/14 |
| 3-검증 | `d674950` | `effectByOperation` 실검증 — 세 주장 모두 참(내 예상 하나는 틀렸다). 하네스 문서화 |
| 3-정리 | `9b5e2fc` `c459242` `2ae531e` `c1080ba` `519fa9b` | 압축정수 디코더 3->1벌 · 문서 수치 실측화(dev-0.0.6) · prototype 을 교차검증으로 명시 · `search_project` 경로 안내 · 약한 단언 조임 |
| 3-감사2 | `41a5932` `059d0de` `0e29494` | 2차 독립 감사 후속 — `find_callees` 거짓 0 / 시그니처 키에서 안전 축 소실 / `scannedAssetCount` 오염 / prototype 버그 2건(정션 실명 포함) |
| 3-정리2 | `fbc4bbd` `9a42712` `6a1731f` `bacc75b` | 문서 모순 9건 · 과대주장 프로브 5건 · 메서드 키 파싱 단일 출처(`methodkey.js`) · §6 전수 집계 재실행 스크립트 |

### 인덱스 성능 (MainProject, Unity 6000.3.16f1) — 2026-08-24 재측정
```
레이어 A    3,300 ms   수집 0.5s / .meta 3142 0.4s / YAML 1144파일 181MB 1.7s / 기타 텍스트 0.5s
레이어 B      550 ms   DLL → 사용자 어셈블리 14 / 타입 1169 / 소스 560
호출 그래프    70 ms   메서드 7170 디코딩, 실패 0, 엣지 8673
────────────────────
전체        3,900 ms   콜드. 참조 엣지 6,244 (맨GUID 18 / .meta 122 / 기타 텍스트 83)
캐시 로드     750 ms   대부분이 지문 재계산(파일 5,794개 stat)
질의          0~1 ms   find_references
```
⚠️ 위의 옛 표(레이어 A 615 ms)는 **정션 수정 이전** 수치였다(YAML 783파일 115MB).
지금 값이 큰 이유의 대부분은 그때까지 안 보던 35% 이고, 2026-08-24 에 추가된 몫은
맨 GUID 스캔 약 1.3 초다(§4-10).

비교: 인덱스 없는 기존 역참조 질의는 **1건에 2,425 ms**, 그 동안 에디터 메인스레드 점유.

### 로컬 인덱스 도구 12개 (브릿지가 Unity 를 거치지 않고 응답)
`unity_index_status` / `unity_index_rebuild` / `unity_find_references` /
`unity_find_component_usages` / `unity_find_missing_scripts` / `unity_get_type_symbols` /
`unity_find_callers` / `unity_find_callees` / `unity_get_asset_components` /
`unity_project_map` / `unity_impact_analysis` / `unity_explain_compile_errors`
→ `tools/list` 총 **85개** (Unity 73 + 로컬 12), **43,368 B** (배열 기준. 응답 줄 43,412 B)
(레이어 D 시점 82개 / 39,669 B — P1 +1,236 B, P2 +1,237 B, 합계 ≈ +670 토큰/세션)

---

## 3. 작업 목록 (이력 + 미완) — **다음 작업은 §0.5 를 볼 것**

이 절은 **이력**이다. 완료 항목(`[x]`)에 왜 그렇게 했는지와 실측이 붙어 있으므로
같은 결정을 다시 내리지 않게 남긴다.
⚠️ **남은 일의 정본은 §0.5 "남은 것" 이다.** 여기의 `[ ]` 와 완전히 같지 않다 —
§3 에만 있는 것(Phase 3 원안), §0.5 에만 있는 것(오버로드 키 교체 · gap 의 unknown · push)이
있다. 두 곳이 다르면 §0.5 를 따를 것(2026-08-28 감사에서 이 문장이 실제로 틀렸다).

- [x] ~~**YAML 컴포넌트 값 해석기**~~ — 완료(2026-08-24). `Bridge/index/yamlvalues.js` +
      `unity_get_asset_components`. 값은 인덱스에 넣지 않고 **질의 시점에 그 파일 하나만**
      파싱한다(일반 프리팹 3~4 ms, 최대 17 MB 프리팹 0.7 s). 전수 검증: 에셋 1,144개
      181 MB / 문서 69,891개에 **못 읽은 줄 0**, 정규식 스캐너가 찾은 GUID 전부가 값 트리에도 있다.
      필드 검사까지 붙였다 — 이름이 바뀐 필드가 남긴 낡은 키가 751개 중 19개에서 보인다.
- [x] ~~**Phase 3 P0 — 보이지 않는 엣지 인덱스**~~ — 완료(2026-08-24).
      `Bridge/index/wiring.js` + `symbols.js` 속성 수집 + `scan.js` 경로 로드.
      네 축 전부: UnityEvent 배선 24건(22건 조인 해석) / 타입 이름 참조 48타입·52엣지 /
      속성 붙은 메서드 243개 / 경로 로드 엣지 23개.
      완료 기준이던 두 재현이 답을 낸다 — `GoToResultButton` 배선 2건,
      `BombAction` → `Wells.asset`. **새 도구 0개**, `tools/list` 39,669 B 그대로.
      비용: 배선 0.27 s + 타입이름 0.08 s + 경로로드(레이어 A 안) — 콜드 5.8 s.
- [x] ~~**Phase 3 P1 — 프로젝트 맵(`unity_project_map`)**~~ — 완료(2026-08-26).
      **원안의 전제(중심성 랭킹)는 측정으로 기각했다.** 게임 레포 커밋 188건 정답지에서
      중심성 hit@50 28.2% < 부착 33.0% < churn 33.5%, 최고는 attachThenChurn 35.1%.
      PageRank 상위 20은 컴파일러 생성 타입·벤더 라이브러리·`[Conditional]` 로그 래퍼로 채워졌다.
      그래서 주역을 **배치·진입점**으로 바꿨다: 빌드 씬 목록 / 레지스트리(out-degree 상위 .asset) /
      씬·프리팹 배치 / 호출자 0인데 붙는 타입 69 / 진입점 4축(속성·배선·타입이름·조건부).
      `Bridge/index/projectmap.js`, 인수 프로브 `Tools/probe-project-map.js`(기본 6,000 에서 6/7).
      비용: `tools/list` +1,236 B, 질의 콜드 1,093 ms(git 250 ms 포함) / 웜 48 ms.
      설계·측정 전문: `C:/dev/unity-mcp-p1-design.md` (⚠️ 이 머신에만 있다).
- [x] ~~**Phase 3 P2 — 영향 분석(`unity_impact_analysis`)**~~ — 완료(2026-08-26).
      `Bridge/index/impact.js`. 축 9개를 **합치지 않고** 각각 낸다(합계를 만들면 어디를 고칠지
      알 수 없다). `unknown` 을 응답의 일부로 싣는다 — 동적 로드 44, 인터페이스 구현 미인덱스,
      인덱스 밖 엣지, 바이너리 681, 디스크 상태, 오버로드 병합, 중첩 타입 이름 충돌.
      대상은 타입 / `Type::Method` / 에셋(경로·GUID). 전이는 `depth`(기본 1, 최대 3)로 명시,
      상속 폐쇄만 완전. 팬아웃 실측(메서드 120 표본): depth1 중앙 1·p90 3·최대 59 /
      depth2 중앙 1·p90 5·최대 76. 인수 프로브 `Tools/probe-impact-analysis.js` 10/10.
- [ ] ~~Phase 3 원안~~ (제안서: `C:/dev/unity-mcp-phase3-plan.md` — ⚠️ 이 머신에만 있다)
      🔴 **아래 본문은 P0(`b0f2c2e` 계열)로 이미 해결된 상태를 현재형으로 말한다.**
      `BombAction` 은 지금 `referencedByTypeName [Wells.asset]` + "NOT unused" note 를 받고,
      `GoToResultButton` 은 인스펙터 배선 2건을 받는다(§6 회귀 기준). 그리고 **수치 셋은
      원안 작성 시점의 추정치**다 — 실측은 타입이름 48타입·52엣지 / 속성 243 /
      경로 로드 해석 25·미해석 2·동적 44 다. 아래는 **그때의 제안서 문장으로만** 읽을 것.
      지금까지 고친 것은 전부 **데이터 → 데이터** 참조다. 반대 방향(**데이터 → 코드**)은
      인덱스에 없고, 같은 형태의 오답이 코드 축에 그대로 남아 있다. 실측 재현:
      `BombAction` 은 세 도구가 모두 "아무도 안 쓴다" 고 답하지만 보스 행동트리의 노드이고,
      `GameManager.GoToResultButton` 은 호출자 0 이지만 씬 2개에 인스펙터로 배선돼 있다.
      이 프로젝트의 보이지 않는 엣지: UnityEvent 배선 **24**건 / Behavior 타입 문자열 **32**건 /
      속성 진입점 **112**건 / 경로 기반 로드 **73**건.
      UnityEvent 는 프로토타입으로 **24/24 해석**을 확인했다(기존 모듈만, 1 s).
      JetBrains Rider 가 2018.3 부터 같은 것을 인덱싱한다 — 접근이 검증돼 있다.
- [x] ~~**Phase 3 P3-a — 진단 정직성과 인덱스 신선도**~~ — 완료(2026-08-27). **새 도구 0개.**
      ① `ensureIndex` 가 한 번 올린 인덱스를 세션 내내 재검증하지 않던 것(`8f80fce`) —
      2단 검사로 고쳤다(어셈블리 서명 11~18 ms 매 호출 / 전체 지문 690 ms 10초 throttle).
      ② 컴파일 상태가 `unobserved` / `collecting` / `complete`+observed 0 / `complete`+observed N
      을 구분한다(`1d294f2`). 예전엔 넷이 같은 응답이었다.
      ③ 콘솔이 버린 개수를 센다(`0a32e82`). ④ 오류 출처 분류(assets/packageCache/embedded/local).
      프로브 `Tools/probe-verify-loop.js` 7/7. `tools/list` 42,142 B **불변**.
- [x] ~~**Phase 3 P3-b — 조인 복합 도구**~~ — 완료(2026-08-27, `99a6fd7` `f29dd3b`).
      `unity_explain_compile_errors`. **Unity 로 왕복하지 않는다** — 진단을 입력으로 받는 순수
      로컬 조인이라 전송 계층 리팩터(독립 감사가 지목한 최대 위험)와 30초 큐·리로드 노출이
      통째로 사라졌다. 줄 귀속에 확신도 3단계(`exact`/`signature`/`gap`).
      **컴파일 실패 시 어셈블리가 안 갱신되는 것은 못 고쳤고, 대신 말한다** —
      `freshness.state: last-good`. 실측으로 확인했다(실패 전후 어셈블리 서명 바이트 동일).
      비용 +1,226 B. 프로브 14/14.
      만들더라도 기존 도구 응답 가공(A)이 아니라 **별도 브릿지 도구(B)** 로 격리한다 — §4-(25).
- [x] ~~**PDB SequencePoints 디코딩**~~ — 완료(2026-08-27, `d0714ad`). `MethodDebugInformation`
      의 1번 컬럼을 디코딩해 메서드마다 `line`/`endLine` 을 낸다.
      🔴 **그 디코딩이 틀렸다 — 2026-08-28 에 고쳤다(§4-(34)).** 당시 적은 "Assembly-CSharp
      3,027개 해석 / 716개 없음 / 오류 0, 디스크 대조 표본 40/40" 중 **오류 0 과 40/40 이
      전수에서 무너진다**: 전수 대조로 27.4% 불일치 / 겹치는 범위 3,036쌍 / 음수 줄번호
      126개. 원인은 숨은 시퀀스 포인트 뒤의 절대 줄을 부호있는 델타로 읽은 것이다.
      고친 뒤 전수 0% / 겹침 0 / 음수 0, 현재 모집단은 3,005 해석 / 689 없음.
      ⚠️ 당시 적은 콜드 빌드 5.66 -> 7.15~7.45 s 는
      **Unity 재컴파일 중에 잰 값이라 무효다.** 그때 적은 4.77~4.92 s(Unity 유휴)도 이제 낡았다 —
      현재는 **5.69~6.90 s(Unity 꺼짐)** / 4.67~4.71 s(Unity 유휴). §0.5 표를 볼 것.
      캐시 +184,618 B (+5.7%). 웜 질의 불변.
      ⚠️ **본문 범위이지 시그니처가 아니다** — 첫 시퀀스 포인트가 여는 중괄호 다음에 찍힌다.
      컴파일 오류 위치의 포함 검사로 쓰면 시그니처 줄 오류가 범위 밖으로 떨어진다.
      P3-b 를 할 때 이 자리를 먼저 볼 것(선언 줄까지 위로 넓히거나, 다음 메서드로 귀속).
- [x] ~~**필드 타입 서명 디코딩**~~ — 완료(2026-08-27, `97e77da` + `25270a9`).
      `Bridge/index/sigtypes.js` 에 ECMA-335 II.23.2.12 Type 디코더. 원시 타입은 C# 이름으로,
      제네릭은 `List<Unit>` 형태로. 모르면 null(추측 금지). 전 어셈블리 필드 **105,972개
      실패 0건**, 순수 디코딩 68 ms, 디스크 대조 150/150.
      `unity_get_type_symbols` 의 `fields[].type` 과 `unity_get_asset_components` 의
      `fieldCheck.declaredTypes` 양쪽에 실린다 — **처음엔 후자를 빼먹었다**(§4-(21)).
      값 자체는 판정하지 않는다. Unity 직렬화 매핑을 추측하면 거짓 양성이 나오고,
      삭제·수정 판단에 쓰이는 신호에서 거짓 양성이 가장 나쁘다.
- [x] ~~**오버로드 구분**~~ — 완료(2026-08-27, `d4c853d` `7b486e7`). **키를 교체하지 않았다.**
      사용자가 "키 바뀌는 거 감안하고" 지시했지만 먼저 재니 오버로드된 키를 향하는 엣지가
      8,673개 중 **366개(4.2%)**, 영향 키 101개의 상위가 대부분 벤더였다. 그리고 결정적으로
      **UnityEvent 배선과 속성은 메서드 이름만 갖고 있어** 이름 단위 롤업이 어차피 필요하다 —
      키를 바꿔도 버릴 수 있는 것이 아니다. 그래서 `callsFromSig`/`callersOfSig` 를
      **병렬로** 얹었다. `find_callers` 가 `Type::Method(int,string)` 를 받고, 이름 질의에는
      `perOverload` 로 분해를 준다. 소비자 6곳 파급 0, 기준선 전부 유지.
      되돌리고 싶으면 위 수치가 근거다.
- [x] ~~**중첩 타입 이름 충돌**~~ — 완료(2026-08-27, `c330cfb` `c8588ee`). **규모부터 정정했다.**
      123은 **레코드 수**이고 고유 이름은 **29개**, 그중 19개가 컴파일러 생성이다.
      사용자가 질의할 만한 것은 PassData(3)·Segment(2)·Tab(2)·FactorySettings(2)·EffectState(2)
      다섯이고, 그중 벤더를 빼면 **자기 코드는 PassData 와 Segment 둘**이다.
      다섯 개를 위해 이름 체계를 바꾸면 호출 그래프 키까지 바뀌므로 **키는 그대로 뒀다.**
      대신 `NestedClass`(0x29)로 `declaringType` 을 싣고 `Outer/Inner` 별칭(맵 374개)을 준다.
      모호하면 거절하되 `candidates` 로 유일한 이름을 준다.
- [x] ~~**`effectByOperation` 실검증**~~ — 완료(2026-08-27, `d674950`).
      워크트리 복사(1.2 GB) 대신 **자체 픽스처**(스크립트 3개 + 프리팹 YAML 1개)로 세 축을
      동시에 걸고 이름 변경을 세 형태로 적용했다. 절차와 관측은 `Tools/verify-effect-by-operation.md`.
      **세 주장 모두 참이고 내 예상 하나는 틀렸다** — 클래스만 바꿔도 부착이 살아남는다
      (`get_prefab_info` 가 새 클래스로 보고). 다만 우리 도구의 `typeResolution` 이
      `filename-match` -> `unity-derived` 로 강등되는 것이 관측 가능한 신호다.
      ~~**안 덮은 것**: 파일 하나에 MonoBehaviour 여럿일 때의 클래스 이름 변경, `moveOrReimport`, `delete`.~~
      → **셋 다 2026-08-28 에 돌렸다**(E4·E5·E6, 같은 문서). 셋 다 주장대로였고,
      그 과정에서 `danglingLoads` 를 넣었다. 지금 안 덮은 것은 Resources 키 축의 이동과
      씬 안의 배선뿐이다.
- [x] ~~**증분 갱신**~~ — 부분 완료(`da98185`). 디스크 지문을 대조해 **낡음을 감지**하고
      전체 재빌드로 넘긴다. 변경 파일만 재파싱하는 진짜 증분은 아니다 — 전체 재빌드가
      1.1 초라 그 편이 단순하고 확실해서 여기서 멈췄다.
      ⚠️ **그 판단의 근거 수치가 낡았다**(2026-08-28): 전체 재빌드는 이제 **5.69~6.90 s**
      (Unity 꺼짐)이고 전체 지문은 **678~702 ms** 다. 5배가 됐으므로 "단순한 쪽" 이라는
      결론은 다시 세워야 한다. 지문 계산이 554 ms 이므로 그게
      부담되면 그때 진짜 증분으로 간다.
- [ ] **Missing Script 실제 정리** — 아래 §5. 2026-08-24 재측정: **14개 GUID / 134 에셋 /
      164 참조**. 캐시 인덱스와 PackageCache 포함 전체 재빌드 두 경로가 같은 값을 낸다.
      어제 기록한 13/133/163 과 1씩 다른 이유는 **프로젝트가 그 사이에 늘었기 때문**이다
      (README 표의 YAML 1,131 → 현재 1,144). 도구가 바뀐 게 아니다.
      새 도구로 값까지 볼 수 있게 됐으니 판단 근거가 늘었다:
      `unity_get_asset_components` 로 그 컴포넌트를 보면 `m_EditorClassIdentifier` 에
      사라진 타입의 전체 이름이 남아 있고(`VeyTrace.Rendering.Occlusion.OcclusionSection`),
      `renderers` 같은 필드값도 그대로 보존돼 있다 — 복원할지 지울지 정하는 데 쓸 것.
- [x] ~~**`unity_search_project` 의 `folder` 버그**~~ — 완료(2026-08-27, `337276f`).
      **전제가 틀렸다.** 백로그는 "타임아웃에 걸리니 인덱스 기반으로 교체" 였는데,
      타임아웃은 참조 검색의 고유 비용이 아니라 **`folder` 를 무시하던 버그**였다.
      실측: `GetAllAssetPaths()` 24,233 = Assets **3,143** + Packages **21,075**.
      프로젝트 에셋 하나를 묻는 데 패키지 2만 개에 `GetDependencies` 를 돌리고 있었다.
      folder 를 지키게 하니 **31초 타임아웃 -> 즉시 완주**. 상한 8,000 초과는 거절하고
      `unity_find_references` 를 가리킨다. 빈 스코프도 거절한다(참조 0 이 아니라 folder 가 틀린 것).
      **교체는 하지 않았다** — 교체할 이유였던 타임아웃이 사라졌기 때문이다.
- [x] ~~**확장자 허용목록의 구조적 한계**~~ — 버렸다(2026-08-24). 확장자로 고르지 않고
      **내용으로** 고른다: 앞 512바이트에 NUL 이 없으면 텍스트로 보고 훑는다.
      비용은 스니핑 1,508개 357 ms + 텍스트 816개 8.7 MB (바이너리 692개 945 MB 는 안 읽는다).
      이걸로 놓치던 (출처,대상) 쌍 83건과 **"참조 0" 오답 19건**이 사라졌다 — `.shadergraph`,
      `.asmdef`, `.hlsl` 이 목록 밖이었다. `.meta` 도 출처로 넣어 18건이 더 사라졌다.
- [ ] **컴포넌트 값 파싱 결과 메모** — 지금은 질의마다 다시 파싱한다. 17 MB 프리팹을
      페이지로 훑으면 매 호출 0.7 s 를 다시 쓴다. 한 칸짜리 메모(경로+mtime+size 키)면
      충분하지만, 25,449개 문서의 파싱 트리를 상주시키는 메모리 비용을 먼저 재야 한다.
      일반 프리팹이 3~4 ms 라 아직 급하지 않아 넣지 않았다.
- [ ] **패키지 어셈블리 심볼** — 실측 완료(2026-08-28, 파견). **문구가 틀렸으니 고쳐 읽을 것**:
      "어셈블리 → 타입 이름" 맵은 조인 키가 아니라 **null 을 하나도 못 메운다.** 필요한 것은
      **`GUID → 타입 이름`** 이고, 그 키는 실재한다 — 패키지 어셈블리의 PDB Document 경로가
      **상대 경로**(`.\Library\PackageCache\...`)여서 `toProjectRelative` 가 버리고 있었을 뿐이다
      (직접 재현: PDB 189개 중 절대 15 / 상대 174, PackageCache document 10,849). §4-(37).
      실측: null 6,156/7,020(87.7%) → 얕은 맵이 5,365 를 메운다(guid 160/162).
      대가 +832 KB(+18.6%) / 콜드 +3.9 s(표적 `.meta`) / 파싱 +3~5 ms.
      검증 두 오라클 0 오차(`.cs` 원문 160/160, ECID 593/593).
      ⚠️ 메워지는 것의 83%가 VFX 내부 객체다. **먼저 할 것은 0 비용인 ECID 승격** —
      상세와 권고는 §0.5 "남은 것" 5번.
- [x] ~~**`totalReferenceCount` 이름 정정**~~ — 완료(2026-08-28). `assetGuidPairCount` 로,
      행 단위 `referenceCount` 는 `referencingAssetCount` 로 바꿨다.
      **여기 적어 뒀던 `affectedAssetCount` 는 채택하지 않았다 — 그 이름이 같은 응답에
      이미 있었다**(전역 중복 제거 에셋 수 134 vs 쌍 수 164). §4-(35).
      깨질 자리를 먼저 셌다: 리터럴 5건 = 생산 1 + 문서 4, **코드 소비자 0** — 그래서 별칭 없이 교체.

---

## 4. 내가 작업 중 **틀렸다가 정정한 것** (다시 재현하지 말 것)

48개가 됐다. 제목만으로는 못 찾으므로 **주제별 색인**을 앞에 둔다.
새 기능을 시작하기 전에 **해당 줄의 교훈들을 먼저 읽는 것**이 이 절을 쓰는 방법이다.

| 언제 읽나 | 교훈 |
|---|---|
| **인덱스에 새 축·필드를 넣을 때** | (20) 새 능력을 새 도구로 내지 않았다 · **(21) 데이터를 넣고 도구의 0 을 안 고쳤다** · (22) 같은 실수를 새 도구에서 또 했다 · (26) (21) 을 읽은 직후 (21) 을 재현했다 · **(30) 필드를 늘리는 것과 그 필드가 맞는 값을 담는 것은 다르다** |
| **분류·판정을 만들 때** | **(41) 정밀도만 보고 재현율을 세 번 놓쳤다 — 두 방향을 같이 재라, 그리고 정답지는 독립이어야 한다** |
| **정리·삭제를 할 때** | **(48) 삭제만 하는 작업은 diff 의 추가가 0 이어야 한다 · 프리팹은 `m_Component` 도 지워야 한다 · 정리 뒤 기준선이 내려간 것을 회귀로 오독하지 말 것** |
| **정리·삭제를 할 때** | **(43) 그 판단에 쓰는 도구를 먼저 의심하라 — 조인 키가 없는 실패는 조인이 못 본다. 그리고 기준선 감소가 개선인 경우가 있다** · **(44) 지우기 전에 "지워지긴 했나" 를 물어라 — 미머지 브랜치의 흔적일 수 있다** |
| **도구의 수치를 읽을 때** | **(44) 단위를 확인하라 — 도구의 "참조 N" 은 (에셋,GUID) 쌍이지 컴포넌트 수가 아니다(132 vs 560)** · (23) 노드와 엣지의 정의 · **(35) 이름이 이미 다른 뜻으로 있을 수 있다** |
| **새 축·필드를 낼 때** | **(42) "없어야 한다" 만 단언하면 죽은 분기를 초록불로 덮는다 — 답이 나오는 입력을 합성해서라도 넣어라** |
| **프로브를 쓸 때** | (22) 프로브가 축 누락을 잡는다 · **(24) 프로브를 먼저 써도 경계 조건은 안 덮인다** · (27) 표본을 추측으로 고르면 보장이 없다 · **(31) 프로브가 구현과 같은 실수를 하면 검사가 안 된다 — 교차검증만 잡는다** · **(34) 표본 통과는 전수의 증거가 아니다 — 잴 수 있으면 전수로 재라** · **(36) 불일치가 나오면 데이터와 검사 중 무엇이 틀렸는지 먼저 가르라** |
| **수치를 잴 때** | (16) 내 측정 자체가 페이지 상한에 걸려 있었다 · (19) "원리적으로 불가능" 을 다시 재라 · (23) 노드와 엣지의 정의를 먼저 재라 · **(28) 백로그의 전제부터 다시 재라** · **(32) 0 이 나오면 "재는 방법이 틀렸다" 를 먼저 의심하라** · **(34) "둘 다 참일 수 없는" 불변식(겹침·음수)을 찾으면 소스를 안 읽고도 잡힌다** |
| **응답을 설계할 때** | (12) 부분 커버리지에서 `exists: false` 를 쓰지 말 것 · (17) 조용히 잘리는 것은 계속 새 형태로 나온다 · **(29) 제시한 길이 막다른 길이면 거절만도 못하다** · **(35) 지표 이름을 바꿀 때 그 이름이 이미 쓰이는지 보라** |
| **백로그 항목을 착수할 때** | **(28) 전제부터 다시 재라** · **(35) 백로그가 적은 이름이 이미 다른 뜻으로 있을 수 있다** · **(37) 문구대로 만들면 답이 안 나오는 항목이 있다 — 조인 키부터 확인하라** |
| **셋업·재현을 다룰 때** | **(46) 우회로를 문서화하면 정문이 고장 난 것을 아무도 보고하지 않는다 — UI 가 문서와 같은 답을 내는지 같이 확인하라. 그리고 새 위치에 파일을 만들면 커밋 전에 `git check-ignore -v`** |
| **새 능력을 넣은 직후** | **(47) 이 답을 원래 누가 냈어야 하나 — 그 도구의 note 를 읽어라. 못 한다고 적혀 있으면 그게 신호다** · (21)(30) |
| **에디터 안의 동작을 판단할 때** | **(46) 끄고 잰 값으로 켜고 도는 코드를 판단하지 말라 — 파일 존재 여부마저 조건이 다르면 다른 답이 나온다** · (39) 조건 없는 관측 |
| **도구를 늘릴지 말지** | (20) 기존 질문의 빠진 답인가 새 질문인가 · **(25) 그 원칙을 과잉적용했다 — 적용 조건까지 적어라** |
| **캐시를 건드릴 때** | (13) 캐시와 도구 목록이 조용히 죽어 있었다 · (18) 캐시 버전을 안 올리면 고친 것이 안 먹는다 · (34) 디코딩을 고치면 구 캐시가 틀린 값을 담고 있다 |
| **바이너리 규격을 디코딩할 때** | **(34) 상태 변수 하나가 두 뜻을 가지면 어긋난다 — 규격의 조건절마다 상태를 따로 두라** |
| **문서·인수 자료를 쓸 때** | **(32) 기준선에는 다시 재는 명령을 같이 적어라** · **(33) 명령이 전제하는 상태까지 적거나, 명령이 그 상태를 만들게 하라** · **(36) 검사의 한계를 검사 안에 적어라** · **(40) 재측정 명령이 붙은 수치만 산다 — 산문이 든 값은 전부 흘렀다** · **(39) 조건 없이 적은 관측은 회귀로 읽힌다** |
| **보안·경로** | (14) 새 도구가 막았던 구멍을 다시 뚫었다 · (15) 화이트리스트는 세 번 실패했다 |
| **외부 검증** | (8) "Unity 쪽이 더 권위 있다" 는 틀렸다 · (9) 검증이 내 구현을 두 번 고쳤다 · (25)(31) 독립 감사가 잡은 것 · (37) 파견 측정이 백로그 문구의 전제를 뒤집었다 |

**세 번 이상 반복된 형태 둘.**

1. **데이터·필드·규칙을 새로 넣을 때 그것을 쓰는 자리를 빠짐없이 훑지 않는다.**
   (21)(22)(26)(30) 이 전부 같은 실수다. 실효적인 대책은 하나뿐이었다 —
   **그 값을 세팅/소비하는 자리가 몇 개인지 `grep` 으로 먼저 센다.**
   2026-08-28 의 이름 교체(35)에서 실제로 먼저 셌고(리터럴 5건 / 코드 소비자 0),
   그래서 별칭 없이 교체할 수 있었다.
2. **표본으로 재고 전수라고 적는다.** (16)(27)(31)(34) 가 같은 형태다. (34) 는 그 대가가
   가장 컸다 — 표본 40/40 이 통과한 층이 전수로는 27.4% 틀렸고, 그 사이 나흘 동안 그
   층 위에 도구를 두 개 더 얹었다. **잴 수 있으면 전수로 잰다**(그 대조가 61 ms 였다).


다음 세션이 같은 함정에 빠지지 않도록 남긴다.

**(1) "심볼 인덱스는 부가가치가 낮다" — 틀렸다.**
초기에 "Claude Code 가 이미 `.cs` 를 읽으니 Roslyn 인덱스는 낮은 우선순위"라고 판단했다.
Fab 페이지가 반증했다. NarshaADK 의 PDB 레이어 존재 이유가 정확히 그 반대다
("not just text matching", "call graphs with minimal false positives").
**"읽을 수 있다"와 "이해한다"는 다르다.** 실측으로 확인됐다 — `TryResolveHit` 를 grep 하면
20건 중 4건이 주석이고, 호출 그래프는 실제 호출자 8개를 준다.

**(2) "무제한 응답이 문제" — 부정확했다.**
대부분 도구엔 이미 하드코딩 상한이 있었다. 진짜 문제는 **잘린 뒤를 가져올 방법이 없다**는 것.
`unity_get_assets` 가 에셋 3144개 중 30개만 주고 나머지 3114개는 조회 불가였다.

**(3) "전수 스캔이 30초 메인스레드 캡을 넘는다" — 틀렸다.**
30,072ms 를 측정했지만 그건 도메인 리로드 중 `-32001 busy` 응답이었다. 참값은 2.4초다.
또 `maxResults` 가 작으면 조기 break 해서 빠르게 보인다(3건 요청 시 62ms).
최악 케이스를 재려면 참조가 적은 대상을 골라야 한다.

**(4) Missing Script 89건 — 79건이 오분류였다.**
Assets/Packages 만 스캔한 상태로 판정하면 패키지 스크립트가 전부 "없다"고 잡힌다.
`find_missing_scripts` 는 반드시 전체 GUID 커버리지(PackageCache 포함)를 먼저 확보해야 한다.
또 `0` 으로 시작하는 GUID(`0000000000000000e000000000000000` = UnityEditor.dll)는
Unity 내장 어셈블리로 정상이다.

**(5) 타입 해석에 base-type 만 보면 안 된다.**
`WallOcclusionMaterialBinder.cs` 가 같은 파일의 보조 struct 로 해석됐다.
정적 유틸 클래스(`base=System.Object`)는 MonoBehaviour 필터에 걸리지 않는다.
Unity 규칙인 **파일명 == 타입명**을 1순위로 써야 한다.

**(6) `08_NARSHAADK_COMPARISON.md` 의 주장은 추측이 아니었다.**
Rust 단일 바이너리, PDB 인덱스, shared daemon 전부 Fab 페이지에서 확인됐다.
내가 한동안 "검증 안 됨"으로 깎아뒀던 것이 잘못이었다.

---

**(7) 정확도 감사(2026-08-23~24)에서 드러난 것 — 이게 가장 중요하다.**

인덱스가 **프로젝트의 35% 를 못 보고 있었다.** Windows 정션은 `readdirSync` 의 Dirent 에서
`isDirectory()` 거짓 / `isSymbolicLink()` 참이라, `isDirectory` 와 `isFile` 두 분기만 보던
워커가 통째로 건너뛰었다. `Assets/50.Art` 가 그 정션이고 `.meta` 1,105개가 빠졌다.
그런데 통계는 `filesFailed: 0` 을 계속 보고했다 — **건너뛴 것은 실패로 세지 않는다.**
수치 근거를 볼 때 "실패 0" 을 커버리지의 증거로 쓰지 말 것.

그리고 `unity_find_references` 가 **실제로 참조되는 에셋에 `totalCount: 0` 을 오류도 경고도
없이 답하고 있었다.** `YAML_EXT` 가 6종뿐이라 `.vfx` 등에서만 참조되는 에셋이 인덱스에
없었다. 삭제 판단에 쓰이는 도구에서 가장 나쁜 형태의 답이다.

**(8) "Unity 자체 의존성 DB 가 더 권위 있다" — 틀렸다. 확인했다.**

0건 응답에 "권위 있는 `unity_search_project` 로 확인하라" 는 안내를 넣었다가 정정했다.
Unity 를 열어 재보니 `AssetDatabase.GetDependencies` 는 **VFX Graph 의 그래프 구조 참조를
모른다.** `recursive` 양쪽 모두 그렇다. 확장자별 전수 비교 결과 `.prefab`/`.unity`/`.mat`/
`.asset`/`.controller` 는 누락 0건이고 맹점은 VFX 계열에만 있다(프로젝트 자산 기준 18건).
즉 이 인덱스가 그 축에서는 Unity 보다 완전하다. **어느 한쪽도 완전하지 않다 —
0 은 어느 쪽에서 나와도 참조 없음의 증거가 아니다.**

**(9) 내 구현을 검증이 두 번 고쳤다. 검증 없이 넘기지 말 것.**

캐시 지문을 `(파일 수, 총 바이트, 최대 mtime)` 으로 만들었는데, 가장 새로운 파일보다
오래된 파일의 mtime 변경이 **감지되지 않았다**(최댓값이 그대로다). 파일별 (mtime, size) 를
접어 **합산**하는 방식으로 바꿨다. 그리고 상속 판정의 간접 상속을 고쳤더니 대조군에서
`MonsterSpawner` 가 여전히 `false` 였다 — 체인이 패키지 어셈블리에서 끊기는데 그걸 false 로
단정했다. **프리팹 9개에 붙어 있는데도** 그랬다. `true/false/null` 3상태로 바꿨다.

교훈: 대조군 없는 음성 결과를 믿지 말 것. 두 번 다 대조군이 문제를 드러냈다.


**(10) 값 해석기를 만들자 레이어 A 가 또 거짓말하고 있었다 (2026-08-24).**

새 파서의 결과를 기존 정규식 스캐너와 대조했더니 **양쪽이 서로 다른 것을 놓치고 있었다.**
- 파서 쪽 누락: GUID `0000000000000000e000000000000000` 을 지수표기 숫자로 읽어 **0 으로 바꿨다**.
  씬 71개에서 그랬다. 값이 사라진 게 아니라 조용히 다른 값이 되는, 더 나쁜 형태다.
- 스캐너 쪽 누락: VFX Graph 는 참조를 **YAML 문자열 안의 JSON** 으로 쓴다
  (`m_SerializableObject: '{"obj":{"fileID":…,"guid":"…"}}'`). TMP 는 `m_SourceFontFileGUID`,
  Addressables 는 `m_GUID` 로 맨 GUID 를 쓴다. `guid:\s*<hex>` 정규식은 셋 다 못 본다.
  **실제로 참조되는 에셋 3건이 "참조 0"** 이었다(자기 SDF 에셋이 쓰는 `NotoSansKR.ttf` 포함).

형태를 하나씩 추가하는 대응은 확장자 화이트리스트에서 이미 실패한 방식이다. 그래서
**32자리 hex 토큰 중 실제 `.meta` GUID 인 것**만 참조로 치는 쪽으로 갔다. 형태를 모른 채로도
맞는다. 비용은 YAML 스캔 515 ms → 1.9 s(정규식 둘을 한 스캔으로 합쳐 2.5 s 에서 줄인 값).

교훈: **대조군은 새 구현을 검증할 때만 쓰는 게 아니다.** 새 구현이 기존 구현의 오류를 드러낸다.
어제 "정확도 감사 완료" 라고 적었지만 같은 종류의 오답이 형태만 바꿔 남아 있었다.

**(11) "짧은 이름이 유일하면 그 타입이다" — 즉시 오답이 났다.**

본문이 빈 클래스(`partial class X : Y { }`)는 메서드가 없어 PDB 가 소스 파일을 안 알려준다.
그래서 파일명으로 되돌아가 찾는 폴백을 넣었는데, 전수 스윕을 돌리자마자 URP 패키지의
`Volume.cs` 가 프로젝트 안 `Ami.BroAudio.Volume` 으로 해석됐다. 인덱스 안에서만 유일했을 뿐이다.
안전장치 둘을 걸어야 했다 — (a) 스크립트가 `Assets/` 안일 것, (b) 후보 타입이 **어떤 소스
파일에도 매핑되지 않을 것**. 폴백을 넣을 때는 그 폴백이 틀릴 수 있는 경우를 먼저 세어 볼 것.
**전수 스윕이 아니었으면 못 봤다.** 단위 테스트 몇 개로는 안 나온다.

**(12) 부분 커버리지에서 `exists: false` 를 쓰면 안 된다 — §4-4 와 같은 함정을 새 도구에서 반복했다.**

`Assets`/`Packages` 만 인덱싱한 상태로 스윕을 돌렸더니 **4,667건이 "없는 스크립트"** 로 잡혔다.
실제는 270건이다(17배). PackageCache 안의 스크립트가 전부 없는 것으로 보인 것 —
`find_missing_scripts` 가 이미 겪었던 그 함정을 새 도구에서 그대로 반복했다.
`exists` 를 `true/false/null` 3상태로 바꿨다. **모를 때 false 를 내면 안 된다**는 원칙은
도구를 새로 만들 때마다 다시 적용해야 한다 — 자동으로 상속되지 않는다.

**(13) 캐시와 도구 목록이 조용히 죽어 있었다.**

`find_missing_scripts` 가 PackageCache 를 병합한 뒤 캐시를 저장할 때 지문이 `null` 로 기록됐다
(캐시에서 올린 인덱스에 `fingerprint` 를 복원하지 않아서다). 다음 세션은 그걸 "구 캐시" 로 보고
버렸다 — 답은 계속 맞았고, 매번 3.9 초를 다시 썼을 뿐이라 아무도 눈치채지 못한다.
그리고 Unity 가 꺼져 있을 때 `tools/list` 를 디스크 캐시로 답하는 경로가 **로컬 도구까지
캐시 그대로** 냈다. 이번에 추가한 도구가 목록에서 통째로 빠졌다. 로컬 도구는 Unity 없이
동작하는 것이 존재 이유인데 하필 Unity 가 없을 때 사라진다.
**"틀린 답" 만 결함이 아니다. 맞는 답을 비싸게 내거나, 있어야 할 것이 안 보이는 것도 결함이다.**


**(14) 새 도구가 Phase 0-A 에서 막은 구멍을 다시 뚫었다 (2026-08-24).**

`getAssetComponents` 는 호출자가 준 경로를 봉쇄 없이 `path.join(root, asset)` 해서 읽었다.
`../../..` 로 프로젝트 밖 `.prefab` 을 만들어 부르니 내용이 그대로 응답에 실렸다.
이 파일(`queries.js`)의 다른 질의는 전부 맵 조회라 파일을 열지 않았고, 그래서 여기엔
봉쇄가 없었다 — **파일을 여는 첫 로컬 도구**를 만들면서 그 사실을 놓쳤다.
독립 감사(Codex)도 같은 것을 찾아 PoC 를 만들었다.

교훈: 보안 조치는 레이어에 붙는 게 아니라 **동작에 붙는다.** C# 쪽에 `McpPathGuard` 가
있다는 것이 JS 쪽에서 파일을 여는 것을 막아 주지 않는다. 새 도구가 새 종류의 부작용
(파일 읽기/쓰기/네트워크)을 처음 하는 것이라면 그 부작용의 가드가 이 레이어에 있는지 본다.

**(15) 화이트리스트는 세 번째로 실패했고, 이번엔 버렸다.**

확장자 목록 6종 → 24종으로 늘려도 `.shadergraph`(텍스처를 JSON 안 GUID 로 참조)와
`.asmdef`(어셈블리를 "GUID:…" 로 참조)는 여전히 밖이었다. **에셋 19개가 "참조 0"** 이었다.
`.meta` 는 아예 출처로 보지도 않았다(FBX 임포터의 `externalObjects`) — 18개가 더 있었다.

이제 확장자로 고르지 않는다. 앞 512바이트에 NUL 이 없으면 텍스트로 보고 훑는다.
**판정이 틀리는 방향을 고른 것이 핵심이다** — 바이너리를 텍스트로 오인하면 비용만 들고,
텍스트를 놓치면 답이 틀린다. 비용이 틀리는 쪽으로 실패하게 둔다.
비용은 스니핑 357 ms + 텍스트 8.7 MB(바이너리 945 MB 는 안 읽는다).

원리적으로 남는 한 축은 **GUID 로 저장되지 않는 참조**다(`Resources.Load("path")`,
Addressables 주소, 런타임 조립 경로). 어떤 정적 인덱스도 못 본다 — 0 응답에 그렇게 적었다.

**(16) 내 측정 자체가 페이지 상한에 걸려 있었다.**

전수 스윕을 도구 호출로 돌렸는데 파일당 500개 페이지 상한에 걸려 **컴포넌트 39,921개를
안 보고** 있었다. 스크립트 컴포넌트를 5,418개로 적었지만 실제는 7,019개다.
출력에 "not examined due to cap" 을 찍어 놓고도 그 아래 숫자를 문서에 그대로 옮겼다.
Codex 의 독립 집계(7,019 / 864)가 어긋나서 알았다.

교훈: **도구로 도구를 측정하면 도구의 한계가 측정에 들어온다.** 전수 수치는 상한이 없는
경로로 재고, 재는 코드가 도구와 같은 함수를 쓰는지 확인한다(그래서 `_checkFields` 를
측정용 시임으로 내보냈다).

**(17) 조용히 잘리는 것은 계속 새 형태로 나온다.**

파서 상한(배열 200개)에 걸려 자른 사실이 응답에 없었다 — 자식 710개인 Transform 이
200개로 보였고 문서 24개가 그랬다. `ScriptableObject` 의 `m_Name` 은 통째로 버려지고 있었다
(컴포넌트의 빈 `m_Name` 만 감추려던 것인데 구분을 안 했다). 중첩 시퀀스(`- - a`)는 문자열이
됐다. 셋 다 "틀린 값" 이 아니라 **없는 값**이라 눈에 띄지 않는다.
자를 때는 자른 사실을, 못 읽을 때는 못 읽은 줄을 반드시 함께 낸다.

**(18) 캐시 버전을 안 올리면 고친 것이 안 먹는다 — 또 그럴 뻔했다.**

지문은 **디스크 상태**만 본다. 코드가 바뀐 것은 못 잡는다. 엣지 규칙을 바꾼 커밋에서
버전을 안 올렸고, 그대로 뒀으면 기존 캐시가 옛 엣지를 계속 서빙했을 것이다(5 → 6).
`CACHE_VERSION` 주석에 이미 적혀 있던 함정을 그대로 밟았다.


**(19) "원리적으로 불가능" 이라고 적은 것도 다시 재보면 상당 부분 가능했다 (2026-08-24).**

경로 기반 로드(`Resources.Load`)를 "GUID 로 저장되지 않으므로 어떤 정적 인덱스도 못 본다" 고
문서에 적었다. 맞는 말이지만 **범위를 너무 넓게 잡았다.** 실제로 재보니 로드 호출 71개 중
인라인 리터럴은 1개뿐이고 나머지 70개는 경로를 `const string` 필드에 두고 이름으로 부른다.
같은 파일의 상수를 접는 40줄짜리 처리로 엣지 23개가 살아났다(보스 프리팹, 볼륨 프로파일, 셰이더).
남은 44건(런타임 조립)만이 진짜 불가능한 몫이다.

교훈: **"불가능" 은 측정 결과여야 한다.** 한계를 문서에 적을 때는 그 한계 안에서 실제로
얼마나 잃는지 세어 보고 적는다. 안 그러면 할 수 있는 것까지 안 하게 된다.

**(20) 새 능력을 새 도구로 내지 않았다.**

배선·타입이름·속성·경로로드 네 축을 붙이면서 **도구를 하나도 추가하지 않았다.**
기존 `find_callers` / `find_component_usages` / `get_type_symbols` 의 응답이 정확해졌을 뿐이다.
`tools/list` 고정비는 39,669 B 그대로다. 도구를 늘리면 AI 가 새 도구를 알아야 하고
세션 고정비가 늘지만, 응답을 고치면 **이미 쓰던 질문에 더 맞는 답이 온다.**
새 축을 붙일 때 먼저 물을 것: "이건 새 질문인가, 기존 질문의 빠진 답인가."

**(21) (20) 의 그림자 — 데이터를 넣고 도구의 0 을 안 고쳤다 (2026-08-25).**

(20) 을 자랑스럽게 적었는데, 정확히 그 방식의 실패가 같은 커밋에 남아 있었다.
레이어 D 가 타입이름 엣지 52개를 인덱스에 넣고 `get_type_symbols` /
`find_component_usages` 두 도구에 실었다. 그런데 **`find_references` 는 안 고쳤다.**
결과: `unity_find_references(BombAction.cs)` → `totalCount: 0`.
전수 스윕 결과 같은 형태가 **38건**이었다.

이게 나쁜 이유는 도구 이름 때문이다. "이거 지워도 되나" 는 `find_references` 로 묻는다.
게다가 그 0 일 때의 note 가 대조군으로 `unity_search_project` 를 권했고, Unity 를 켜서
재보니 **그쪽도 0** 이었다(에셋 24,233개 전수). 두 출처가 합창하는 오답 —
검증하려는 사람이 검증할수록 더 확신하게 되는 형태다.

교훈: **인덱스에 축을 추가하면 "0 을 답할 수 있는 도구" 전부를 훑어야 한다.**
데이터가 있는지가 아니라 **그 데이터가 실제로 어느 응답에 실리는지**가 정확도다.
(21) 은 (20) 의 반대가 아니라 조건이다 — 도구를 늘리지 않는 선택은 기존 도구를
빠짐없이 고칠 때만 정확도가 된다.

**(22) (21) 을 쓴 당일에 같은 실수를 새 도구에서 또 했다 (2026-08-26).**

P1 지도의 첫 구현에는 **타입이름 참조 축(48타입)이 어느 목록에도 없었다.** 부착 0 · 호출자 0
이라 `ranked` 밑으로 밀리고, `dataOnly`(부착>0 조건)에도 안 걸린다. 인덱스에는 있고
`find_references`·`get_type_symbols`·`find_component_usages` 세 도구도 답하는데,
**새로 만든 지도만 몰랐다.** 이 프로젝트가 가장 공들인 축이다.

잡아낸 것은 코드 리뷰가 아니다. **디스크에서 독립적으로 답을 아는 질문 7개**(구조 프로브)를
페이로드에 돌린 것이다. 리뷰로는 두 번 놓쳤고 프로브는 첫 실행에 잡았다.
`Tools/probe-project-map.js` 로 리포에 넣었다 — 기본 예산에서 6/7 미만이면 종료코드 1.

같은 세션에서 프로브가 잡은 나머지: 예산 회계가 고정 문구를 안 세어 2,000 예산이
2,379 토큰으로 나간 것, 섹션 상한 때문에 마지막 섹션이 0개가 된 것, 속성 그룹 한 행이
너무 커서 "MenuItem 70개" 라는 사실 자체가 응답에서 빠진 것.

교훈: **새 응답을 만들 때는 "이 응답으로 답할 수 있어야 하는 질문" 을 먼저 적어라.**
그 목록이 없으면 무엇이 빠졌는지 볼 방법이 없다 — 있는 것만 보이고 없는 것은 안 보인다.

**(23) 중심성이 "중요도" 라는 전제가 이 프로젝트에서 틀렸다 (2026-08-26).**

로드맵 P1 은 PageRank/중심성을 랭킹으로 쓰기로 했었다. 커밋 188건 정답지로 채점하니
중심성이 부착·churn 보다 낮았고(hit@50 28.2% vs 33.0%/33.5%), 상위 20은
컴파일러 생성 타입(`<>c` — 어셈블리 14개가 한 키로 병합된다), 벤더 오디오 라이브러리,
그리고 **`[Conditional("UNITY_EDITOR")]` 로그 래퍼**(`Edit`, 파일 단위 유입 49)로 채워졌다.
빌드에는 그 호출이 아예 없다.

거기에 두 가지 계측 오류가 겹쳐 있었다:
- 호출 in-degree 를 **타입** 단위로 세면 같은 파일 안 보조 타입끼리의 호출이 남는다.
  `TwentyThreeBoss.cs` 는 유입 엣지 233개 중 232개가 같은 파일이고 실제 유입 파일은 1개다.
- `symbols.js` 의 `NOISE_ATTRIBUTE_RE` 가 `System.Diagnostics.*` 를 버려
  `[Conditional]` 증거가 인덱스에 없었다 — 즉 **위 사실을 설명할 근거를 우리가 지우고 있었다.**

둘 다 독립 감사(Codex CLI)가 지적하고 내가 재현해 고쳤다. 교훈: 그래프 지표를 만들 때
**노드와 엣지의 정의를 먼저 재라.** "in-degree" 는 정의가 세 개고(메서드/타입/파일) 답이 다 다르다.

**(24) 프로브를 먼저 써도 경계 조건은 안 덮인다 (2026-08-26, P2).**

(22) 대로 P2 는 구현 전에 프로브 10개를 썼고 **첫 실행에 10/10** 이었다. 그런데 그 뒤
경계 조건을 손으로 훑자 결함이 셋 나왔다:

1. **없는 에셋에 "영향 0" 을 답했다.** 경로 입력은 존재 확인이 항상 참이 되는 로직 버그였다.
   프로브는 전부 "실재하는 대상" 이었으므로 이 축을 건드리지 않았다.
2. **모호한 타입 이름을 조용히 하나로 해석했다**(`Segment`, `PassData`).
   원인은 두 겹이었다 — 캐시가 중복 타입 123개를 잃어 모든 짧은 이름이 유일해 보였고,
   전체 이름이 `namespace.name` 이라 중첩 타입의 선언 타입이 빠져 서로 다른 클래스의
   `PassData` 셋이 한 이름으로 겹쳤다.
3. `depth > 1` 로 얻은 전이 호출자가 요약(`byAxis`)에 안 실렸다. 데이터는 응답에 있는데
   요약만 보는 쪽에는 0 으로 보인다 — (21) 의 축소판이다.

교훈: **프로브는 "내가 생각한 질문" 만 덮는다.** 프로브를 먼저 쓰는 것은 여전히 옳지만
(그게 없으면 축 누락을 못 본다), 그 뒤에 **경계 조건 목록**을 따로 훑어야 한다:
없는 대상 / 모호한 대상 / 빈 결과 / 상한 초과 / 요약과 본문의 불일치.

곁가지로 하나 더 배웠다. (2) 의 캐시 결함은 **어제 "현재 오답 경로 0건" 으로 미뤄둔 것**이다.
그 판단은 어제 기준으로 맞았고 오늘 두 번째 소비자가 생기면서 틀렸다.
"영향 0건" 으로 미룰 때는 **무엇이 생기면 영향이 생기는지**를 같이 적어야 한다.

**(25) 내가 §4-(20) 을 과잉적용했고, 독립 감사가 그걸 잡았다 (2026-08-27).**

P3 를 설계하며 나는 **A안**(기존 `unity_get_compilation_status` 의 응답을 브릿지가
가공한다)을 권했다. 근거는 (20) 이었다 — "새 질문인가, 기존 질문의 빠진 답인가".
컴파일 오류의 원인 지점은 그 도구가 원래 답했어야 할 것이라고 봤다.

사용자가 착수 전에 Codex CLI 와 의논하라고 지시했고, 감사가 셋을 지적했다.
전부 직접 재현했고 전부 맞았다:

1. **조인 키가 아예 없다.** `symbols.js` 의 `readPdbMethodDocuments` 는
   `MethodDebugInformation` 의 **0번 컬럼(Document)만** 읽는다. SequencePoints blob 은
   건드리지 않는다. 그래서 메서드->파일은 있어도 **줄번호->메서드가 없다.**
   P3 의 핵심 약속을 지금 데이터로 만들 수 없다.
2. **인덱스가 정확히 필요한 순간에 낡는다.** 컴파일이 실패하면 Unity 는
   `ScriptAssemblies` 를 갱신하지 않는다. 즉 인덱스는 직전 성공 빌드를 설명하고
   오류 줄번호는 새 소스 기준이다. 깨진 코드일수록 조인이 틀린다.
3. **(20) 의 적용 범위를 내가 넓혔다.** "컴파일 상태가 뭔가" 와 "이 오류들의 영향이 뭔가"
   는 **다른 질문**이다. 입력의 신선도 의미가 다르고 실패 양상이 다르다. (20) 은
   *같은 질문의 빠진 답*일 때만 유효하다.

그래서 P3 를 쪼갰다. **P3-a**(완결성·신선도·절단·출처)는 진짜 (20) 케이스라 도구 0개로
했고, **P3-b**(조인)는 새 질문이므로 만들더라도 별도 도구로 격리하기로 하고 보류했다.

교훈: **원칙은 적용 조건까지 같이 적어야 한다.** (20) 을 "도구를 늘리지 마라" 로 줄여
기억하면, 늘려야 할 때 기존 도구에 억지로 밀어넣게 된다. 그러면 실패가 격리되지 않는다 —
조인이 깨지면 멀쩡하던 상태 조회까지 같이 깨진다.

부수로 배운 것: **내 예측이 부분적으로 틀렸다.** "재컴파일하면 `warningCount` 가 0 에서
뛸 것" 이라고 했는데 안 뛰었다 — 그때 Editor.log 전체에 `warning CS` 가 0건이었다.
🔴 **그 결론("경고 없이 컴파일된다")은 2026-08-28 에 틀린 것으로 확인됐다** — 실제로
재컴파일하면 `warningCount: 3` 이다(CS0618 둘, CS0114 하나). 그때 본 것은 **컴파일이
일어나지 않은 상태**의 로그다. §4-(39)-2 참조. 대신 더 나쁜 것이 나왔다(§4-(26) 의 재료).
**예측을 세우고 실측으로 깨는 절차 자체가 이득이었다.**

**(26) (21) 을 읽은 직후 (21) 을 그대로 재현했다 (2026-08-27).**

`ensureIndex` 신선도 검증을 만들면서, `_asmSig` 를 **캐시 로드 경로에만** 기록하고
**신규 빌드 경로를 빠뜨렸다.** 그대로 뒀으면 캐시 없이 시작할 때 `_asmSig` 가 null 이라
매 호출 "낡음" 판정 -> 매 호출 전체 재빌드(5.7 초)에 빠진다.

프로브 6개가 전부 통과한 상태였다. **캐시 경로만 밟았기 때문이다** — 프로브 4 는
`_forceStaleForTest()` 로 낡음을 만드는데, 그 시점엔 이미 캐시에서 올라온 뒤다.
손으로 "캐시 없이 시작하면?" 을 물어서 잡았고, 프로브 7번으로 박아넣었다.

이게 (21)/(24) 와 다른 점은 **간격이 없다는 것**이다. (21) 은 하루 뒤, (22) 는 당일,
이번엔 그 문장을 읽고 **같은 함수를 고치는 중**에 저질렀다. 원칙을 아는 것과
그 순간에 적용하는 것은 별개다.

실효적인 대책은 하나뿐이었다: **상태를 쓰는 경로가 몇 개인지 먼저 세는 것.**
`grep -n "_index = " tools.js` 가 4곳을 냈고, 그중 둘만 고쳐져 있었다.
"이 값을 세팅하는 자리가 전부 어디인가" 를 grep 으로 세고 나서 고칠 것.


**(27) 프로브의 표본을 추측으로 고르면 통과해도 아무것도 보장하지 않는다 (2026-08-27).**

메서드 위치 프로브의 6번은 "선언이 하나뿐이면 오버로드 경고가 안 붙는다" 였고,
대상으로 `BaseAttack::TryResolveHit` 를 골랐다. **단일 선언이라고 가정했다.**
디스크로 확인하니 **오버로드 4개**였다(130/153/165/176행).

이번엔 프로브가 실패해서 잡혔다. 하지만 반대 방향이었으면 조용히 통과했을 것이다 —
"오버로드가 있으면 경고가 붙는다" 를 단일 선언 메서드로 검사했다면 통과할 수도 있었고,
그러면 계약이 검사된 적 없는 채로 초록불이 켜진다.

고친 방식: 대상을 손으로 고르지 않고 **인덱스에서 조건에 맞는 것을 찾아 쓴다**
(선언이 정확히 1개인 메서드를 순회로 찾는다).

교훈: **프로브의 단언은 데이터에서 유도한 성질에 걸어라.** 내가 안다고 믿는 표본에
걸면, 검사하는 것은 코드가 아니라 내 기억이다. (24) 가 "프로브는 내가 생각한 질문만
덮는다" 였다면 이건 그 안쪽이다 — **내가 생각한 질문조차 내가 틀린 전제 위에 있을 수 있다.**

곁가지 수확: 이 오답 덕분에 회귀 기준의 `BaseAttack::TryResolveHit -> 호출자 8개` 가
사실 **오버로드 4개에 걸친 합계**라는 것이 드러났다. 기준선 숫자 자체는 맞았지만
그 뜻을 우리가 잘못 알고 있었다.


**(28) 백로그의 전제가 두 건 연속으로 과장이었다 (2026-08-27).**

같은 세션에서 §3 의 항목 두 개를 집었는데 **둘 다 적힌 근거가 실제와 달랐다.**

- `unity_search_project` — "타임아웃에 걸리니 인덱스 기반으로 교체해야 한다" 였다.
  실제로는 타임아웃이 참조 검색의 고유 비용이 아니라 **`folder` 를 무시하던 버그**였다.
  `GetAllAssetPaths()` 24,233 의 정체는 Assets 3,143 + Packages 21,075 였고,
  folder 를 지키게 하니 31초 타임아웃이 즉시 완주로 바뀌었다.
  **교체할 이유가 사라졌다** — 교체를 먼저 했으면 버그를 그대로 둔 채 도구만 늘렸을 것이다.
- 중첩 타입 충돌 — "사용자 어셈블리 123개" 였다. 123은 **레코드 수**이고 고유 이름은 29개,
  그중 19개가 컴파일러 생성이다. 자기 코드의 충돌은 **둘**(`PassData`, `Segment`).
  이름 체계를 바꿔 호출 그래프 키를 흔들 규모가 전혀 아니었다.

두 경우 다 **먼저 재고 나서 설계가 뒤집혔다.** (23) 이 "그래프 지표를 만들 때 노드와 엣지의
정의를 먼저 재라" 였다면 이건 한 단계 위다 — **작업을 집을 때 그 작업이 존재하는 이유부터
다시 재라.** 백로그에 적힌 수치는 그것을 적을 당시의 이해이고, 그 이해가 틀렸을 수 있다.
특히 "N개" 라고 적힌 것은 **무엇을 N개 센 것인지**를 확인해야 한다.

**(29) 제시한 길이 막다른 길이면 거절만도 못하다 (2026-08-27).**

`unity_impact_analysis` 가 모호한 타입 이름을 거절하면서 "candidates 중 하나를 넣어라" 라고
답하게 만들었다. 그런데 **그 후보를 넣으면 "인덱스에 없다" 로 거절했다.**
`resolveTarget` 이 `/` 를 보면 무조건 에셋 경로로 판정하는 분기가 타입 분기보다 앞에 있었다.

프로브는 통과했다 — 5번이 "후보가 응답에 실리는가" 까지만 검사했기 때문이다.
**제시와 수용은 다른 코드 경로**라서 따로 검사해야 하는데, 프로브를 쓸 때 그 둘을 하나로 봤다.

교훈: **응답이 호출자에게 다음 행동을 지시하면, 그 행동을 프로브가 실제로 해봐야 한다.**
"무엇을 하라고 말했는가" 가 아니라 "말한 대로 하면 되는가" 를 검사한다.
(24)(27) 과 같은 계열이지만 축이 다르다 — 이건 **왕복**의 문제다.


**(30) 응답에 필드를 늘리는 것과 그 필드가 맞는 값을 담는 것은 다른 문제다 (2026-08-27).**

P3-b 를 만들며 `attachedAssetCount` 를 응답에 넣었는데, 읽는 맵 이름을 `scriptUsers` 로
썼다. **그런 맵은 없다** — 이 인덱스에서는 `scriptRefs` 다. 그래서 붙은 에셋이 몇 개든
**항상 0** 을 답했다. 프로브 4번은 통과했다. `type` 과 `callerCount` 만 검사했기 때문이다.

같은 세션에서 필드 타입을 디코딩해 인덱스에 넣었는데, `checkFields` 가 이름만 받아서
**값 검증 경로에는 도달하지 않았다.** 그 작업을 한 이유가 바로 값 검증이었다.

두 건이 같은 형태다 — (21) 은 "인덱스에 축을 추가하면 0 을 답할 수 있는 도구 전부를
훑어야 한다" 였는데, 이번엔 한 걸음 더 안쪽이다:

- **필드를 응답에 추가했으면 그 필드의 값을 프로브가 독립 출처와 대조해야 한다.**
  "필드가 있는가" 는 검사가 아니다. `attachedAssetCount` 는 존재했고 타입도 number 였다
- **데이터를 인덱스에 넣었으면 그것을 쓰기로 했던 소비자를 열어 확인해야 한다.**
  넣은 이유를 적어 뒀는데 그 이유가 실현됐는지는 아무도 안 봤다

실효적인 대책: 새 필드를 응답에 넣을 때 **그 값을 다른 경로로 한 번 더 구해 비교하는
프로브를 같이 쓴다.** 프로브 11번이 그 형태다 — 인덱스에서 직접 센 수와 응답의 수를 맞춘다.


**(31) 프로브가 구현과 같은 실수를 하면 검사가 안 된다 — 교차 검증만 잡는다 (2026-08-27).**

오버로드 키를 `Type::Method(인자)` 로 만들면서 키를 쪼갤 때 `indexOf('(')` 를 썼다.
**명시적 인터페이스 구현의 메서드 이름에는 괄호가 들어간다**(실측:
`IEnumerable<(System.Type,System.Type)>.GetEnumerator`). 그래서 이름 안의 괄호를 인자
목록 시작으로 읽었다.

프로브도 같은 `indexOf('(')` 를 썼다. **같은 실수를 공유하면 그 축은 검사되지 않는다.**
잡아낸 것은 프로브 4번이었는데, 그게 "시그니처 엣지의 이름 투영이 이름 그래프에도
있는가" 라는 **교차 검증**이었기 때문이다 — 한 축을 자기 논리로 검사하는 프로브는
자기 버그를 통과시키고, 두 출처를 맞추는 프로브만 그걸 잡는다.

교훈: **프로브 목록에 최소 하나는 "다른 경로로 구한 답과 맞춰보는" 항목을 넣어라.**
"형태가 맞는가", "필드가 있는가" 같은 자기 논리 검사만 모으면 통과율은 높고 보장은 없다.
이번 세션의 프로브 중 실제로 버그를 잡은 것은 전부 그 형태였다 —
필드 타입의 디스크 대조, 붙은 에셋 수의 인덱스 대조, 이 교차 검증.

곁가지: 첫 실행에서 프로브 4번이 **빈 맵으로 통과**했다(검사할 것이 없어서).
§4-(27) 의 퇴화 케이스가 이번 세션에서 네 번째다 — 프로브를 쓴 직후
"이 단언이 아무것도 없을 때도 참이 되는가" 를 물을 것.


**(32) 0 이 나오면 "없다" 가 아니라 "재는 방법이 틀렸다" 를 먼저 의심한다 (2026-08-28).**

§6 의 전수 집계를 다시 재는 스크립트를 쓰면서 **세 번 틀렸고 세 번 다 수치가 그럴듯했다.**

1. `splitDocuments` 는 text 를 받는데 lines 를 넘겼다 → **전 항목이 0.** 만약 "컴포넌트가
   0개" 를 그럴듯한 답으로 받아들였다면 거기서 끝났을 것이다. 0 은 답이 아니라 신호다
2. `withStaleKeys` 를 145 로 셌다 → 19 에서 145 면 회귀다. 파고들자 **도구가 아니라 내
   지표 정의가 틀렸다** — 확신(베이스 체인 완결)과 불확실(베이스가 패키지로 나감)을
   뭉갰다. 가르니 확신이 정확히 19. 도구는 처음부터 note 로 inconclusive 라고 말하고 있었다
3. "전체 커버리지" 를 두 번 다르게 해석했다 → 좁게 보면 패키지 컴포넌트가 "없는 스크립트"
   로 떨어지고, 넓게 보면 남의 패키지 에셋까지 훑어 28,319 가 된다. 답은 **GUID 해석은
   전체, 문서 스캔은 Assets** 였다

교훈 둘. 하나는 위 제목이다. 다른 하나는 **지표 이름이 정의를 담아야 한다** —
`withStaleKeys` 는 "확신하는 낡은 키" 인지 "낡았을 수도 있는 키" 인지 말하지 않는다.
그래서 §6 의 19 를 재현하려는 사람이 145 를 얻고 회귀라고 부르게 된다.
`staleKeysConfident` / `staleKeysInconclusive` 로 갈랐다.

그리고 이 일 전체의 교훈: **문서에 측정 방법이 적혀 있는데 그것을 수행하는 스크립트가
없으면, 그 수치는 회귀 기준이 아니라 옛 기록이다.** `_checkFields` 는 그 측정을 위해
export 됐는데 4일간 아무도 부르지 않았고, 독립 감사가 그것을 dead export 로 지목해서야
드러났다. 기준선을 적을 때는 **그것을 다시 재는 명령**을 같이 적어야 한다.


**(33) 인수 스위트가 캐시 없이는 8개 중 6개가 죽었다 (2026-08-28).**

세션을 넘겨받아 §0.5 의 "30초 안에 돌려볼 것" 을 그대로 붙여넣자 **6개가 FAIL** 이었다.
회귀를 의심하고 원인을 봤더니 회귀가 아니라 **부트스트랩 결함**이었다 —
프로브 6개가 `ensureIndex(..., cacheOnly=true)` 로 시작해서, 디스크 캐시가 없으면
`인덱스 캐시가 없다` 를 찍고 exit 2 한다. 아무것도 검사하지 못한 채 실패한다.
`probe-impact-analysis`/`probe-project-map` 둘만 스스로 빌드해서 통과했고, 그래서
"6 FAIL / 2 통과" 라는 **회귀처럼 보이는 모양**이 나왔다.

교훈 셋:
1. **인수 스위트는 스스로 서야 한다.** 전제(캐시)를 만들어 주지 않으면 다음 사람은
   실패 원인 조사에 시간을 쓴다. 캐시 없으면 한 번 빌드하고 그렇게 말하도록 고쳤다
2. **실패 메시지에 무엇을 하라는 말이 없었다.** "캐시가 없다" 는 진단이지 지시가 아니다
3. 문서에 명령을 적을 때는 **그 명령이 전제하는 상태까지** 적거나, 명령이 그 상태를
   만들게 해야 한다. §4-(32) 의 "기준선에는 재는 명령을 같이 적어라" 와 같은 형태다

**(34) 표본 40/40 통과가 전수 27.4% 실패를 가렸다 — 그리고 상태 하나로 두 가지를 표현했다 (2026-08-28).**

이번 세션 최대 수확이고, 형태가 §4-(21) 계열이 아니라 **새 형태**다.

`gap` 을 분류하려고 줄을 훑다가 `BuildWindowsPlayer.cs` 의 `return false;` 가 메서드
밖으로 나오는 것을 봤다. 그 파일에서 `Validate` 의 범위가 **54-73** 인데 실제 선언은
106, 본문은 108-127 이고, `BuildWindows64(52-55)` 와 **겹쳤다.** 같은 파일의 두 메서드
범위가 겹치면 둘 중 하나는 틀렸다 — 이것이 실측 가능한 불변식이다.

전수로 재니:

| 지표 | 고치기 전 | 고친 후 |
|---|---|---|
| 선언 대조(전수, 디스크) | **27.4% 불일치** (5,111/7,040) | **0%** (5,313/5,313) |
| 겹치는 범위 쌍 | **3,036** (123 파일) | **0** |
| 음수·역전 줄번호 | **126개 메서드** (`-8043` 같은 값) | **0** |

원인은 `readSequencePoints()` 가 `first` **한 변수로 두 가지**를 표현한 것이다:
ΔILOffset 이 절대값인 첫 레코드인가 / 줄·열이 절대값인가. 숨은 시퀀스 포인트
(ΔLines=0 ∧ ΔColumns=0)는 **줄·열 필드를 갖지 않는데** 그것을 만나면 `first=false` 로
내려서, 뒤따르는 첫 실제 포인트의 **절대 줄**을 부호있는 델타로 읽었다. 압축 정수는
부호 유무로 값이 달라진다(106 → 53). 그 오차 위에 이후 델타가 쌓이고, 부호있는 디코딩이
큰 값을 음수로 만들면 -8043 이 나온다.

그리고 부분 클래스에서 **메서드가 어느 파일인지** 잃고 있었다. PDB 는 메서드마다 문서를
갖는데 인덱스는 타입 단위로만 남겨서, `AudioSourceProxy.cs` 와 `.Methods.cs` 가 서로의
범위를 갖고 있었다.

교훈:
1. **표본 통과는 전수의 증거가 아니다.** 기존 프로브 3은 타입 3개에서 앞 20개를 봤고
   20/20 이었다. 문서의 "디스크 대조 표본 40/40" 도 같은 표본이다. 이 층은 **전수로 잴
   수 있었다**(대상 5,313개, 61 ms) — 잴 수 있는데 표본으로 재고 있었던 것이 문제다
2. **"둘 다 참일 수 없는" 불변식을 찾아라.** 겹침·음수는 소스를 안 읽어도 판정된다.
   버그를 드러낸 것은 디스크 대조가 아니라 그 두 가지였다
3. 규격 기반 디코더에서는 **상태 변수 하나가 두 뜻을 가지면** 반드시 어긋난다.
   규격의 조건절(숨은 포인트, document-record, 절대/델타)마다 상태를 따로 두라
4. 곁가지: 캐시 버전을 올려야 한다(12 → 13). 구 캐시는 틀린 범위를 담고 있다 — §4-(18)

**(35) 백로그가 적어 둔 "맞는 이름" 이 이미 다른 뜻으로 쓰이고 있었다 (2026-08-28).**

§3 에 "`totalReferenceCount` 는 실제로 (에셋, GUID) 쌍 수다. `affectedAssetCount` 가
맞는 이름" 이라고 4일 전 내가 적어 뒀다. 그대로 바꾸려고 응답을 열었더니
**`affectedAssetCount` 가 같은 응답에 이미 있었다** — 전역 중복 제거된 에셋 수(실측 134)
이고 쌍 수(164)와 **값이 다르다.** 그대로 실행했다면 두 지표가 한 이름에 뭉개졌다.

교훈: **백로그가 제안하는 이름·수치도 현재 응답과 대조한 뒤에 쓴다.** 백로그는 그때의
관측이고, 그 사이 응답이 자란다. §4-(28)("백로그의 전제부터 다시 재라")의 이름 버전이다.
바꾼 이름은 `assetGuidPairCount` / 행 단위 `referencingAssetCount` 다.
깨질 자리를 먼저 셌다 — 리터럴 5건 중 코드 소비자 0, 문서 4. 그래서 별칭 없이 교체했다.

**(36) 검사 규칙이 틀려서 맞는 데이터를 128건 틀렸다고 말했다 (2026-08-28).**

(34) 를 고친 뒤 전수 대조가 2.6% 불일치로 남았다. 파고들자 **전부 검사 쪽 문제**였다:
여러 줄에 걸친 시그니처(파라미터 4줄) · 선언 위 긴 주석 블록 · `op_Addition` 처럼 소스에
`operator +` 로 적히는 이름 · 로컬 함수를 품은 메서드(자기 첫 문장이 로컬 함수 뒤에 온다) ·
명시적 인터페이스 구현(`Ami.BroAudio.IAudioPlayer.get_AudioSource`).
"본문 시작 위 8줄에 이름이 있다" 는 규칙이 데이터가 아니라 **검사의 가정**이었다.

교훈 둘:
1. 불일치가 나오면 **데이터와 검사 중 무엇이 틀렸는지 먼저 가른다.** 여기서 실제 결함
   비율은 27.4% → 0% 였고, 남은 2.6% 는 전부 검사였다. 검사를 고치지 않으면 다음 사람이
   "0.3% 는 원래 실패한다" 를 정상으로 학습한다
2. **검사의 한계를 검사 안에 적는다.** 지금 프로브 10 은 40줄 안에 같은 이름의 *호출*이
   있으면 통과할 수 있다 — ±몇 줄 오차는 못 잡는다. 그 문장을 코드에 박아 뒀다.
   날카로운 것은 프로브 8(음수)과 9(겹침)이라는 것도 같이 적었다

**(37) 백로그 문구를 그대로 구현하면 값만 커지고 답은 안 나오는 항목이 있었다 (2026-08-28).**

"패키지 어셈블리 심볼 — `Library/ScriptAssemblies` 의 비-사용자 어셈블리도 읽으면 되는데
인덱스가 커진다. **타입 이름만 얕게 담는 별도 맵**이 현실적이다" 를 측정으로 검증하니,
그 문구가 말하는 맵(어셈블리 → 타입 이름)은 **null 을 하나도 메우지 못한다.** 조인 키가
`GUID → 타입` 이라서다.

그리고 왜 조인 키가 없어 보였는지가 드러났다 — **패키지 어셈블리의 PDB Document 경로는
상대 경로다.** 직접 재현했다: PDB 189개 중 첫 문서 경로가 절대인 것 **15개**, 상대인 것
**174개**, `Library/PackageCache/...` 를 가리키는 document **10,849개**. `toProjectRelative` 가
절대 경로를 전제하므로 이것들을 전부 null 로 버린다 — 그래서 (a) 그 어셈블리가
"비-사용자" 로 분류되고 (b) `.cs` 키가 없어 보였다. 키는 실재한다.

교훈: **"현실적이다" 라고 적어 둔 설계도 착수 전에 조인 키부터 확인한다.**
백로그 문구는 그때의 가정이고, 이 항목은 가정이 틀린 채 4일 남아 있었다.
측정 결과와 권고(ECID 승격을 먼저)는 §0.5 "남은 것" 에 옮겨 적었다.

**(38) 독립 검증이 내 수정에서 셋을 더 찾았고, 둘은 그날 내가 만든 것이었다 (2026-08-28).**

(34) 를 고친 뒤 Codex CLI 에 같은 두 자리를 독립으로 맡겼다(내 결론은 넘기지 않았다).
.NET 참조 구현으로 재디코딩한 결과 **5,960개 전부 line 일치**였다 — 수정은 확인됐다.
그런데 셋을 더 짚었다. 전부 직접 재현했고 셋 다 참이었다.

1. **`endLine` 이 "마지막 포인트의 시작 줄" 이었다.** 마지막 문장이 여러 줄이면 꼬리가
   범위 밖으로 떨어진다 — 133개 메서드, 차이 중앙 2줄 / **최대 22줄**. 참조 구현과
   **디코딩이 같다**는 것은 **필드의 뜻이 맞다**는 뜻이 아니다. 값의 정의를 따로 검증해야 한다
2. **부분 클래스 누출 두 경로.** `Document` 컬럼이 0 인 메서드(`.ctor` 5개)와, 전체 이름이
   겹쳐 한 레코드만 남은 타입(`<>c`, 자기 코드 `PassData`)의 람다가 남의 파일 뷰에 나왔다.
   (34) 에서 `fileIndex` 를 넣었지만 **그 값이 없는 경우를 안 정했다** — §4-(30) 의 형태다
3. **`lineKind` 를 본문 줄에도 붙였다.** 내 주석에는 "exact 아닌 줄만" 이라고 적었는데
   코드는 전부 분류했다. 그래서 본문의 `damage += 1;` 이 `field-declaration` 으로 나갔고
   정밀도가 **18.8%** 였다(예측 14,643 중 맞은 것 2,754, 오차의 거의 전부가 본문 줄 12,534).

가장 아픈 것은 3번의 **프로브가 왜 못 잡았나**다. 프로브 18·19 는 모집단을 "exact 아닌 줄"
로 **내 의도대로 걸러서** 세고 있었다. 의도와 코드가 다르면 그 필터가 차이를 감춘다.
교훈: **검사 모집단을 내 의도로 좁히지 말고, 도구가 실제로 답하는 전 범위에서 세라.**
지금은 프로브 21 이 **본문 줄만** 골라서 "그 줄에는 분류를 붙이지 않는다" 를 확인한다
(3,091줄 중 0). 그리고 프로브 20 이 파일 뷰 누출을 전수로 막는다(5,958개 중 0).

곁가지(도구 운용): `codex exec` 가 설치된 스킬 때문에 **계획 승인을 묻고 멈출 수 있다**.
비대화식에서는 "승인을 묻지 말고 끝까지 실행하라" 를 프롬프트에 명시해야 한다.
그리고 검증이 도는 동안 내가 같은 코드를 고치면 보고의 일부는 **수정 전 상태**를 본 것이다 —
받은 주장은 반드시 현재 HEAD 에서 다시 재현할 것.

**그리고 2차 검증이 그 수정에서 또 하나를 짚었다 — 내가 뭉툭하게 고쳤다.**
3번을 "`exact` 면 분류하지 않는다" 로 고쳤는데, **시퀀스 포인트는 필드 초기화자에도 붙는다**
(그 자리는 `.ctor`/`.cctor` 의 범위다). 그래서 진짜 필드 선언 **2,417줄**이 통째로
`in-method-body` 로 가려졌다(반례: `private static readonly string[] DevScenes =`).
생성자 범위에서는 분류를 돌리게 고쳤고 **1,187건**이 회수됐다. 같은 라운드에서 필드 판정에
선언 자리 요구를 걸었고(파라미터 이름이 필드로 잡히던 것), 여러 문서에 걸친 생성자의
두 번째 범위도 싣게 했다.

교훈(1): **"틀린 답을 없애는 가장 짧은 수정" 이 맞는 답까지 없앤다.** 정밀도(거짓을 줄였나)만
보고 재현율(참을 잃었나)을 안 보면, 지표는 좋아지고 답은 사라진다. 그 라운드에서 내가 본
수치는 "field-declaration 예측 2,037건 전부 본문 밖" 이었다 — **가려진 2,417건은 그 지표에
아예 나타나지 않는다.**

수정 후 같은 스크립트로 재측정한 값: `line` 불일치 0/5,960 · `endLine` 이 참조 구현의
`EndLine` 과 다름 **0/5,960** · 부분 클래스 오범위/누락/`fileIndex` 없음 **0/0/0** ·
`field-declaration` 정밀도 **93.8%**(1,645/1,753, 멤버 이름 오류 **0**).
⚠️ 그 스크립트의 `in-method-body` 행은 믿을 수 없다 — 검증 쪽이 그 규칙을 **자기가 다시
구현**해서 생성자 예외를 모른다(§4-(31) 의 형태가 검증자 쪽에서 난 것이다).


**(39) 같은 규칙을 한 소비자만 고친 형태가 또 나왔다 — 그리고 조건 없는 관측을 또 인용했다 (2026-08-28).**

Unity 를 켜서 `effectByOperation` 실검증(E4~E6)을 하다가 픽스처가 두 가지를 드러냈다.

1. **`unity_impact_analysis` 가 타입의 스크립트 에셋을 `sourceFiles[0]` 하나로만 찾았다.**
   메서드가 없거나 시퀀스 포인트가 없는 타입(필드만 있는 데이터 컴포넌트)은 PDB 문서가
   안 잡히므로 **`attachedAssets` 축이 통째로 빠진 채 impactedCount 0** 이 나왔다.
   실측: 사용자 타입 1,052 중 문서 없음 **307**, 그중 실제로 에셋에 붙은 것 **4개** —
   `TwentyThreeBasicAttackFigure` 는 에셋 3개에 붙어 있는데 "영향 0" 이었다.
   그리고 **`queries.js` 는 이미 파일명 폴백을 갖고 있었다**(`filename-fallback (type has no
   method bodies…)`). 즉 같은 데이터의 같은 함정을 **한 소비자만** 고친 상태로 나흘을 보냈다 —
   §4-(21)(22)(26)(30) 과 정확히 같은 형태이고, 이번엔 그 목록을 읽고 나서도 났다.
   대책은 그때 적은 것과 같다: **그 데이터를 소비하는 자리를 `grep` 으로 먼저 센다.**
   이번에는 `grep -rn "sourceFiles" Bridge/index/*.js` 가 6곳을 즉시 보여줬다.
2. **"이 프로젝트의 CS 경고 0건" 이 틀렸다.** 실제로 재컴파일하니 3건 나온다
   (CS0618 둘, CS0114 하나). 예전 기록은 **컴파일이 일어나지 않은 상태**의 Editor.log 를
   본 것이다 — 콜드 빌드 4.67 s vs 5.7 s 와 **같은 종류의 착오**(조건 없이 적은 관측).
   교훈은 §0.5 함정 표에 이미 있는 문장 그대로다: 무엇을 **어떤 조건에서** 쟀는지 적어라.
   조건 없는 관측은 다음 사람이 "회귀" 라고 부르거나 "없다" 로 오해한다.


**(40) 파견 감사가 문서에서 30건을 찾았고, 흘러간 것은 **전부** 재측정 명령이 없는 수치였다 (2026-08-28).**

이 세션에 코드를 여섯 번 고치고 문서를 세 번 갱신한 뒤, 별도 에이전트에게 **읽기 전용
모순 감사**를 맡겼다(문서 8개 + 코드 대조 + 커밋 해시 52개 검증). 결과의 모양이 교훈이다:

- **깨끗했던 것**: 프로브 개수(8·11·8·10·8·24·11·7) · 응답 필드 이름 전체
  (`assetGuidPairCount`·`lineKind`·`extraSpans`·`fileIndex`·`danglingLoads`·`scriptAssetBasis`) ·
  캐시 버전 16 · 커밋 해시 52개 · 파일 경로 25개. **전부 스크립트나 코드가 재확인해 주는 값**이다
- **흘러간 것**: 콜드 빌드(문서 6곳에 서로 다른 다섯 값, 조건 없음) · `fileIndex` 588 vs 583 ·
  `tools/list` 82 vs 85(§6 회귀 기준에 옛 값) · "커밋 4개"(실제 10) · 로컬 도구 12개인데 목록은 11개 ·
  버전 라벨 dev-0.0.6 vs dev-0.0.7 · 이미 철회한 주장의 잔존본 3건. **전부 산문이 들고 있던 값**이다

교훈: **재측정 명령이 붙은 수치만 산다.** §4-(32) 가 "기준선에는 다시 재는 명령을 같이
적어라" 였는데, 이번 감사는 그 규칙을 지킨 값과 안 지킨 값이 **정확히** 살아남은 값과
흘러간 값으로 갈렸다는 것을 보여준다. 그래서 이 패스에서 고친 방식도 그렇게 했다 —
개수는 숫자를 지우고 세는 명령(`git rev-list --count …`)을 적었고, 옛 값은 지우지 않고
"그때 값 / 현재 값 + 조건" 으로 나란히 뒀다(지우면 왜 달라졌는지가 사라진다).

부수 교훈: **철회한 주장은 철회를 적어도 원본에 표시가 없으면 계속 읽힌다.**
"경고 없이 컴파일된다"(§4-(25))는 세 곳에서 철회됐는데 원본이 긍정문으로 남아 있어,
그 절을 먼저 읽는 사람은 철회된 사실을 배웠다. 원본 자리에 🔴 를 붙였다.


**(41) 정밀도만 보고 재현율을 보지 않았다 — 세 라운드 연속 (2026-08-28).**

`lineKind` 의 `field-declaration` 을 세 번 고쳤고, 세 번 다 **"거짓을 줄였나" 만** 봤다.

| 라운드 | 내가 본 지표 | 실제로 벌어진 일 |
|---|---|---|
| 1차 | "본문 줄까지 분류해서 정밀도 18.8%" | 고침: 본문 줄을 빼기 → **필드 초기화자 2,417줄이 사라졌다** |
| 2차 | "예측 2,037건 전부 본문 밖" | 그 지표에는 **가려진 2,417건이 아예 안 나타난다** |
| 3차 | "정밀도 96.2%" | 독립 검증이 재현율을 재니 **70.5%** — 속성이 같은 줄인 필드 996줄이 가려져 있었다 |

지금은 **정밀도 98.9% / 재현율 95.3%** 다(같은 정답지). 즉 세 번 모두 고칠 수 있었고,
매번 놓친 이유는 하나다 — **내가 만든 지표가 한쪽 방향만 셌다.**

교훈:
1. **분류·판정을 만들면 두 방향을 같이 재라.** "틀린 것을 얼마나 줄였나"(정밀도)와
   "맞는 것을 얼마나 잃었나"(재현율). 하나만 좋아지는 수정은 대개 다른 쪽을 깎는다
2. 그 둘을 재려면 **독립 정답지**가 필요하다. 자기 규칙으로 세면 정의상 100% 다 —
   이번에 재현율을 잰 것은 내가 아니라 Roslyn 구문 트리였다(§4-(31) 의 형태)
3. 프로브에 넣을 단언도 그렇게 갈라야 한다: 25번은 거짓 양성(대입 우변)을, 26번은
   재현율(속성+필드 한 줄)을 각각 막는다


**(42) 죽은 분기를 프로브가 초록불로 덮었다 — 그리고 개선이 회귀처럼 보였다 (2026-08-28).**

`danglingLoadPaths` 축을 만들고 프로브를 이렇게 썼다: *"관계없는 에셋에는 이 축이 비어
있어야 한다"* → 통과. 그런데 그 축은 **한 번도 실행되지 않았다.** 에셋 대상의 경로 필드는
`target.assetPath` 인데 `target.path` 를 읽어서 분기가 통째로 죽어 있었다.
비어 있으니 "비어 있어야 한다" 는 단언은 **항상 참**이다 — §4-(27) 의 형태가 세 번째다.
독립 검증이 잡았고, 프로브를 **합성 입력**(존재하는 에셋과 파일명이 같은 dangling 항목을
인덱스에 끼운다)으로 바꿔 축이 실제로 답하는 것을 확인했다.

교훈: **"없어야 한다" 만 단언하는 프로브는 그 코드가 죽었는지 살았는지 구분하지 못한다.**
새 축을 만들면 반드시 **답이 나오는 입력**을 하나 만들어라. 이 프로젝트의 데이터에 그런
입력이 없으면(여기서는 옮겨진 에셋이 없었다) **합성해서라도** 넣는다.

같은 라운드의 곁가지: 상수-상수 경로를 접게 되자 참조 엣지가 6,305 → **6,306** 으로
늘어 **프로브 하나가 빨간불이 됐다.** 개선인데 회귀처럼 보였다. 기준선을 갱신하면서
"줄어들면 문제, 이 증가는 개선" 을 그 자리에 적었다 — 숫자만 적어 두면 다음 사람이
같은 판단을 다시 해야 한다.


**(43) 정리하려고 열었더니 도구가 말한 것보다 많았다 — 조인 키가 없는 실패는 조인이 못 본다 (2026-08-28).**

§0.5 "남은 것" 2번(Missing Script 정리)을 하려고 `DefaultVolumeProfile.asset` 을 열었다.
도구는 그 파일에 죽은 컴포넌트가 **4개**라고 했는데 실제로는 **9개**였다.
나머지 5개는 `m_Script: {fileID: 0}` — **GUID 가 아예 없는** 형태다.
`unity_find_missing_scripts` 는 `m_Script` 의 GUID 를 `.meta` 와 조인해서 찾으므로
**찾을 GUID 가 없는 실패를 원리적으로 못 본다.** 전수로 재니 프로젝트에 정확히 5건이고
전부 그 파일에 있었다(그래서 그동안 다른 자리에서 드러날 기회가 없었다).

교훈 셋:

1. **정리(파괴적 작업)의 첫 단계는 그 판단에 쓰는 도구를 의심하는 것이다.** "도구가 4개라고
   했으니 4개" 로 들어갔으면 5개를 남기고 "정리했다" 고 적었을 것이다. 지우기 전에 **원본을
   직접 훑어** 도구의 답과 대조했더니 갈렸다
2. **실패 형태마다 조인 키가 다르다.** GUID 가 죽은 것과 GUID 가 없는 것은 다른 실패이고,
   전자는 되살릴 키가 있고 후자는 없다(힌트는 `m_EditorClassIdentifier` 뿐). 한 이름으로
   묶으면 "Missing Script 14건" 이 깨진 컴포넌트의 전부인 것처럼 읽힌다.
   그래서 `scriptlessComponentCount` 를 **따로** 세고 왜 조인이 못 보는지를 note 에 적었다
3. **기준선이 줄어드는 것이 개선인 경우가 하루에 세 번 났다** — 상수-상수 경로를 접어
   참조 엣지 +1, 정리로 −4, 정리로 `missingScript` −4·`scriptComponents` −3.
   전부 프로브·스윕이 빨간불로 잡았고 전부 정상이었다. 기준선을 갱신할 때 **숫자만 바꾸지
   말고 "왜 줄었는지" 와 "여기서 더 줄면 도구 문제" 를 그 자리에 적었다.**
   숫자만 적으면 다음 사람이 같은 판단을 처음부터 다시 해야 한다

곁가지(둘 다 오늘 실제로 겪었다): **파괴적 편집 전에 Unity 를 꺼라** — 에디터가 그 서브에셋을
메모리에 들고 있어서, 디스크를 고친 뒤 Unity 가 먼저 쓰면 편집이 사라진다.
그리고 **한글을 `\uXXXX` 로 손으로 이스케이프하지 마라** — "지웠다" 가 "지우메" 로,
"참조" 가 "추적" 으로 조용히 바뀌어 문서에 들어갔다. 문법 오류가 아니라 **뜻이 바뀐다.**

**(44) "삭제된 것" 이 삭제된 적이 없었다 — 그리고 "참조 132" 는 컴포넌트 수가 아니었다 (2026-08-29).**

§0.5 "남은 것" 2번의 A(오클루전 3개 / 132 참조)를 판단하려고 근거를 모았다.
문서에는 두 세션 연속으로 **"의도된 삭제였나 를 사람이 결정할 것"** 이라고 적혀 있었다.
그 질문 자체가 틀렸다. `git log --all -S "OcclusionSection"` 이 답한 커밋은 **하나뿐이고
그것은 추가 커밋**이었다(`bb35c92`, 2026-08-09). 세 클래스는 **한 번도 지워진 적이 없다** —
`origin/feature/trensparent` 에만 있고 우리 브랜치의 조상이 아니다(`merge-base
--is-ancestor` = false). 프리팹만 `fix/pixel` 을 타고 넘어왔고 스크립트는 안 따라온 것이다.

교훈 셋:

1. **"왜 지웠나" 를 묻기 전에 "지워지긴 했나" 를 물어라.** 두 세션이 삭제를 전제로
   판단을 미뤘는데, 그 전제를 깨는 명령은 한 줄(`git log --all -S`)이었다.
   Missing Script 는 "코드가 사라진 흔적" 처럼 보이지만 **"코드가 아직 안 온 흔적"** 이기도 하다.
   둘은 처방이 정반대다 — 전자는 복원/정리, 후자는 머지이거나 **그냥 기다리는 것**이다
2. **수치의 단위를 확인하라 — 또 걸렸다.** 도구의 "132 참조" 는 `totalReferenceCount`,
   즉 **(에셋, GUID) 쌍** 수다(§3 에 이미 적혀 있던 정의다). 원본 YAML 을 직접 세니
   에셋 110개 / 쌍 132 / **실제 컴포넌트 인스턴스 560개**(OcclusionSection 515 · ElevationLevel 33 ·
   ElevationStack 12)였다. 쌍은 정확히 132 로 재현됐다 — **도구가 틀린 게 아니라 내가 다른 것을
   읽고 있었다.** 만약 "프리팹 정리" 를 택했다면 지울 대상은 132 가 아니라 560 이었다.
   §4-(43) 이 "도구가 못 보는 것" 이었다면 이번은 **"도구가 말한 것을 잘못 읽은 것"** 이다
3. **"영향 없음" 은 직접 참조만 봐서는 못 말한다.** CONTEXT.md 의 "정본 씬은 이 프리팹을
   쓰지 않는다" 를 그대로 믿지 않고 네 축으로 다시 쟀다: 씬 직접 참조 0 · **27개 씬 전부에서
   GUID 전이 BFS 도달 0** · Addressables 그룹 교집합 0 · 코드의 경로 로드 0.
   네 개가 다 0 이어야 "게임 경로 영향 없음" 이고, 하나만 봤으면 근거가 약했다

부수: 좁은 복원의 최소 폐포는 **4파일**이다(세 클래스 + `WallOcclusionRegistry`.
`using` 은 UnityEngine/System 뿐이라 닫힌다). 하지만 우리 브랜치에는 이 타입을 **쓰는 코드가
0건**이다(드라이버가 `fix/pixel` 계열). 그래서 복원해도 살아나는 것은 인스펙터의 오소링
데이터뿐이고 런타임 동작은 없다 — **"클래스를 복원하면 배선도 살아난다" 는 절반만 참이다.**

**(45) 백로그가 적어 둔 수는 맞았는데 **모집단이 절반**이었다. 그리고 "0 비용" 이 0 이 아니었다 (2026-08-29).**

§0.5-5 의 권고("ECID 승격을 먼저 하라")를 착수하면서 §4-(28) 대로 전제부터 다시 쟀다.
문서의 수는 하나도 안 틀렸다 — 붙은 컴포넌트 중 패키지 미해석·ECID 있음 **530**,
ECID 없음 **292**, 합 **822**. 셋 다 정확히 재현됐다.

**틀린 것은 모집단이었다.** 822 는 "패키지라 미해석" 만 센 수다. 스크립트가 **아예 없는**
컴포넌트 중 ECID 를 가진 것이 **560** 건 더 있었고, 그 560 이야말로 ECID 말고는 근거가
하나도 없는 자리다(패키지 쪽은 언젠가 심볼을 넣으면 검증할 수 있지만, 없는 스크립트는
영원히 못 한다). 승격의 실효는 **530 이 아니라 1,090** 이었다 — 두 배다.

그리고 그 560 은 §5 의 A군 오클루전 컴포넌트와 **정확히 같은 수**다(515+33+12).
다른 두 경로로 센 값이 맞아떨어진 것이라 둘 다 근거가 한 단계 세졌다.

교훈 넷:

1. **백로그의 수치를 검증할 때 값만 맞춰 보면 통과한다.** 530 도 292 도 822 도 맞았다.
   틀린 것은 "무엇을 세었나" 였다. §4-(28) 은 "전제를 다시 재라" 였는데, 이번 형태는
   **"같은 이름으로 다른 집합을 세고 있지 않은지"** 다. 갈래를 쪼개 보기 전에는 안 보인다
   (붙음여부 × 미해석사유 × ECID유무 로 쪼개니 즉시 드러났다)
2. **"0 비용" 은 축을 적지 않으면 거짓이 된다.** 문서의 "0 바이트·0 ms" 는 **캐시와
   콜드 빌드** 이야기였고 그건 참이었다. 하지만 응답 바이트는 0 이 아니었다 —
   처음 구현은 사유 설명문을 **행마다** 실어서 승격 필드가 응답의 **15.5%** 를 먹었다
   (Volume Profile 한 파일에 같은 문단 7번). 설명을 응답 수준으로 올리고 행에는 사유
   코드만 남겨 11.6% → 문구를 조여 **10.7%** 로 내렸다. 재기 전에는 몰랐다
3. **새 답을 만들었더니 그 답을 담을 자리가 없었다.** `displayName` 은 필터용으로
   계산만 되고 **응답에 실리지 않고 있었다.** 구현 전에 쓴 프로브가 그것을 잡았다 —
   "승격한 이름이 행의 이름이 된다" 를 단언하자 통과할 방법이 없었다. 프로브를 나중에
   썼으면 `script.typeNameFromAsset` 만 추가하고 "답 자리에 올렸다" 고 적었을 것이다
4. **능력을 늘렸으면 도구 설명도 늘려야 한다** (§4-(21)(30) 의 형태). 설명은
   "GUID 를 컴파일된 타입으로 해석한다" 까지만 말하고 있었다. 못 할 때 무엇을 하는지
   안 적으면 그 능력은 없는 것과 같다. 한 문장 추가에 **+288 B / 약 72 토큰/세션**이고,
   이건 재서 적은 값이다

5. **독립 검증이 여섯 건을 냈고 여섯 건 다 "이 프로젝트에는 아직 없는" 결함이었다.**
   프로브 15개가 전부 초록불인 상태에서 Codex 에 결론을 알려주지 않고 결함만 찾게 했다.
   나온 것: ⑴ `A::B::C` 를 마지막 `::` 로 쪼개 **없는 어셈블리/타입 짝**을 만든다 ·
   ⑵ `Asm::not a type` 처럼 타입 이름이 아닌 값도 타입 자리로 올린다 ·
   ⑶ `displayName` 만 필터에 쓰면 승격된 행이 `component: "MonoBehaviour"` 에서 **사라진다** ·
   ⑷ 사유를 `script.exists` 로 정해서, **Assets 안에 있는데 해석이 안 된** 스크립트를
   "패키지라서" 로 말한다 · ⑸ 도구 설명이 실제보다 좁은 사유만 약속한다 ·
   ⑹ "확인할 게 없다" 가 과한 말이다 — GUID 가 안 맞는 것과 그 이름의 타입이 안
   컴파일되는 것은 다르다(스크립트를 지웠다 다시 만들면 `.meta` GUID 만 바뀐다).

   전수로 재 보니 **여섯 다 지금은 발현되지 않는다**: ECID 1,726건이 전부 `::` 정확히
   1개인 표준형이고, `unresolvedInAssets` 는 0 이다. 그래서 넘길 수도 있었다.
   **넘기지 않은 이유는 ⑷ 때문이다** — `unresolvedInAssets` 가 0 이 아니게 되는 날은
   컴파일·빌드가 깨진 날이고, 하필 그날 도구가 "패키지 문제" 라고 엉뚱한 곳을 가리킨다.
   **잠복 결함은 가장 필요한 순간에 발현된다.**

   ⑹ 은 지적이 아니라 **새 능력**이 됐다: 승격할 때 그 이름의 타입이 인덱스에 있는지
   맵 조회 한 번으로 보고 `typeWithThisNameCompiles` 로 싣는다. true 면 처방이
   "복원" 이 아니라 **"컴포넌트에 스크립트를 다시 지정"** 이다. 오클루전 A군은 false 다
   (그래서 §5 의 판단이 그대로 선다). ⑶ 은 필터가 `displayName` 과 `className` 을
   **둘 다** 보게 고쳤다 — 넓어지는 쪽이라 숨기는 쪽보다 안전하다.
   여섯 건은 프로브 12b·15·16·17 로 고정했다.

부수: `component:` 필터는 예전부터 `displayName` **하나만** 봤다. 그래서 해석된 행은
원래 `component: "MonoBehaviour"` 에 안 걸렸고, 승격을 넣자 1,090 행이 같은 규칙을
따라 거기서 사라질 뻔했다 — 열거하려던 사람이 "없다" 를 답으로 받는 §4-(21) 형태다.
지금은 `displayName` 과 `className` 을 **둘 다** 본다. 이름으로도 걸리고
`MonoBehaviour` 로도 걸린다. 넓어지는 쪽이라 숨기는 쪽보다 안전하고,
프로브 12(자기 이름)와 12b(숨지 않음)가 양쪽을 고정한다.

**(46) 정문이 막혀 있는데 아무도 몰랐다 — 문서가 있는 우회로를 다들 쓰고 있었기 때문이다 (2026-08-30).**

"git 과 svn 만 받으면 다른 환경에서 되는가" 를 감사하다가, 이 패키지의 **첫 셋업 경로가
잘못된 것을 등록하고 있다**는 것을 찾았다. `Window/MCP Server` 의 **Copy Config to
Clipboard** 가 런처가 아니라 **브릿지를 직접** 가리키는 설정을 내고 있었다.

⚠️ **처음 진단의 절반은 틀렸다. 아래 2번을 먼저 읽을 것** — 그 오진 자체가 이 항목의
두 번째 교훈이다.

1. ✅ `AssetDatabase.FindAssets("mcp-bridge t:TextAsset")` — `.js` 는 `DefaultImporter` 로
   임포트돼 `DefaultAsset` 이 된다. **`t:TextAsset` 필터는 원리적으로 못 맞춘다.**
   `.meta` 를 보면 바로 나오는데 아무도 안 봤다. 라이브로 확인: **결과 0건.**
   그래서 **항상 폴백으로 넘어가고 있었다.**

2. ❌ **내가 틀린 것.** "폴백의 `Packages/com.community.unity-mcp/Bridge/mcp-bridge.js` 는
   Unity 의 가상 경로라 `File.Exists` 가 못 찾고, 그래서 설정에 `[BRIDGE_PATH]`
   플레이스홀더가 나간다" 고 적고 그대로 커밋했다. **아니다.**
   Unity 에디터 안에서는 그 경로가 **뚫린다** — 라이브 실측:

   ```
   CWD           : C:\Unity\MainProject
   File.Exists("Packages/com.community.unity-mcp/Bridge/mcp-bridge.js")  : True
   Path.GetFullPath(...)                                                 : C:\dev\unity-mcp\Bridge\mcp-bridge.js
   Directory.Exists("Packages/com.community.unity-mcp")                  : True
   ```

   **왜 틀렸나: Unity 를 끄고 잰 값으로 Unity 안에서 도는 코드를 판단했다.**
   셸에서 `ls Packages/` 를 하면 `manifest.json` 과 `packages-lock.json` 둘뿐이고,
   **Unity 가 켜져 있어도 그대로다**(정션도 심링크도 안 생긴다). 그런데 에디터 프로세스
   안에서는 `System.IO` 가 `Packages/<패키지>/...` 를 실제 위치로 리다이렉트한다.
   파일시스템 링크가 아니라 **인프로세스 가상화**라, 밖에서는 원리적으로 안 보인다.
   그래서 옛 코드는 플레이스홀더를 낸 게 아니라 **브릿지의 실경로**를 냈다.

   → 이 프로젝트의 `Packages/` 에 파일이 둘뿐이라는 관측 자체는 맞았다. **관측을 잘못
   해석했다.** §0.5 함정 표의 "조건을 안 적으면 다른 것을 잰다" 가 또 나온 것이고,
   이번 조건은 **Unity 가 켜져 있느냐**였다.

**그래서 진짜 결함은 무엇이었나.** 플레이스홀더가 아니라 **런처 대신 브릿지를 등록시키는
것**이다. 로컬 `file:` 로 쓰는 사람에게는 경로가 안정적이라 티가 안 난다. 하지만 git URL 로
받는 사람에게 그 경로는 `Library/PackageCache/com.community.unity-mcp@<해시>/Bridge/
mcp-bridge.js` 이고, **핀을 올리는 순간 폴더 이름이 바뀌어** 조용히 낡은 사본을 돌리거나
사라진 경로를 가리킨다. 런처가 존재하는 이유가 정확히 그것인데, 창은 런처를 고를 수
있는 코드가 아예 없었다.

**왜 안 걸렸나 — 첫 번째 교훈.** `LOCAL_DEV_SETUP.md` 가 "런처를 고정 경로로 등록하라" 는
**손으로 하는 절차**를 적어 두었고, 이 레포를 만지는 사람은 전부 그 길로 들어왔다.
정문(에디터 창)은 새로 오는 사람만 쓰는데, 새로 온 사람이 없었다.
**우회로를 문서화하면 정문이 고장 난 것을 아무도 보고하지 않는다.**
셋업 절차를 문서로 적을 때는 **UI 가 같은 답을 내는지도 같이 확인할 것.**

**두 번째 교훈.** 위 2번이다. **끄고 잰 값으로 켜고 도는 코드를 판단하지 말 것.**
이 레포는 이미 같은 형태로 세 번 데었다(콜드 빌드 시간 · CS 경고 0건 · 컴파일 실패 시
어셈블리 갱신). 이번엔 **파일 존재 여부**였다 — 가장 의심하지 않는 종류의 사실이라
더 잘 통과한다. 에디터 안에서 도는 코드의 경로 판정은 **에디터 안에서** 재라.

고친 것: `PackageInfo.resolvedPath` 로 실체 경로를 구하고 **런처를 우선**한다(런처는 실행
시점에 브릿지를 다시 찾으므로 핀이 바뀌어도 안 깨진다). 런처가 없는 옛 핀이면 브릿지를
주되 "핀을 올리면 깨진다" 고 경고한다. 그리고 **Resolved Package** 줄로 지금 실제로 도는
패키지의 출처(로컬 경로 / git 해시)를 보여준다 — `manifest.json` 에 `skip-worktree` 가
걸려 있으면 커밋된 핀과 도는 것이 다를 수 있고 `git status` 에는 안 나오기 때문이다.

**런타임 검증 완료 (2026-08-30, Unity 6000.3.16f1 라이브).** 진짜 창 인스턴스의 private
필드를 리플렉션으로 읽었다(로직을 다시 구현해 재현하면 그 사본이 통과한 것일 뿐이다):

```
_bridgePath    : C:/dev/unity-mcp/Bridge/mcp-bridge-launcher.js
_usingLauncher : True
_packageSource : Local  C:/dev/unity-mcp
런처를 가리키나 : True     [BRIDGE_PATH] 남나 : False
PackageInfo    : Local / 2.3.0-dev.0.0.7   resolvedPath : C:\dev\unity-mcp
```

⚠️ **git URL 로 받은 경우는 아직 실검증 안 했다.** 이 머신은 로컬 `file:` 참조를 쓴다.
핀으로 받는 환경에서 `resolvedPath` 가 `Library/PackageCache/...@해시` 를 주는지는
다음에 그런 환경이 생기면 볼 것.

곁가지 셋:

- **독립 감사의 수치를 재현했더니 달랐다.** 감사는 "git 추적 에셋 350개가 SVN 소유 GUID
  232종을 22,918번 참조" 라고 했고, 같은 질문을 직접 재니 **392 / 265 / 23,097** 이었다.
  방향과 자릿수는 같고 세는 방법이 달랐다(감사의 `files=2048` 은 `.meta` 포함, 내 943 은
  제외 — 943+1,105=2,048 로 맞는다). §4-(44)(45) 와 같은 형태가 세 번째다:
  **주장은 재현하되 수치는 내가 잰 것을 적는다.**
- **`/Tools/` 가 gitignore 였다.** 게임 레포에 검사 스크립트를 넣으려다 `git check-ignore`
  로 걸렀다 — `.gitignore` 132행이 `/Tools/` 를 "로컬 AI 에이전트 협업 파일 · 커밋 금지" 로
  제외한다. 그냥 `git add` 했으면 **조용히 아무것도 안 들어갔을 것**이다
  (`git add` 는 무시된 경로에 경고를 안 낸다). **새 위치에 파일을 만들면 커밋 전에
  `git check-ignore -v` 를 돌릴 것.**
- **잠복 결함이 또 나왔다.** 감사가 낸 6건 중 이 창 건을 뺀 나머지도 전부 "지금은 안
  터지는" 것이었다(§4-(45) 의 독립 검증과 같은 양상). 그중 아트 리비전이 git 에 안
  묶여 있다는 것은 **터지면 같은 커밋에서 다른 화면이 나오는** 형태라, 게임 레포에
  핀 파일과 검사 스크립트를 넣었다.

**(47) 답을 만들어 놓고, 그 질문을 하는 도구는 안 고쳤다 (2026-08-31).**

전날 ECID 승격을 넣어 `unity_get_asset_components` 가 미해석 컴포넌트의 이름을 답하게
했다. 그런데 **"무엇이 없어졌나" 를 묻는 도구는 `unity_find_missing_scripts` 다.**
그쪽은 그대로 GUID 만 답하고 있었고, note 로 이렇게 떠넘기고 있었다 —
"힌트는 `m_EditorClassIdentifier` 뿐이고, 그건 `unity_get_asset_components` 가 보여준다."
GUID 열 개를 받아 하나씩 다른 도구로 옮겨 물어야 답이 나오면, 그 답은 없는 것과 비슷하다.

§4-(21) 과 같은 형태다(데이터를 넣고 소비자를 안 고쳤다). 다른 점은 이번엔 **소비자가
자기가 못 한다는 것을 문장으로 적고 있었다**는 것이다. 그 문장이 바로 신호였는데,
승격을 만들면서 그 note 를 다시 읽지 않았다.
**새 능력을 넣었으면 "이 답을 원래 누가 냈어야 하나" 를 묻고 그 도구의 note 를 읽을 것.**

고친 뒤: missing 10건 중 3건이 이름으로 나온다(오클루전 3종). 나머지 7건은 ECID 가 없어
**이름을 지어내지 않는다.** `evidenceAsset` 으로 어느 에셋에서 읽었는지도 같이 준다.

**비용을 어떻게 낮췄나 — 순진한 구현은 49배 느렸다.** 처음엔 "첫 참조 에셋을 통째로
파싱" 이었고 **544 ms / 34.45 MB** 였다. 두 가지를 고쳐 **11 ms / 2.12 MB** 가 됐다(답은 동일):

1. 참조 에셋 중 **가장 작은 것**을 고른다 — 같은 답인데 읽는 양이 준다
2. GUID 가 실제로 들어 있는 **그 문서만** 파싱한다(줄 범위 텍스트 검사로 먼저 거른다)

프로브 21이 이 예산(300 ms)을 지킨다. 깨지면 "전 문서를 파싱" 으로 되돌아간 것이다.

곁가지 — 검증 오라클을 레포로 들이면서 밟은 것 둘. **둘 다 조용히 틀리는 형태다:**

- **"설치된 Unity 중 최신" 으로 고르면 안 된다.** 오프라인 컴파일용 rsp 생성기를 처음
  그렇게 썼더니 프로젝트가 쓰는 `6000.3.16f1` 대신 옆에 깔린 `6000.4.8f1` 을 잡았다
  (참조 DLL 158 → 175). **컴파일은 통과한다** — 다른 Unity 로 컴파일했다는 것만 안 보인다.
  지금은 `ProjectVersion.txt` 를 읽고, 그 버전이 없으면 거절한다.
  같은 함정의 일반형: **"적당히 하나 고르기" 는 고른 것이 틀렸을 때 조용하다.**
- **Git Bash 경로를 csc 에 그대로 주면 안 된다.** `/c/dev/...` 를 csc 가 `C:\c/dev/...` 로
  읽어 소스 30개가 전부 `CS2001` 로 죽었다. 이건 시끄럽게 실패해서 금방 잡혔다 —
  §0.5 에 이미 적혀 있던 함정인데(`cygpath -m`) 생성기를 쓰면서 잊었다.
  **문서에 적힌 함정은 그 함정을 자동화할 때 다시 읽을 것.**

**(48) 정리를 하면서 세 번 틀렸다 — 그리고 세 번 다 "숫자가 이상하다" 가 잡아냈다 (2026-08-31).**

B군 정리(§5)는 실제 편집보다 **재는 데서 더 많이 틀렸다.** 셋 다 눈으로는 안 보이고
수치가 안 맞아서 잡혔다.

1. **내 집계가 도구보다 17배 컸다.** B군 규모를 도구와 별개로 세려고 `.meta` 를 훑어
   "존재하는 GUID" 집합을 만들었는데 **`Assets` 만** 훑었다. 그러자 `Library/PackageCache`
   에 사는 GUID 가 전부 "없는 스크립트" 로 떨어져 **missing 171건**이 나왔다(도구는 10).
   §0.5 함정 표에 이미 있는 항목이고 2026-08-28 에도 같은 착각을 했다.
   **교훈: 대조군을 만들 때 모집단을 도구와 맞췄는지부터 확인하라.** 다행히 171 이라는
   수가 터무니없어서 바로 걸렸다 — **틀린 수가 그럴듯했으면 그대로 갔을 것이다.**

2. **마지막 문서를 지우면서 파일 끝 개행을 잃었다.** 22개 파일 전부가
   `\ No newline at end of file` 이 됐다. 원인은 단순하다: `"a\r\nb\r\n".split("\r\n")` 의
   마지막 빈 원소가 끝 개행을 나타내는데, 마지막 문서의 줄 범위가 **그 원소까지 삼켰다.**
   `git diff --numstat` 이 `+4 -246` 으로 나온 것이 신호였다 — **삭제만 있어야 하는
   작업에 추가가 4줄** 있었다. 되돌리고 고쳐 `0 추가 / 242 삭제`로 다시 했다.
   **교훈: 삭제만 하는 작업은 diff 에 추가가 0 이어야 한다. 그 등식을 검사로 쓸 것.**

3. **정리를 회귀로 두 번 오독했다.** `probe-overloads` 가 참조 엣지 **7,392**(기준 6,302)를
   보고 빨간불을 켰다. 정리로 줄어야 하는데 늘었으니 큰일인 줄 알았는데,
   **내가 직전에 전체 커버리지로 강제 재빌드해 그 캐시를 디스크에 남긴 것**이었다.
   기본 커버리지로 다시 재니 6,275(−27)로 예상대로였다.
   이 함정은 순서 의존이라 재현이 어렵다 — `sweep-field-checks` 와
   `probe-ecid-promotion` 은 전체 커버리지로 빌드하고 그 캐시를 남기므로,
   **그 뒤에 앞쪽 프로브를 다시 돌리면 거짓 실패가 난다.**
   그래서 프로브를 고쳤다: `guidCoverage` 가 `assets` 가 아니면 그 항목을 **재지 않고
   건너뛴다고 말한다.** 조건이 다르면 침묵하는 것보다 "안 쟀다" 고 말하는 것이 낫다.

곁가지 — **`.asset` 정리와 프리팹 정리는 다른 작업이다.** 2026-08-28 의
`DefaultVolumeProfile.asset` 은 `components:` 목록이 죽은 것을 **참조하지 않아서**
문서만 지우면 됐다. 프리팹은 반대다: 컴포넌트가 GameObject 의 `m_Component` 에
**반드시 실려 있다.** 문서만 지우면 dangling 이 된다. 전례를 그대로 적용했으면 22개
프리팹에 깨진 참조를 심었을 것이다. **"저번에 이렇게 했다" 를 쓰기 전에 이번 대상이
같은 구조인지 볼 것.**

---

## 5. 부수 발견 — 실제 프로젝트 문제

### 가리키는 것이 없는 로드 경로 2건 (2026-08-28 신규)

`unity_index_status` 의 `danglingLoads` 로 나온다. 컴파일러도 콘솔도 침묵하고,
런타임에 그 호출이 그냥 `null` 을 돌려준다.

| 부르는 파일 | 없는 경로 |
|---|---|
| `Assets/1.Scripts/Effects/Editor/EffectSystemSetup.cs:30` | `Assets/5.VFX/Common/FX_Hit_Spark.prefab` |
| `Assets/1.Scripts/Effects/Editor/EffectSystemSetup.cs:31` | `Assets/5.VFX/Common/FX_Hit_Blunt Variant.prefab` |

독립 확인: `Assets/5.VFX/Common/` 폴더가 **아예 없고** `find` 로 후보 0건.
그 파일은 그 프리팹들로 `EffectEntry` 를 만드는 에디터 오서링 스크립트다 —
이름이 바뀌었는지, 폴더가 옮겨졌는지, 기능이 죽은 것인지는 **사람이 알아야 한다.**

### 2026-08-28 — 정리 1건 실행: `DefaultVolumeProfile.asset` 의 고아 컴포넌트 9개

**무엇을 지웠나.** 그 파일의 YAML 문서 29개 중 **9개**를 지웠다(20개 남았다 = 프로필 본체 1 +
살아있는 컴포넌트 19). 지운 것은 **전부 고아**였다 — 프로필의 `components:` 목록이 참조하지
않고, 프로젝트의 어떤 에셋도 그 fileID 를 참조하지 않는다.

| 무엇 | 수 | 이름 |
|---|---|---|
| 죽은 GUID | 4 | `OutlineVolumeComponent` · `TestAnimationCurveVolumeComponent` · `OasisFogVolumeComponent` · `TestVolume` |
| `m_Script: {fileID: 0}` | 5 | `CopyPasteTestComponent1~3` · `VolumeComponentSupportedEverywhere` · `VolumeComponentSupportedOnAnySRP` |

**왜 있었나 — 우리 프로젝트가 만든 것이 아니다.** URP 17.3.0 패키지가 **템플릿 자체에**
그 9개를 담아 배포한다: `Library/PackageCache/com.unity.render-pipelines.universal@…/Editor/Volume/DefaultVolumeProfile.asset`
에 같은 fileID·같은 `m_EditorClassIdentifier`(`Unity.RenderPipelines.Core.Editor.Tests:…`)로
byte 단위로 같은 것이 들어 있다. 그 템플릿은
`URPDefaultVolumeProfileSetting.cs` 의 `AssetDatabase.CopyAsset` 으로 **파일째 복사**되므로
서브에셋 쓰레기까지 따라온다. 이 프로젝트의 최초 커밋(`97c7b5a`)부터 있었다.
**⚠️ Graphics Settings → URP Default Volume Profile → Reset 을 누르면 9개가 다시 들어온다.**

**절차와 검증**(전부 실측):
1. **Unity 를 껐다** — 에디터가 그 서브에셋들을 메모리에 들고 있으므로, 디스크 편집 후
   Unity 가 먼저 쓰면 편집이 사라진다. 이것이 이 작업의 유일한 실질 위험이었다
2. 문서 9개만 제거(`components:` 목록·프로필 본체·`.meta` 는 **건드리지 않았다**).
   CRLF 보존 — `.gitattributes` 가 `*.asset text eol=lf` 인데 워킹트리는 CRLF 다
3. 정적 검증: `git diff --numstat` **0 추가 / 188 삭제**, 25,021 -> 19,577 B,
   문서 29 -> 20, `components` 19개 그대로, 목록의 fileID 전부 문서 있음, 남은 고아 0
4. Unity 재기동 -> 임포트 오류·경고 **0**, 파일 해시 불변(Unity 가 재직렬화하지 않았다),
   `UniversalRenderPipelineGlobalSettings.asset` 의 프로필 참조 유지
5. 인덱스: missing **14 -> 10**, 영향 에셋 **134 -> 133**, 쌍 **164 -> 160**,
   `scriptlessComponentCount` **5 -> 0**, 그 파일의 컴포넌트 문서 **29 -> 20**
6. `sweep-field-checks` 기준선 둘을 내렸다(scriptComponents 7019->7016, missingScript 601->597) —
   **감소가 개선인 유일한 경우**라 그 이유를 그 자리에 적었다

**되돌리기**: `git -C C:/Unity/MainProject checkout -- Assets/99.Settings/DefaultVolumeProfile.asset`
하나로 완전 복구된다(GUID·`.meta` 불변, `Library/` 는 gitignore).
**게임 레포에는 커밋하지 않았다** — 워킹트리 변경으로 남겨 뒀다(팀 레포 `feature/Boss23`).

**독립 검증 둘.** Codex 가 문서 29개를 전수 분류해 같은 9개를 짚고 "목록은 건드리지 말 것"을
경고했다(목록에 죽은 fileID 가 하나도 없어서, 목록을 함께 편집하면 **살아있는 컴포넌트를
잃는다**). 파견 에이전트가 URP 템플릿 오염이라는 출처와 "Unity 를 먼저 끄라"를 찾았다.

**아직 남은 것**(이번에 손대지 않았다):
- **A. 자기 코드 3개 / 132 참조** — `VeyTrace.Rendering.Occlusion.{OcclusionSection,ElevationLevel,ElevationStack}`.
  필드값이 살아 있어 클래스를 복원하면 배선도 살아난다. **지우면 그 오소링이 사라진다** —
  사람이 "의도된 삭제였나" 를 결정해야 한다
- **B. 서드파티·테스트 잔여물 7개 / 28 참조** — `50.Art` 의 포탈 회전/오실레이터, HDRP 플러그인
  메타가 남은 `.mat` 5건, MapGen 메시 캐시, INab 데모. 재임포트로 되돌아올 수 있는 자리다

### 2026-08-31 — 정리 2건 실행: B군 서드파티·아트 잔여물 (문서 28개)

**사용자 판단**: 단독 작업이고 서드파티·아트 잔여물이므로 지우는 것이 맞다.

**무엇을 지웠나.** 죽은 스크립트 GUID 6종을 가리키는 MonoBehaviour 문서 **28개**와,
그 fileID 를 가리키던 `m_Component` 항목 **18개**. 22개 파일.

| 소유 | 파일 | 문서 | 무엇 |
|---|---|---|---|
| SVN (`50.Art`) | 6 프리팹 | 12 + 5 | 포탈·존의 회전 스크립트와 오실레이터 |
| SVN (`50.Art`) | 3 `.asset` | 3 | MapGen 메시 캐시(`ConvexMeshes`). **`Mesh` 본체는 그대로** |
| SVN (`50.Art`) | 1 프리팹 | 1 | KMK 테스트 로봇 |
| git | 5 `.mat` | 5 | INab 데모 머티리얼의 HDRP 플러그인 메타(이 프로젝트는 URP) |
| git | 1 `.asset` | 2 | INab `URP Renderer` 의 고아 SSAO |

**건너뛴 1건**: `INab Studio/…/Character Fire.vfx` 의 컴포넌트 하나.
**VFX 그래프의 노드 목록이 그 fileID 를 참조한다** — 문서만 지우면 dangling 이 되어
지금보다 나빠진다. 스크립트가 자동으로 걸러 냈다(§4-(48)).

**절차와 검증**(전부 실측):
1. **Unity 를 껐다.** 파괴적 편집의 전제다
2. `.asset` 정리(2026-08-28)와 **다른 점**: 프리팹은 컴포넌트가 GameObject 의
   `m_Component` 목록에 **실려 있다.** 문서만 지우면 dangling 참조가 남는다.
   문서와 목록 항목을 **함께** 지웠다
3. 지우기 전에 전수 확인: 그 fileID 가 문서 헤더·`m_Component` 항목 **말고 다른 곳**에서
   참조되면 그 파일은 건너뛴다. 그래서 `.vfx` 가 걸러졌다
4. 지운 뒤 전수 확인: 편집한 23파일에 로컬 dangling 참조 **0건**
5. git diff **0 추가 / 242 삭제**. 줄끝(CRLF/LF)과 **파일 끝 개행** 보존
6. 인덱스: missing **10 -> 4**, 영향 에셋 **133 -> 111**, 쌍 **160 -> 133**,
   참조 엣지 **6,302 -> 6,275**(−27: 쌍 기준이고 `URP Renderer` 가 같은 GUID 를 2번 가졌다),
   `scriptComponents` **7,016 -> 6,988**(−28), `missingScript` **597 -> 569**(−28)
7. `attached|missing|noecid` 갈래가 **18 -> 0** 이 됐다. `unresolvedWithEcid` 1,213 은 불변
   (지운 것은 전부 ECID 가 없던 것이다)

**되돌리기**: git 쪽 `git checkout -- "Assets/INab Studio"` / SVN 쪽
`svn revert -R C:/svn/GA7thFinal_VeyTrace`. 둘 다 한 줄이다.

⚠️ **재임포트로 되돌아온다.** INab 패키지를 다시 받거나 아트가 그 프리팹을 다시 내보내면
같은 고아가 생긴다. 그때 다시 지우면 된다 — 되돌아왔다고 도구 문제로 보지 말 것.

**남은 missing 4건**: 오클루전 A군 3(현상 유지로 종결) + 위 `.vfx` 1.

### Missing Script — A군(오클루전 3개). **2026-08-29 종결: 현상 유지**

`unity_find_missing_scripts` 가 찾은 상위 3건. **결론부터: 삭제된 적이 없다. 아직 안 온 것이다.**

| 도구가 말한 것 | 실제 |
|---|---|
| `VeyTrace.Rendering.Occlusion.OcclusionSection` 108 | 에셋 108 / **컴포넌트 515** |
| `VeyTrace.Rendering.Occlusion.ElevationLevel` 12 | 에셋 12 / **컴포넌트 33** |
| `VeyTrace.Rendering.Occlusion.ElevationStack` 12 | 에셋 12 / **컴포넌트 12** |
| 합계 "132 참조" | 프리팹 **110** / (에셋,GUID) 쌍 **132** / **컴포넌트 인스턴스 560** |

"132" 는 `totalReferenceCount` = **(에셋, GUID) 쌍** 이다. 원본 YAML 직접 집계로 쌍은 132 로
정확히 재현됐다 — 도구가 틀린 게 아니라 **단위가 다르다.** 정리를 택했다면 대상은 560 이었다.
전부 `Assets/2.Prefabs/Map/LevelDeliveryV3/` 아래다(Dependencies 98 · Zones 11 · Stage 1).

⚠️ 이 문서와 게임 레포 `CONTEXT.md` 가 예전에 적던 **"프리팹 125개"/"124개" 는 근거가 없는 수다.**
110 은 두 독립 방법으로 맞췄다(python YAML 집계 · `grep -rl` 합집합, 둘 다 110).
GUID별 108/12/12 는 도구가 답한 수와 정확히 일치한다 — 틀렸던 것은 프리팹 총수뿐이다.

**출처 (2026-08-29 확정)**

- 세 클래스는 `bb35c92` "feat: redesign registered wall occlusion"(2026-08-09, noisyboy632)
  에서 **추가**됐고, 그 커밋은 `origin/feature/trensparent` **에만** 있다.
  `git merge-base --is-ancestor bb35c92 HEAD` = **false**. 삭제 커밋은 전 브랜치에 **없다**
- `.meta` GUID 3개가 프리팹이 부르는 GUID와 **정확히 일치**한다
  (`010b78ca…5804` · `5caf0a47…1807` · `90be40df…965d`) — 머지하면 배선이 그대로 붙는다
- 프리팹은 `fix/pixel` 을 타고 넘어왔는데 **스크립트가 안 따라왔다**

**게임 경로 영향 = 없음. 네 축으로 쟀다**

| 축 | 결과 |
|---|---|
| 씬이 직접 참조 | **0** (27개 씬) |
| 씬에서 GUID 전이 BFS 도달 | **0** (27개 씬 전부, 도달 에셋 1~273개를 훑고도 교집합 0) |
| Addressables 그룹 | **0** (`Default Local Group` · `unifiedraytracing` 둘 다 교집합 0) |
| 코드의 경로 로드 | **0** (`grep -rn LevelDeliveryV3 Assets/1.Scripts Assets/Tests` 무응답) |

재는 스크립트는 `Tools/scan/occlusion-reach.py` 에 남겼다.

**판단 (사용자 결정, 2026-08-29): 현상 유지. 코드도 프리팹도 건드리지 않는다.**

근거:

- 좁은 복원(4파일: 세 클래스 + `WallOcclusionRegistry`. `using` 이 UnityEngine/System 뿐이라
  컴파일은 닫힌다)을 해도 **우리 브랜치에는 이 타입을 쓰는 코드가 0건**이다. 드라이버·오소링이
  `fix/pixel` 계열이라 살아나는 것은 인스펙터 데이터뿐이고 런타임 동작은 없다.
  즉 **죽은 코드를 넣는 대가로 콘솔 경고를 지우는 것**이고, 나중에 trensparent 를 제대로
  머지할 때 충돌만 늘린다
- 프리팹 정리(560개 제거)는 **되돌릴 수 없고**, trensparent 리디자인이 전제하는 오소링 기록
  (`renderers`/`colliders`/`contentRoot`/`contentRenderers` 배열)을 없앤다
- trensparent 전체 머지는 충돌 12건 + `WallOcclusion*` 이 우리 쪽이 더 최신이라 역행 위험
  (CONTEXT.md 2026-08-15 조사). 별도 세션 분량이다

**따라서 이 항목은 "정리 대상" 이 아니라 "미머지 브랜치의 알려진 부작용" 으로 재분류한다.**
`missingScript` 기준선 10 중 3개는 여기서 더 줄지 않는 것이 정상이다 — 줄었다면
trensparent 가 머지됐거나 누군가 프리팹을 정리한 것이니 **먼저 원인을 물을 것**.

### 2026-08-27 전수 분류 — 14개 GUID / 164 참조

지금까지 상위 3건만 정리돼 있었다. 나머지 11개(32 참조)를 처음으로 분류했다.
근거는 원본 YAML 에서 뽑은 `m_EditorClassIdentifier` / `m_Name` / 보존된 필드 이름이다
(산출물: `Tools/verify/missing-evidence.json` — 2026-08-31 에 레포 안으로 옮겼다).

**A. 프로젝트 자기 코드 — 3개 GUID / 132 참조 (실제 문제)**

| 참조 | ident | 보존된 필드 |
|---|---|---|
| 108 | `VeyTrace.Rendering.Occlusion.OcclusionSection` | `renderers`, `colliders` |
| 12 | `VeyTrace.Rendering.Occlusion.ElevationLevel` | `contentRoot`, `contentRenderers` |
| 12 | `VeyTrace.Rendering.Occlusion.ElevationStack` | (없음) |

세 개가 **한 서브시스템**이다. 필드값이 살아 있으므로 클래스를 복원하면 배선도 살아난다.
지우면 그 오소링 기록이 사라진다 — 되돌릴 수 없는 쪽이다.

**B. 서드파티·테스트 잔여물 — 11개 GUID / 32 참조 (자기 코드 아님)**

| 참조 | 무엇이었나 | 어디 |
|---|---|---|
| 12 | `rotateVector`, `rotateSpace` (회전 스크립트) | `50.Art/VFX/.../Portals/*` |
| 5 | `waveFunction/amplitude/phase/frequency` (오실레이터) | 같은 Portals |
| 5 | `version`, `hdPluginSubTargetMaterialVersions` | `.mat` — **HDRP** 플러그인 메타. 이 프로젝트는 URP 다 |
| 3 | `ConvexMeshes`, `HashOfSourceMeshes` | `50.Art/MapGen/.../*_Convex.asset` (메시 캐시) |
| 1 | (필드 없음) | `INab Studio/.../Character Fire.vfx` |
| 1 | `TestAnimationCurveVolumeComponent` | `99.Settings/DefaultVolumeProfile.asset` |
| 1 | `OasisFogVolumeComponent` | 같은 파일 |
| 1 | `OutlineVolumeComponent` | 같은 파일 |
| 1 | `TestVolume` | 같은 파일 |
| 1 | `ScreenSpaceAmbientOcclusion` | `INab Studio/Common URP/URP Renderer.asset` |
| 1 | `materialNameToModify`, `materialToModify` | `50.Art/TestAssets/KMKTestAssets/...` |

`DefaultVolumeProfile.asset` 하나에 죽은 Volume 컴포넌트가 **4개** 있다 — 전부
이름에 Test/Oasis/Outline 이 붙은 것이라 실험하다 남은 것으로 보인다.

**⚠️ 2026-08-27 세션은 아무것도 지우지 않았다.** A 는 "의도된 삭제였나" 를 사람이
알아야 하고(132 참조의 오소링이 걸려 있다), B 는 서드파티 에셋을 건드리는 일이라
재임포트로 되돌아올 수 있다. 분류까지가 도구의 몫이고 그 다음은 판단이다.

정리한다면 B 부터가 안전하다 — 특히 `DefaultVolumeProfile.asset` 의 죽은 Volume 4개는
자기 코드와 무관하고 참조가 각 1건이다.

---

## 6. 검증 방법

**무엇을 언제 돌리나.** Unity 없이 도는 것부터 적는다 — 대부분 Unity 가 필요 없다.

| 언제 | 명령 | Unity |
|---|---|---|
| JS 를 고쳤다 | `node Tools/probe-*.js` (8개 스위트) | 불필요 |
| 인덱스 구조를 고쳤다 | 위 + `node Tools/sweep-field-checks.js` | 불필요 |
| C# 을 고쳤다 | 오프라인 Roslyn 컴파일(아래) → `unity_recompile_scripts`(약 25 s) | 필요 |
| 새 파일을 커밋한다 | `unity_recompile_scripts` 로 `.meta` 생성 | 필요 |
| 새 엣지를 주장한다 | `Window/MCP Dependency Cross-Check` 로 Unity 와 대조 | 필요 |
| 독립 오라클과 맞춘다 | `node Tools/prototype/missing-scripts.js <프로젝트>` | 불필요 |

⚠️ `Tools/prototype/*` 는 **덜 찾는다** — 확장자 화이트리스트를 쓴다. 그게 한계이자
교차검증의 값이다. 수치 차이의 규명은 `Tools/prototype/README.md` 에 있다.


### 오프라인 C# 컴파일 (Unity 안 열고)
```bash
U="/c/Program Files/Unity/Hub/Editor/6000.3.16f1/Editor/Data"
dotnet "$U/DotNetSdkRoslyn/csc.dll" -nologo -target:library -nostdlib+ -noconfig -langversion:9 \
  -define:UNITY_EDITOR -out:out.dll @bcl.rsp @refs.rsp @srcs.rsp
dotnet build-server shutdown
```
- `bcl.rsp` = `$U/NetStandard/ref/2.1.0/netstandard.dll` + `$U/NetStandard/compat/2.1.0/shims/**/*.dll`
- `refs.rsp` = `$U/Managed/*.dll` + `$U/Managed/UnityEngine/*.dll` + Newtonsoft.Json.dll
- `srcs.rsp` = `Editor/**/*.cs` **단 `InputTools.cs` 제외** (Unity.InputSystem 은 소스 배포)
- rsp 안의 경로는 공백 때문에 **반드시 인용부호로 감쌀 것**

### 브릿지 인덱스 도구 단독 테스트
브릿지는 SSE 로 계속 살아있으니 백그라운드로 띄우고 kill 해야 한다.
```bash
( cat requests.txt; sleep 20 ) | node Bridge/mcp-bridge.js > out.txt 2> err.txt &
BPID=$!; sleep 25; kill $BPID
```
`requests.txt` 는 JSON-RPC 한 줄씩. `initialize` 를 먼저 보낼 것.
⚠️ `unity_index_status` 는 빌드를 강제하지 않으니 **다른 질의 뒤에** 두어야 통계가 나온다.

### 라이브 서버 직접 호출
```bash
TOK=$(python -c "import json,io;print(json.load(io.open(r'C:/Users/u5519/.unity-mcp/auth-token-3000.json',encoding='utf-8'))['token'])")
curl -s -X POST http://127.0.0.1:3000/message \
  -H "Content-Type: application/json" -H "X-Unity-MCP-Token: $TOK" \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}'
```

### 회귀 기준 (2026-08-24 감사 후 재측정)
- Missing Script: `coverage=full`, `missingScriptCount` **4**, `affectedAssetCount` **111**,
  `assetGuidPairCount` **133**, 상위 3건(오클루전) `referencingAssetCount` 합계 **132**.
  ⚠️ 2026-08-31 에 10/133/160 에서 내려왔다 — **회귀가 아니라 B군 정리다**(문서 28개, §5).
  남은 4 = 오클루전 A군 3 + VFX 그래프가 참조해 건너뛴 1. **더 줄면 A군이 지워진 것이니 먼저 물을 것.**
  ⚠️ 2026-08-28 에 14/134/164 에서 내려왔다 — **회귀가 아니라 정리다**(`DefaultVolumeProfile.asset`
  의 고아 컴포넌트 9개 제거, §5). 그리고 `scriptlessComponentCount` 는 **0** 이어야 한다(그전 5)
  (필드 이름은 2026-08-28 에 바뀌었다 — 옛 이름은 `totalReferenceCount`/`referenceCount`. §4-(35).
  부분 커버리지에서는 missing 0 / `exists: null` 이 정상이다 — §4-12)
- 메서드 줄 범위 — **`node Tools/probe-method-lines.js` 로 재실행한다**(프로브 8·9·10).
  전수 디스크 대조 **5,313/5,313 (0% 불일치)** · 겹치는 범위 **0 쌍** · 음수·역전 **0**.
  모집단은 줄 있음 **5,960** / 없음 **1,305**(Assembly-CSharp 만 3,005 / 689).
  2026-08-28 이전 값(27.4% / 3,036 / 126)은 **버그였다** — 회귀 기준이 아니다(§4-(34))
- 호출 그래프: `BaseAttack::TryResolveHit` → 호출자 **8개** — 단 이것은 **오버로드 4개의 합계**다
  (본문 시작 132/155/167/178, 선언 130/153/165/176). `declarations` 가 4개로 나와야 한다.
  `Unit::TakeDamage` 는 선언 2개(62, 147)에 호출자 9개
- 오류 줄 분류(`lineKind`) — **`node Tools/probe-error-impact.js`**(프로브 15~19, 22~26).
  gap 줄의 분류율 **86.0%**(7,706 중 6,630), unknown 13.9%. `file-level` 은 `method: null` 이어야 하고,
  본문(exact) 줄은 `in-method-body` 여야 한다(프로브 21). 파일 뷰 누출 0(프로브 20)
- IL 디코딩 실패 **0**, 엣지 **8,673**
- 참조 엣지 **6,305** = 위 분해는 6,267 시점의 것이다(ProjectSettings 를 스캔 루트에 넣기 전).
  분해: `guid:` 형태 + 맨 GUID **18** + `.meta` **122** + 기타 텍스트 **83** + 경로 로드 **23**
  - 자기 참조 **0** (에셋이 자기 GUID 를 적어둔 것은 참조가 아니다)
  - 텍스트 일치로만 얻은 대상(`weakRefs`) **58**
- 값 파서 전수: 문서 **69,891**, 못 읽은 줄 **0**, 상한에 걸린 문서 **24**, 정규식 대조군 누락 **0**
- 전수 집계 — **`node Tools/sweep-field-checks.js` 로 재실행한다.** 2026-08-28 에 이 수치가
  재현되는 것을 처음 확인했다(그전까지는 방법만 적혀 있고 수행하는 스크립트가 없었다).
  스크립트 컴포넌트 **7,019** / 타입 해석 **864** / 패키지라 미해석 **5,554** /
  Assets 안 미해석 **0** / 없는 스크립트 **601** / 필드 검사 864 / 베이스 체인 미완 242 /
  `m_EditorClassIdentifier` 대조 **511**, 불일치 **11** (전부 클래스 이름 변경 — §4-10)
  - **낡은 키 가진 컴포넌트 19 는 "확신" 케이스다** — 베이스 체인이 사용자 어셈블리 안에서
    끝나 "이 키에는 필드가 없다" 고 단정할 수 있는 것. 확신/불확실을 뭉개서 세면 **145** 가
    나오고 회귀처럼 보인다. 나머지 126 은 베이스가 패키지로 나가 도구가 이미 note 로
    inconclusive 라고 말한다(예: `ShowTopMostFoldoutHeaderGroup` 은
    `Unity.Netcode.Components.NetworkTransform` 의 필드다)
  - **"전체 커버리지" 는 GUID 해석 범위**를 뜻한다(`includePackageCache: true`, GUID 27,321).
    문서 **스캔** 범위는 Assets 다 — PackageCache 에셋까지 훑으면 컴포넌트가 28,319 로 뛴다
  - 2026-08-28 실측은 7,020 / 5,555 / 513 으로 +1/+1/+2 다. 프로젝트가 자란 것이다.
    스크립트는 **감소만** 실패로 보고, `unresolvedInAssets != 0` 은 무조건 실패로 본다
- ECID 승격 — **`node Tools/probe-ecid-promotion.js` 로 재실행한다**(19항목, 약 14 s).
  승격 대상 **1,213**(= 미해석 중 ECID 있음) = 붙음·스크립트없음 **560** + 붙음·패키지 **530**
  + 안 붙은 문서 **123**. 승격으로 **못** 답하는 것은 붙음·패키지·ECID없음 **292** 다.
  해석에 성공한 컴포넌트에 붙은 승격은 **0 이어야 한다**(프로브 14 — 여기가 1 이라도 되면
  낡은 ECID 가 검증된 타입을 덮은 것이다). ECID 원문은 전수 **1,726건이 전부 `::` 정확히
  1개인 표준형**이고, 그래서 엄격 파싱으로 잃는 것이 0 이다.
  ⚠️ 이 수들은 **줄면** 문제다(도구가 덜 답한다는 뜻). 늘어나는 것은 프로젝트가 자란 것이다.
  대가(실측 2026-08-29): 도구 정의 **+288 B**, 승격이 있는 응답 **+10.7%**, 캐시·콜드빌드 **0**
- 경로 봉쇄: 12케이스(절대/UNC/상대탈출/Assets 경유/역슬래시/접두사 유사/디렉터리 등) 전부 거부,
  프로젝트 밖 내용 유출 **0**
- 레이어 D: 인스펙터 배선 **24**건(조인 해석 22) / 타입 이름 참조 **48**타입·**52**엣지 /
  속성 붙은 메서드 **243**개 / 경로 로드 해석 **25**·미해석 **2**·동적 **44**
  - `GameManager::GoToResultButton` → 코드 호출자 **0**, 인스펙터 배선 **2**
  - `BombAction` → 붙은 에셋 **0**, 타입 이름 참조 **1**(`Wells.asset`)
- P3-a 신선도: 어셈블리 서명 **378 파일 / 11~18 ms**, 전체 지문 **5,825 파일 / 678~702 ms**
  - `probe-verify-loop.js` — 그때 **7/7**, **현재는 8/8**(2026-08-28 에 8번을 넣었다: 깨진 로드
    경로를 목록으로 내는지). 웜 질의는 검증을 붙이고도 **200 ms 미만**(실측 13~15 ms)
  - `unity_get_compilation_status` 응답 약 **612 B**(긴 note 붙는 최악), 통과 시 note 는 빈 문자열
- `tools/list` — 레이어 D 시점 **82개 / 39,669 B**(그때 도구를 늘리지 않았다는 근거).
  ⚠️ **85개 / 43,368 B** 가 2026-08-28 기준이다(P1 +1,236 · P2 +1,237 · P3-b +1,226).
  이 줄의 82 를 지금 기준으로 읽으면 도구 3개가 회귀로 보인다.
  2026-08-29 에 ECID 승격 설명으로 **+288 B** 늘었다 — 도구 **개수는 그대로 85** 이고
  `unity_get_asset_components` 설명만 1,802 → 2,090 B 다. Unity 없이 잴 수 있는
  로컬 12개 배열은 10,868 → **11,156 B**
- `Tools/prototype/` 의 프로토타입과 수치가 일치해야 한다

⚠️ 이 수치를 다시 잴 때 **도구를 파일마다 1회 호출하는 방식으로 재지 말 것.**
`getAssetComponents` 는 파일당 500개 페이지 상한이 있어 컴포넌트 39,921개가 빠진다(§4-16).
`Bridge/index/*.js` 를 직접 require 해서 문서를 전부 도는 방식으로 잰다. 필드 검사는
`queries._checkFields`(측정용 시임)를 쓰면 도구와 같은 코드로 잴 수 있다.

---

## 7. 파일 지도

```
Bridge/
  mcp-bridge.js            토큰 헤더, SSE 캐리버퍼, 로컬 도구 가로채기, tools/list 병합
  index/
    scan.js                레이어 A — .meta GUID, YAML 역참조, PackageCache 지연 병합
    symbols.js             레이어 B — TypeDef/Field/MethodDef + Portable PDB 소스 매핑
    callgraph.js           레이어 B-2 — IL 옵코드 테이블, 호출 엣지 추출
    clrmeta.js             ECMA-335 컨테이너/테이블 파서 (DLL + Portable PDB 공용)
    queries.js             질의 + 조인(resolveScriptType) + 페이지네이션 + 컴포넌트 값 조립
    wiring.js              레이어 D — 인스펙터 배선(UnityEvent) + 타입 이름 문자열 참조
    yamlvalues.js          Unity YAML 값 파서. splitDocuments(text)->{lines,docs}, parseDocument(lines,doc)
    sigtypes.js            ECMA-335 시그니처 디코더 + **blob 압축 정수의 단일 출처**
                           (Type II.23.2.12 / FieldSig II.23.2.4 / MethodDefSig II.23.2.1)
    methodkey.js           **메서드 키 파싱의 단일 출처.** Type::Method 와 Type::Method(int,string)
                           둘 다 다룬다. 인자 목록은 **마지막 `(`** 로 끊는다(이름에 괄호가 들어간다)
    errorimpact.js         P3-b — 컴파일 진단×인덱스 조인. Unity 왕복 없음. 확신도 3단계
                           + `lineKind`(그 줄이 무엇인가) 와 필드 조인. file-level 은 귀속 안 함
    projectmap.js          P1 — 배치·진입점 지도(중심성 랭킹은 측정으로 기각)
    impact.js              P2 — 영향 분석. 축을 합치지 않고 unknown 을 싣는다
    tools.js               로컬 도구 정의/디스패치, 신선도 2단 검사, 디스크 캐시(**버전 16**)
Editor/Core/
  McpPathGuard.cs          경로 포함 검사
  McpAuthToken.cs          세션 토큰 + projectRoot 기록
  McpPaging.cs             페이지네이션 규약
Editor/Tools/
  CompilationTools.cs      컴파일 상태 + **진단 완결성**(diagnosticsState/generation/observed)
  ConsoleTools.cs          콘솔 로그 + **버린 개수 회계**
  SearchTools.cs           search_project. reference 경로는 folder 를 지키고 상한에서 거절한다
Tools/
  probe-*.js               인수 프로브 8개 스위트 (§0.5 계측기 표 참조)
  sweep-field-checks.js    §6 전수 집계 재측정
  verify-effect-by-operation.md  effectByOperation 실검증 절차·관측
  prototype/               **독립 오라클** — 출하와 다른 경로로 같은 질문에 답한다.
                           덜 찾는다(확장자 화이트리스트). 수치 차이 규명은 그 README 에
LOCAL_DEV_SETUP.md         레포 분리 / manifest skip-worktree / pull 복구
```

기획 문서는 게임 프로젝트 밖 별도 위치:
`C:\Users\u5519\Downloads\unity-mcp-ai-improvement-plan\`
- `11_UNREAL_FEST_FINDINGS_AND_WORKCHECK.md` — 근거·번복 기록·작업체크
- `12_PHASE1_5_INDEX_ARCHITECTURE.md` — 아키텍처 결정과 실측
- `evidence/` — 슬라이드 전사 전문, codex 소스 감사 전문
